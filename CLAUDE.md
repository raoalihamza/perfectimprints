# CLAUDE.md

Project context for Perfect Imprints website rebuild. Read this before any task.

## 1. Project

Rebuild perfectimprints.com as a static, SEO-first content site that funnels traffic to a Geiger e-commerce subdomain. 22,180 category pages (465 roots, 576 modifiers, 21,137 facets, 2 compound facets) and 731 blog posts. Not an e-commerce site. No checkout, no cart, no product detail pages owned by this site. Outbound links only.

Production domain: `perfectimprints.com`
Staging domain: `dev.perfectimprints.com`
Affiliate target: `https://patrickblack.geiger.com/`
Geiger source: `https://www.geiger.com/` (data source only, never emitted in links)

## 2. Stack

| Layer           | Choice                                                                      |
| --------------- | --------------------------------------------------------------------------- |
| Framework       | Next.js 15, App Router, TypeScript strict                                   |
| Styling         | Tailwind CSS                                                                |
| CMS             | Sanity v3 (hybrid model, see Section 7)                                     |
| Hosting         | Vercel (Next.js native platform)                                            |
| DNS             | Cloudflare (DNS-only mode, CNAME to Vercel)                                 |
| AI content      | DeepSeek-V3 via API                                                         |
| Email           | Gmail SMTP via Nodemailer                                                   |
| Search          | Fuse.js with prebuilt JSON index                                            |
| Data pipeline   | Python with httpx HTTP/2, tenacity, beautifulsoup4, rapidfuzz, orjson, tqdm |
| Package manager | pnpm                                                                        |
| Node            | 20 LTS or higher                                                            |

## 3. Architecture Principles

Static-first (hybrid SSG). The headline category surfaces — 465 root pages + 576 modifier pages + 2 compound-facet pages, plus their pagination variants (~1,840 paths total) — render at build time as static HTML and ship in the Vercel deployment. The 21,137 long-tail facet pages render via on-demand SSG (`dynamicParams = true` + `revalidate = false`): the first hit (typically a Googlebot crawl) generates the page and Vercel caches it at the edge permanently until the next deploy. This is a forced deviation from "every page at build time" because pre-building all 22,180 pages plus pagination (~34,857 paths) blows Vercel's per-deployment output budget — Next.js 16 emits ~5 segment artifacts per static page, hitting `ENOSPC` on the build runner. Crawlers and users see identical static HTML once a page is warm. To keep these pages static the `/cat` render path reads NO `searchParams`, makes NO uncached Sanity call (both force Next into dynamic rendering), and has NO render-time `useSearchParams()` in any client component under the route (that one keeps the `●` marker but silently swaps the prerendered HTML for the loading skeleton via CSR bailout — M-SEO5) — faceted filtering runs in a separate dynamic API route called client-side (the URL query is read post-mount from `window.location`), and all Sanity reads are cache-tagged (see Section 13 "Keeping /cat static" and Section 7 owned-slug precedence). Sanity-managed pages use ISR with on-demand revalidation triggered by Sanity webhooks. See Section 13 and `app/cat/[...slug]/page.tsx` for the implementation, and TASKS.md M3-306 for the incident write-up.

URL preservation is non-negotiable. The 22,180 existing category URLs and 731 blog URLs from GA4 must resolve to the same path on the new site. No 301 redirects for migrated URLs.

Hybrid content model. Bulk AI-generated category pages live as JSON files in the repo. Sanity holds curated categories, custom categories, custom products, blogs, home page, mega menu, FAQs, global settings, and anything Patrick edits.

Data baked at build time. Product catalog and category data are scraped once and committed to the repo as JSON. Production builds never call the Geiger API at runtime. A scheduled monthly rebuild refreshes the data and redeploys.

Outbound links open in the same tab unless noted. Affiliate links to Geiger use the patrickblack.geiger.com host. Links to non-Geiger partner sites use whatever URL is set on the custom product document.

Category CTA rule. A category renders the lead-form CTA instead of the product grid based ONLY on the three `shouldShowEmptyStateCTA` rules: `productSkus.length === 0` (empty-skus, Tier 3/4 fallback), `skuFilterMode === 'full-capped-60'` (slug-token filter failed → off-topic top-60 of a parent department), and manual `forceCTA === true` in the JSON. Categories with real matched SKUs — including fuzzy/override mappings like tote-bags — render their product grid.

**The `full-capped-60` false positives, and why 7 pages were fixed in the DATA rather than in the rule (2026-08-10, Patrick's `/cat/pens` report).** The rule is correct but the input can be wrong: the AI pipeline's slug-token filter scores a candidate by asking "does the product NAME contain the category slug?", and real pens are called `Bic® Clic Stic®` / `Javalina®` / `Sharpie®`, so `pens` scored near-zero, fell to `full-capped-60`, and `/cat/pens` rendered the lead form while its JSON held 60 perfectly good pen SKUs. **Do NOT relax the rule to fix this.** An audit of all 66 `full-capped-60` roots (all 66 are root-type - the slug-filter tier only runs on root mappings, so facets are unaffected) found **58 of them are mapped to a BROAD PARENT DEPARTMENT rather than their own product type** - `desk-organizers`, `laser-pointers`, `letter-openers`, `memo-boards`, `notepads`, `staple-removers` and `stock-shaped-notepads` all point at `Home > Office & Technology` (1,738 items), so dropping the rule would render the same 60 unrelated office products on seven different pages, which is exactly the off-topic-grid complaint the rule was added to fix. Those 58 have a **mapping** problem, not a filter problem, and are deliberately untouched. The **7 whose Geiger path genuinely IS their product type** (or, for the three department-level PI slugs, genuinely is that department) were re-derived to the pipeline's own `full` tier by [scripts/monthly/expand-safe-capped-categories.ts](scripts/monthly/expand-safe-capped-categories.ts) (`pnpm monthly:expand-safe`, `--dry-run` supported): `pens` 60→365 (→374 with the sibling path below), `office` 60→1738, `bags` 60→1015, `health` 60→617, `medical-healthcare-items` 60→362, `calendars` 60→87, `workwear` 29→29 (mode-only). The allowlist is hard-coded in that script with a per-slug reason; **do not add a slug to it without re-running the mapping audit for that slug**. The derivation was validated against every target's recorded `rawSkuCount` and matched exactly. It re-derives on every run (a `skuFilterMode === 'full'` early-exit was deliberately removed - that guard would have made the re-run-after-a-Full-Catalog-Rebuild path a silent no-op precisely when a fresh catalog has new products for these pages), and reports "no change" when the input is unchanged. **It now runs INSIDE the Full Catalog Rebuild** ([.github/workflows/monthly-rebuild.yml](.github/workflows/monthly-rebuild.yml), the assemble job, immediately after the prune and before the summary so its changes reach the diff / PR / email; same `needs.scrape-ab.result == 'success'` guard as the prune). That step is what stops these seven pages shrinking a little with every rebuild - nothing else in the job ADDS products to an existing page (`generate_content.py --skip-existing` skips existing category JSONs and `prune-removed-skus.ts` only ever REMOVES dead SKUs), so **new Geiger products still never enter any OTHER existing category page by themselves**. Doing that for all 465 roots is separate, un-started work.

**Sibling paths (`alsoInclude`), and the pens case (FIX-820, 2026-08-11).** An allowlist entry may pull in ONE Geiger path that is a SIBLING rather than a descendant of its mapping, with a per-entry `exclude` predicate. Only `pens` uses it, for `Home > Writing Instruments > Name Brand Writing` (Patrick asked for that shelf by name). **It is a BRAND shelf, not a product type:** of its 36 products, 21 already carried a Pens-subtree path (so they were already on the page) and 6 of the remaining 15 are Sharpie markers, a Post-it highlighter and a uni-ball pencil. `isNotAPen` drops those on two rules - cross-listed under `Highlighters & Markers` or `Pencils` (catches the four Sharpies, whose names never say "marker", plus the pencil), or the NAME/`product_type_unigram` says highlighter/marker/pencil (catches the Post-it highlighter, which Geiger cross-lists nowhere). It deliberately does not read the description, where a real pen's copy can mention a marker in passing. Net: **9 real pens added, 365 → 374, zero removed.** The filter applies ONLY to sibling-path arrivals; the mapped path is taken unconditionally and tested first, so a sibling rule can never drop a product the page already showed. (An exact-match-only "Geiger-menu" gate was tried in M5-504 part 1 and **reverted as too aggressive**: it flipped ~10,694 categories to CTA, including genuine product-bearing ones. The handful of genuinely off-topic categories - e.g. dog-tags, PPE - are fixed by **targeted `categoryOverride` docs** (`forceCTA` / `hiddenSkus`, Section 7), not a site-wide rule.) Audit the CTA split with `pnpm audit:category-rule` → [docs/category-rule-audit.md](docs/category-rule-audit.md); reconcile against Patrick's confirmed old-URL spreadsheet with `pnpm reconcile:missing-urls` → [docs/missing-url-reconciliation.md](docs/missing-url-reconciliation.md).

## 4. URL Structure

All preserved exactly. Slug formats already determined by the existing site. Total: 22,180 valid category URLs and 731 blog posts (verified via `pnpm import-urls` against GA4 export).

Category URL breakdown:

- Home: `/`
- Category root: `/cat/[slug]` (**465** root categories, 1 segment after `/cat/`)
- Category modifier: `/cat/[root]/[modifier]` (**576** modifier pages, 2 segments). Six modifier types observed: `search` (258), `no-minimum` (216), `closeout` (93), `production-time` (6), `eco-friendly` (2), `material` (1)
- Category facet: `/cat/[root]/[facet-type]/[facet-value]` (**21,137** standard facet pages, 3 segments, 36 facet types)
- Category compound facet: `/cat/[root]/[type1]/[value1]/[type2]/[value2]` (**2** compound facet pages, 5 segments, two filter dimensions)
- Category pagination: `/cat/[slug]/page/[n]` for any category with more than 60 products (60 per page). Page 1 lives at the clean URL `/cat/[slug]` and is canonical to itself. Pages 2+ live at `/cat/[slug]/page/N`, carry `noindex,follow`, and canonical back to page 1. `/cat/[slug]/page/1` 308-redirects to the clean URL. Out-of-range `/page/N` returns 404. Only page 1 URLs appear in the sitemap. Build-time generation: pagination variants for root + modifier + compound-facet categories only (~797 paths); facet pagination renders via on-demand SSG with the parent facet page (see Section 3).
- Blog index: `/blog` (**carries the site search box beside the H1** as of Q-170 improvement 3, 2026-08-02, via the shared [components/layout/IndexHeadingWithSearch.tsx](components/layout/IndexHeadingWithSearch.tsx). Heading takes the row, search takes a quarter on desktop and stacks full-width under it below `lg`. The box is the EXISTING `SearchBox`, reused not forked; it searches the WHOLE site and lands on `/search`, deliberately not scoped to blogs; a blog-scoped variant is a separate feature and a question for Patrick, see [docs/quick-quote/Q-170-verification-report.md](docs/quick-quote/Q-170-verification-report.md). **Staticness was the risk and is preserved:** `IndexHeadingWithSearch` is a server component holding no state, reading no `searchParams` and fetching nothing, and no client module in the search island calls `useSearchParams`, and that hook would have silently swapped the prerendered HTML for the loading skeleton while the build still reported `●` (Section 13, the M-SEO5 lesson). `SearchBox` gained one optional additive prop, `panelClassName`, so the dropdown can grow leftwards out of a quarter-width column; unset, the panel is byte-identical to before. **Placeholder wording (FIX-830 task 3, 2026-08-14, Patrick's request): the blog index box reads "Search Blogs" and the video index box "Search Videos"** - derived from `priorityType` inside `IndexHeadingWithSearch` rather than passed per page, so `/blog` and `/blog/page/N` structurally cannot drift (a box worded differently on page 2 reads as a bug). **WORDING ONLY: what is searched is unchanged** - both still search the whole site and land on `/search`; scoping them to their own content type remains the separate agreed piece of work. `SearchBox` gained one additive optional `label` prop (default `'Search'`, so the header box is byte-identical) feeding the existing sr-only `<label>`, so a screen-reader user hears the same wording a sighted one reads. No state, no hooks, no `useSearchParams`: both index pages stay static. **Group order (Q-180, 2026-08-07): the index-page boxes put the matching content type's result group FIRST (blogs first on `/blog` + pagination, videos first on `/videos`) via a second additive `SearchBox` prop `priorityType` passed through `IndexHeadingWithSearch`; the ordering rule is the pure `orderedSearchGroups` in [lib/search/group-order.ts](lib/search/group-order.ts). Reordering alone is NOT enough: the global top-50 over ~30k index entries crowds out every video/blog on a broad query ("custom"), leaving the lifted group EMPTY even though matches exist (found live on /videos, 2026-08-07). So when `priorityType` is set, `search()` also takes `{ensureType, ensureCount}` and guarantees the group its best matches from a lazy TYPE-SCOPED Fuse index (same options, tiny subset, appended via the pure `mergeEnsuredResults` without reordering or rescoring the global list). The search stays site-wide, the static index / server `/search` / ranking options / per-group caps are untouched, and the header box (no prop) searches exactly as before.**)
- Blog index pagination: `/blog/page/[n]` (same search box; an index whose search field vanishes on page 2 reads as a bug)
- Blog post: `/blog/[slug]` (731 posts)
- Blog category: `/blog/cat/[slug]`
- Brand index: `/brands` (new, added 2026-05-26 per Patrick feedback)
- Brand page: `/brands/[slug]` (new, per-brand product listings)
- Deals page: `/deals` (new, aggregator of all on-sale/closeout products)
- New products page: `/new-products` (new, weekly Phase G aggregator)
- Rush products aggregator: `/rush-products` (new, weekly Phase H aggregator of Geiger's 24 Hour Rush Products collection; the "Rush Products" header nav item points here)
- Rush products (legacy preserved URL): `/rush-promotional-products`
- Services pages: `/services/[slug]`
- Custom top-level pages: `/[...slug]` (Issue 2 — any published `page` doc with a non-reserved slug renders at its own top-level address via the **root catch-all** [app/[...slug]/page.tsx](app/[...slug]/page.tsx), for recreating old-site URLs like `/llm-info-perfect-imprints`; reserved slugs — all existing routes + `api`/`admin` + the 8 static slugs — are blocked at the schema and route level. **The route is a catch-all, NOT a single `[slug]` segment, so a page slug MAY contain slashes and renders at its full multi-segment path** (e.g. slug `industry/hvac` → `/industry/hvac`, `facets/special-feature/made-in-usa` → `/facets/special-feature/made-in-usa`). It was originally a single-segment `[slug]` route, which 404'd any slash-containing slug even though the doc published fine (the schema reserved-guard only blocks single top-level segments); converted to `[...slug]` so multi-segment pages route. `params.slug` is a `string[]` joined with `/` to reconstruct the doc slug, matched verbatim by `getPageBySlug`. Dedicated folder routes (`/cat/...`, `/services/...`, `/blog/...`) always beat a root catch-all so nothing existing is shadowed, and the non-optional catch-all does not match `/` (served by `app/page.tsx`))
- Landing pages: `/[...slug]` (P2-AI-005 part 1 — any published `landingPage` doc renders its FIXED local/topic template (hero → local intro → options & ideas + product strip → why-us → FAQ → lead form) at its top-level slug, e.g. `/custom-beach-towels-destin-fl`, via the SAME root catch-all [app/[...slug]/page.tsx](app/[...slug]/page.tsx). **landingPage resolves FIRST, then the `page` fallback** — a slug used by both renders the landing page. Same reserved-slug guard as `page` (mirrored inline in the schema). Reads are cache-tagged `LANDING_TAG` + `landing:<slug>`; **`landingPage` must be in the Sanity webhook Filter** (manual, both environments) or publishes never revalidate. See Section 7 landingPage + Section 23 P2-AI-005.)
- Videos index: `/videos` (**same search box beside the H1** as `/blog`, Q-170 improvement 3, same shared component, same staticness guarantees)
- Video detail: `/videos/[slug]`
- Custom product detail page: `/products/[slug]` (P2-CP-001 part 1 — one page per published `productPage` Sanity doc: color-variant gallery, tiered pricing, description, related carousel, Get-a-Quote button. `generateStaticParams` prebuilds every published slug; **`dynamicParams = true` + `revalidate = false`** so a product published after the last deploy renders on-demand (the /services pattern) instead of 404ing until a rebuild. `products` is a reserved slug in [lib/reserved-slugs.ts](lib/reserved-slugs.ts) + both schema mirrors (page, landingPage). These products surface on /new-products, in related carousels, and in site search — deliberately NOT inside `/cat` pages. See Section 7 productPage.)
- Catalog lead pages: `/shop-by-theme/[slug]` + gated `/shop-by-theme/[slug]/catalog` (P2-CAT-001/002 — one pair per published `catalogPage` Sanity doc. The PUBLIC landing page (indexed, in the sitemap) carries the long-form SEO body + "Get the Catalog" CTAs at top/middle/end — each opens the seeded **`catalog-request` builder form** ([components/catalogs/CatalogCta.tsx](components/catalogs/CatalogCta.tsx) → `FormModalButton` with `hiddenFields={catalogSlug}`; unpublished form → /contact link fallback), and on submit the customer is **emailed that catalog's gated link (cc Patrick)** — see Section 7 catalogPage "Lead capture". The GATED page is **noindex + excluded from the sitemap + never a menu target** (hidden from Google, NOT auth — the emailed link is the advertised entry point) and renders the BROWSE INTERACTIVE CATALOG button + the catalog's products with the /deals-style client filter sidebar + Related Blogs/Videos strips; **the same strips also render at the bottom of the PUBLIC landing page** (below the end CTA — the shared [components/catalogs/CatalogRelatedContent.tsx](components/catalogs/CatalogRelatedContent.tsx), P2-CAT-005, so both pages show one consistent set and the indexed landing page carries them for SEO). The header's "Shop by Theme" dropdown lists the published LANDING pages (`pnpm seed-shop-by-theme-menu`, a targeted megaMenu patch). Both routes prebuild all published slugs with `dynamicParams=true` + `revalidate=false`. `shop-by-theme` is a reserved slug (lib + page/landingPage schema mirrors). See Section 7 catalogPage.)
- Private customer quote: `/quote/[token]` (Q-140 - the read-only page a customer opens from the link Patrick copies out of Studio. `token` is the quote document's slug: 32 lowercase hex chars, read from the PATH only, never a query param. **Hidden, NOT authenticated** - the gated-catalog model: `robots: {index:false, follow:false}` in `generateMetadata`, `referrer: 'no-referrer'` (per-route metadata, so the token cannot leak in a `Referer` header when the customer clicks a link), an `X-Robots-Tag: noindex, nofollow, noarchive` header from [next.config.ts](next.config.ts), never in the sitemap, never in a menu. `app/robots.ts` deliberately does NOT disallow `/quote` - a disallow would stop Google reading the `noindex`, which is worse; the reasoning is in that file. `dynamicParams = true` with **`generateStaticParams` returning `[]`** (prebuilding is wrong twice over: the token set changes every time a quote is written, and a baked list would put live private links in the deployment output) and **`revalidate = 86400`** rather than `false` - expiry is decided at render time, so a daily re-render bounds how stale the "Valid until" line can be; the tagged fetch inside `getQuoteByToken` still has `revalidate:false`, so a scheduled re-render costs no Sanity read and the webhook still updates the page in seconds. Unknown / malformed / empty / unpublished token returns a 404. **The customer's ACTIONS (Q-150) live in the client island [components/quote/QuoteActions.tsx](components/quote/QuoteActions.tsx) under this route: every value it renders arrives as a PROP baked at render, and it must never call `useSearchParams` - that hook forces a CSR bailout during prerender and silently swaps the whole page body for the loading skeleton while the build still reports `●` (Section 13, the M-SEO5 lesson). The page still makes exactly ONE Sanity read.** See Section 7 quote.)
- Customer response write route: `/api/quote-response` (Q-150 - the only thing that writes a `quoteResponse`. `nodejs` + `force-dynamic`, so it has no render path and cannot affect any page's staticness. See Section 7 quoteResponse.)
- Quote PDF download: `/api/quote-pdf/[token]` (Q-160 - [app/api/quote-pdf/[token]/route.ts](app/api/quote-pdf/[token]/route.ts). GET, `nodejs` + `force-dynamic` + **`maxDuration = 60`** (the Q-121 addendum measured a warm render at ~322 ms and a cold one at ~678 ms on the deployed function, so 60 is already enormous headroom; the bulk-import route's 300 was deliberately NOT copied - it only widens how long a pathological request can hang). The browser sends the TOKEN and nothing else that matters: the quote, every price and both contact blocks are re-read server-side via the same tag-cached `getQuoteByToken` the page uses, so no request field can change a number on the document. **Unknown / malformed / unpublished tokens get the same 404 the page gives**, so the route cannot be used to test whether a token exists (an EMPTY token cannot reach it at all - `/api/quote-pdf/` has no segment to fill and the platform 404s it first). **An expired quote still downloads**, with the expiry stated on the document: a customer must always be able to keep a copy of what they were quoted. Rate limited with the shared `createRateLimiter` at **40/hour per IP AND token** - deliberately far more generous than the response route's submission ceiling, because downloading is a normal repeatable action (save it, forward it, lose the attachment, download it again) and it creates no record. Response headers: `application/pdf`, `Content-Disposition: attachment` with the sanitized `quotePdfFileName` (`Quote-Q-1007-Perfect-Imprints.pdf`), `Cache-Control: private, no-store` (a private document must never sit in a shared cache), `X-Robots-Tag: noindex, nofollow, noarchive`, `Referrer-Policy: no-referrer`. A render failure returns a clean JSON 500 and logs loudly; the customer sees one sentence and the print fallback, never a raw error page. It has no render path, so `/quote/[token]` stays static. Never in the sitemap. See Section 7 quote "PDF".)
- Browse-all products page: `/promotional-products` (new, M5-506 — lists all ~7,957 products with Category / Price / Brand / Min-Qty facets, filtered/sorted/paginated entirely server-side so the full catalog never ships to the client; the "Promotional Products" root breadcrumb crumb points here, replacing the old dead `/cat` target). Indexable; filter/sort/page variants are `noindex,follow` + canonical to the clean URL.
- FAQ library: `/faq` (M5-506 — answered FAQs grouped into the 7-category taxonomy as `<details>` accordions + FAQPage JSON-LD. Singular `/faq`, matching the live site, NOT `/faqs`)
- Footer / legal static pages (M5-506, all built on the generic `page` builder, content reproduced from PI's OWN live site): `/about`, `/contact` (includes the lead form), `/sample-policy`, `/shipping-policy`, `/returns`, `/privacy-security`, `/company-core-values`, `/terms`. **Every slug mirrors the live perfectimprints.com URL exactly** (verified against the real footer links) so existing SEO equity / inbound links are preserved — matching the slug beats a redirect. (The original `/privacy` + `/terms` dev stubs were replaced; privacy lives at `/privacy-security` and terms at `/terms`.)

Trailing slashes: match the current site behavior. If GA4 export shows no trailing slash, do not add one.

Canonical host is www.perfectimprints.com. The apex perfectimprints.com redirects to www, configured at the Vercel domain level (set `www.perfectimprints.com` as the project's primary/production domain — Vercel issues the apex→www 308) plus a backup `next.config` `redirects()` rule. **No redirect middleware** — a static site must not run per-request middleware. **Every emitted URL (canonical, OG, Twitter, sitemap, robots `sitemap:`, breadcrumb absolute URLs, JSON-LD `url`/`@id`) derives solely from `NEXT_PUBLIC_SITE_URL`** — production = `https://www.perfectimprints.com` (set in Vercel), staging = `https://dev.perfectimprints.com`. The env value is used verbatim as the origin (only a trailing-slash trim); no string surgery forces or drops `www`, so flipping the env flips every URL. Every page file/SEO helper defaults to the www form when the env is unset.

The full URL list with classification lives at `data/pi-urls/category-urls.json` (built by `pnpm import-urls`). Every component that needs to enumerate URLs reads from there.

## 5. Folder Layout

```
/app                Next.js App Router routes
/components         Reusable React components
/lib                Utilities, Sanity client, scraper output loaders, affiliate URL helpers
/data               Committed JSON for bulk pages
  /categories       One file per AI-generated category page (encoded slug as filename)
  /geiger           Scraped Geiger data: categories.json, products.json, facet-memberships.json, brands.json, brand-logos/
  /mappings         pi-to-geiger.json
  /blogs            Raw blog scrape output (pre-Sanity migration)
/sanity             Sanity studio config and schemas
  /schemas          Document type definitions
  /components       Custom Studio components (including AI generate button)
  /actions          Custom Studio actions
/scripts            Build-time and one-off scripts
  /scrapers/geiger  Python scraper (config.py, client.py, discover.py, products.py, memberships.py, mapping.py, brand_logos.py, run.py, checkpoint.py)
  /scrapers/blogs   Python SeleniumBase UC-mode blog scraper (scrape_sbase.py)
  /ai-pipeline      DeepSeek content generation (deepseek_client.py, generate_content.py, prompts/)
  /search-index     Search index builder
/public             Static assets (logo, favicons, search-index.json)
/docs               Internal docs (this file lives here too)
```

## 6. Naming Conventions

- Files and folders: kebab-case
- React components: PascalCase
- TypeScript interfaces and types: PascalCase
- Sanity document types: camelCase
- JSON data files in /data/categories: filename matches the URL slug with slashes replaced by `__`, dot-json extension (e.g., `water-bottles__material__stainless-steel.json`)
- Environment variables: SCREAMING_SNAKE_CASE, prefixed by service (e.g., `SANITY_PROJECT_ID`, `DEEPSEEK_API_KEY`, `GMAIL_USER`, `GMAIL_APP_PASSWORD`)

## 7. Sanity Content Model

Document types and what each holds:

**curatedCategory.** Top-tier categories Patrick wants to manually edit. Fields: slug, title, hero copy, body sections, FAQs reference, hero image, mapped Geiger URL, related blog post references, meta title, meta description.

**customCategory.** The single doc type for full per-category control — both **brand-new** category pages and **taking over an existing baked JSON page** (the "push the JSON category pages to Sanity 1 by 1" model). The slug is the key: a published `customCategory` OWNS its `/cat/<slug>` and Sanity wins; otherwise the baked JSON renders. Fields: title (heading), slug, `isCustom`, `targetKeyword` (plural), `heroImage`, `heroCopy`, `introHtml` (portable text — paragraphs), `bodySections` (portable text — buying guide), `faqs[]` (**each FAQ's answer `a` is rich text — the `richAnswer` Portable Text type as of M5-516/Task B, so category-page FAQ answers can contain links; the FAQPage JSON-LD uses `portableTextToPlain(a)`. The answer is rendered ONLY through `<RichAnswer>` ([components/category/FAQsAccordion.tsx](components/category/FAQsAccordion.tsx)) — never interpolate `faq.a` directly into JSX, or a Portable Text block reaches React as a raw child and crashes the `/cat` prerender (M5-516 hotfix 2026-06-30; `RichAnswer` tolerates `string | PortableTextBlock[]`). Ship any future content-shape migration and its render code in the SAME release**), `productSkus[]` (editable Geiger SKU list — searchable product picker, see below), `externalUrl` (optional CTA), `seo`. **Renders at `/cat/<slug>` through the `/cat/[...slug]` route** via [components/category/CustomCategoryView.tsx](components/category/CustomCategoryView.tsx) (hero, intro, product grid **with the filter sidebar**, body, FAQs + FAQPage schema, breadcrumb + BreadcrumbList, CTA). No Geiger mapping required. Products = `productSkus[]` + its `customProduct`s (attached via `parentCategory`) + `productPlacement`/`categoryOverride` edits, merged by `mergeCategoryProducts` (removal wins, de-duped, live-resolved). CTA → contact form (`EmptyStateCTA`) when `externalUrl` is blank. **Filter sidebar (P2-CC-001, 2026-08-04):** owned pages render the same `CategoryShell` (FilterSidebar/sort/search-within) as baked pages — previously `CustomCategoryView` had no sidebar at all, so every pushed page silently LOST its filters on publish. Because the grid is fully hand-picked, the sidebar is built exactly like a Replace-products (curated) category: `renderCustomCategory` in [app/cat/[...slug]/page.tsx](app/cat/[...slug]/page.tsx) runs `buildAddedAttrOverlay` over the merged products (picked Geiger SKUs recover real tags from the reverse SKU→facet index; customProducts/productPages contribute their own tag fields) + `buildSidebarData(..., curated=true)`, and pins the new optional **`SidebarData.filterBaseUrl`** to the page's own URL so a filter click on a deeper slug (e.g. `headwear/theme/christmas`) stays on Patrick's page instead of jumping to `/cat/<root>` (the FilterSidebar default). [/api/category-products](app/api/category-products/route.ts) is customCategory-aware: owned slugs (via `getCategoryControlSets`, `!content` fallback for just-published pages) filter the customCategory's OWN merged product set in curated mode with the identical overlay build, so grid and filtered results can never disagree. Filter richness follows the product tags (productPage colors auto-derive from `colorVariants`; Material/Features/Types appear only when filled). Overlay/sidebar read only `facet-memberships.json` via fs — zero extra Sanity reads, no webhook/tag/env change, `/cat` stays static.

**Push to Sanity (taking over an existing page).** A Studio **tool** ([sanity/tools/push-category-tool.tsx](sanity/tools/push-category-tool.tsx), registered via `tools` in [sanity/sanity.config.ts](sanity/sanity.config.ts)) lets Patrick search all 22,180 slugs (via `category-list.json`) and "Push to Sanity": it GETs [app/api/sanity/push-category/route.ts](app/api/sanity/push-category/route.ts), which reads the baked JSON and returns pre-filled fields (`h1`→title, `metaTitle`/`metaDescription`→`seo`, `introHtml` HTML→portable text, `buyingGuideHtml`+`buyingGuideH2`→`bodySections`, `faqs`, `productSkus`), and creates a **draft** customCategory for Patrick to review. HTML→portable-text conversion is [lib/portable-text/html-to-blocks.ts](lib/portable-text/html-to-blocks.ts). Publishing the draft makes Sanity own the page; **deleting/unpublishing reverts to the baked JSON** (non-destructive, reversible).

**Owned-slug precedence + edited-set gating (performance).** Every Sanity read in the `/cat/<slug>` render path is a **cache-tagged** fetch (via the `useCdn:false` `cachedClient`, `{ next: { tags, revalidate: false } }`), NEVER `no-store` — so the route stays statically prerenderable (see Section 13) while the webhook revalidates edits in seconds. To avoid a Sanity lookup on each of ~22,180 baked pages, the route reads ONE shared tag-cached structure ([lib/sanity/queries/owned-categories.ts](lib/sanity/queries/owned-categories.ts), `getCategoryControlSets` — tag `category-control-sets`) giving two sets: `owned` (published customCategory slugs) and `edited` (`owned` ∪ slugs touched by a `categoryOverride`, `productPlacement`, or a `productPage.addToCategories` placement — P2-CP-004 batch 4). An `owned` slug renders from Sanity (`getCustomCategoryBySlug`, wins over JSON); only an `edited` slug runs the per-slug `getCategoryOverride` + `getPlacementSkusForCategory` + `getPlacedProductPagesForCategory` fetches (each tagged `cat:<slug>` — see [lib/sanity/cache-tags.ts](lib/sanity/cache-tags.ts)); **every untouched page renders straight from baked JSON with zero per-page Sanity calls**. Build-time baseline artifact `public/custom-category-slugs.json` ([scripts/build-custom-category-slugs.ts](scripts/build-custom-category-slugs.ts), in `prebuild`) is the offline owned-set fallback (and seeds `generateStaticParams` so owned pages prebuild). The webhook busts `category-control-sets` (membership change) + `cat:<slug>` (that page's content) + `revalidatePath('/cat/<slug>')` on publish/unpublish of a customCategory / categoryOverride / productPlacement (Next 16 2-arg `revalidateTag(tag, 'max')`), so a push/edit goes live within seconds without making the route dynamic.

**Generate with AI** (M5-505, customCategory-only, registered in [sanity/sanity.config.ts](sanity/sanity.config.ts)) — works on both new and pushed docs. POSTs title + `targetKeyword` to [app/api/sanity/generate-content/route.ts](app/api/sanity/generate-content/route.ts) (DeepSeek, server-side `DEEPSEEK_API_KEY`, v2 buying-guide prompt — plural keywords + custom/promotional/branded/personalized/logo/bulk/wholesale derivatives + buyer personas, no "Perfect Imprints" in H1/H2) and patches heading (`title`) + `seo` + `introHtml` + `bodySections` + `faqs` for Patrick to review (loading + error states, never auto-publishes). **FAQ answers are patched as richAnswer Portable Text** (the action converts DeepSeek's plain-string `a` via `plainTextToBlocks`, same as the push route — fixed 2026-07-12 after every AI-generated FAQ showed "Invalid property value" in Studio; pre-fix string answers were migrated by re-running `scripts/migrations/migrate-richtext-answers.ts`). Manual editing of all fields works equally.

**customProduct.** Products Patrick adds manually — covers non-Geiger vendors and any item he wants to feature beyond the auto-scraped catalog. Fields: title, image, description, `externalUrl` (any vendor; non-Geiger URLs pass through unchanged, Geiger URLs auto-rewrite via `lib/affiliate-url.ts`), parent category reference, display order. Placement controls: `placements.onDeals`, `placements.onNewProducts`, `placements.onRush` (booleans — toggle to surface the item on any of the three aggregators). Commerce fields (all optional, only required for `/deals`, `/new-products`, and `/rush-products` use): `brand`, `lowPrice`, `highPrice`, `msrp`, `minQty`, `productionTime`. Filter-tag fields (all optional — tag to make the product show up under that filter on the three aggregators AND on Replace-products categories, leave blank to skip): `colors[]`, `material`, `features[]`, `types[]` — use the same value names Geiger uses (e.g. "Blue", "Stainless Steel", "With A Lid") so the product participates in the Brand/Color/Material/Feature/Type filters alongside scraped Geiger products — plus boolean toggles `madeInUsa` / `ecoFriendly` / `closeout` for the Made-in-USA / Eco / Closeout filters. There is deliberately NO `newItem` field: the NEW badge or `placements.onNewProducts` already drives the normalized `is_new_item`, which every New Items filter reads (helper `customProductIsNewItem`); the CLOSEOUT badge also counts for the Closeout filter (`customProductIsCloseout`). `badges[]` (multi-select: `new` / `sale` / `closeout`) drives the ribbon on the product card. Used in four places: (1) inside category page product grids via `parentCategory` ref, (2) `/deals` when `placements.onDeals == true`, (3) `/new-products` when `placements.onNewProducts == true`, (4) `/rush-products` when `placements.onRush == true`. Normalization to the `GeigerProduct` shape lives in [lib/sanity/queries/custom-products.ts](lib/sanity/queries/custom-products.ts) (`customProductToGeigerProduct()`); the SKU is synthesized as `custom-<sanity-_id>` so it never collides with real Geiger SKUs.

**productPage.** Full custom product DETAIL page (P2-CP-001) — deliberately SEPARATE from `customProduct` (which stays the link-out card with a required `externalUrl`): a productPage is a product Patrick creates from scratch that lives ON the site at **`/products/<slug>`** with a color-variant gallery, tiered pricing, sale flagging, a rich description, a related carousel, and a **Get a Quote** button (no cart, no external link). **The page is a Geiger-style CONFIGURATOR (P2-CP configurator):** every multi-option property starts with a default selected (quantity = the minimum order quantity — max(explicit `minQty`, lowest tier); first color/size/decoration; 0/1-option properties render as fixed text), and changing the quantity live-recalculates an **ESTIMATED total** = qty × the matching tier's unit price + the RESOLVED setup charge (Q-100, 2026-07-30: the selected decoration method's own optional `setupCharge` wins when set, and an explicit 0 there means "no setup fee" and cancels the flat one - blank falls back to the optional product-level **`setupCharge`** field, Pricing fieldset, now the one-time DEFAULT fee; resolution happens ONLY in `effectiveSetupCharge` in [lib/products/quote-estimate.ts](lib/products/quote-estimate.ts), never a second formula) — always labeled "Estimated total — final pricing confirmed in your quote" (quote-based, no checkout, never a firm price). One client provider ([components/products/ProductSelectionContext.tsx](components/products/ProductSelectionContext.tsx)) owns the selection; the gallery is CONTROLLED by it (a swatch click updates the images AND the captured selection) and [components/products/ProductPurchasePanel.tsx](components/products/ProductPurchasePanel.tsx) renders the tier table (active tier highlighted), quantity/size/decoration inputs, and the estimate — all estimate math + the deterministic selection summary live in the pure client-safe [lib/products/quote-estimate.ts](lib/products/quote-estimate.ts) over the same `productPageValidTiers()` list, so table/highlight/estimate can never disagree. **The quote button opens the DEDICATED pre-filled quote form** ([components/products/ProductQuoteForm.tsx](components/products/ProductQuoteForm.tsx) + modal, lazy `ssr:false`; the earlier `LeadForm variant="productQuote"` was RETIRED and LeadForm/LeadFormModal restored — the shared attachment limits moved to [components/forms/attachment-limits.ts](components/forms/attachment-limits.ts)): Quantity/Color/Size/Decoration arrive pre-filled + editable with the recalculating estimate + a deterministic 1-line summary; entered from scratch: **First Name\*/Email\*/Date Needed\*** + optional Last Name/Phone/Company/Shipping Zip/Comments/artwork (this required set applies ONLY to the product form — the `/api/leads` productSlug branch relaxes lastName/phone/company; other forms keep the original requirements). The client POSTs ONLY a hidden `productSlug` + the selection fields; [app/api/leads/route.ts](app/api/leads/route.ts) resolves the doc's **`leadRecipient`** (Visibility fieldset, default patrick@, email-validated — mirrors `landingPage.leadRecipient`) AND the product title SERVER-SIDE via `getProductLeadInfo(slug)` (tagged read — a Studio edit re-routes in seconds; no open relay, and a spoofed hidden title can't lie to Patrick), sends the lead email with a `Product Quote Request: <title>` subject + a Configuration block (Product/Color/Size/Decoration/Estimated total rows), emails the customer a confirmation (same non-fatal policy as landing), and records `company`/`shippingZip`/`comments`/`productTitle`/`productSlug`/`selectedColor`/`selectedSize`/`selectedDecoration`/`estimatedTotal` + the resolved `recipient` on the `leadSubmission` (selection rows skip-when-empty, so non-product forms are byte-for-byte unchanged). Pricing display + card range + JSON-LD AggregateOffer all derive from the single `productPageValidTiers()` (minQty>0 + price>0, sorted, capped at the 5 the table shows) so they can never disagree; a manual related entry with no resolvable SKU and no URL is skipped (never a card linking to the bare affiliate homepage). **"Generate Product Details with AI"** ([sanity/actions/generate-product-with-ai.tsx](sanity/actions/generate-product-with-ai.tsx) → [app/api/sanity/generate-product/route.ts](app/api/sanity/generate-product/route.ts), `nodejs`+`force-dynamic`, registered in [sanity/sanity.config.ts](sanity/sanity.config.ts); needs `title`) drafts EDITORIAL fields only: the `description` (page-shape Portable Text, section H2s IN the body via `buildPageSectionsBody`, internal links auto-placed in 'page' shape — `{href, openInNewTab:true}` as of P2-CP-004), `seo.metaTitle`/`seo.metaDescription`, and — **only-if-empty** — `relatedKeywords`/`relatedCategorySlug`/`decorationMethods`/`relatedVideos`/`relatedBlogs`; Patrick's entered material/variant-colors/sizes are passed IN as context (the prompt forbids inventing specs) and **pricing tiers/images/sizes/brand/minQty/productionTime/leadRecipient are never generated or touched**. Studio-only `ai` fieldset (`aiTopicKeywords`, `aiSuggestedLinks`); no webhook/tag/env change (pure P2-AI-001 engine reuse). Never auto-publishes. Fields ([sanity/schemas/documents/product-page.ts](sanity/schemas/documents/product-page.ts)): title, single-segment slug (slash/uppercase rejected at publish — the `app/[slug]` lesson), brand, `colorVariants[]` (`{colorName, swatchHex?, images[≤10 with alt]}` — **clicking a swatch swaps the gallery to that color's images; the FIRST variant is server-rendered**) + `defaultImages[]` fallback, `pricingTiers[≤5]` (`{minQty, price}` — the QTY/PRICE table; card low–high price derives from these), `onSale`/`salePercentOff`/`saleExpires`, optional `minQty` (else lowest tier), portable-text `description` (page-builder `portableBody`, rendered via `pagePortableComponents`), `decorationMethods[]`, **`sizes[]` (listed only — NO price/image effect)**, `productionTime`, filter tags (`material`/`features[]`/`types[]`/`madeInUsa`/`ecoFriendly`/`closeout` — **colors derive from the variants**, and SALE/CLOSEOUT ribbons derive from `onSale`/`closeout`, no `badges[]` field), Related (`relatedCategorySlug` via `CategorySlugInput` + `relatedKeywords[]` for automatic matching; manual `relatedProducts[]` = `blogProduct` entries + references to `customProduct`/`productPage`, pinned first), `showInNewProducts` (default true), `seo`. **Normalizer** `productPageToGeigerProduct()` ([lib/sanity/queries/product-pages.ts](lib/sanity/queries/product-pages.ts)) yields a `GeigerProduct` with synthetic sku `custom-<_id>` (Item# auto-hidden; lines up with `augmentAggregator`'s filter-tag injection) and the **new optional `GeigerProduct.detailUrl`** — when set, [components/category/ProductCard.tsx](components/category/ProductCard.tsx) renders a same-tab internal `next/link` (no `sponsored` rel) instead of the affiliate `<a>`; products without `detailUrl` are byte-identical to before. **Surfacing:** its own page, the TOP of `/new-products` (via `getAugmentedNewProductsData({productPageDocs})` — prepended before custom products/pins/scrape, filter tags injected through `productPageAsCustomDoc()`), related carousels (`matchRelatedProducts` opt-in `includeProductPages` — default false because AI strips persist SKU-only entries that can't resolve the synthetic SKU), and site search (LIVE delta only, `product` entries with the new `SearchItem.internal` flag → `resultTarget` routes to `/products/<slug>`, never the affiliate host; the static prebuilt index and the server-side `/search` faceted page are untouched). **NOT inside `/cat` pages automatically — but attachable per-category**, two ways: the PRIMARY product-side **`addToCategories`** field (P2-CP-004 batch 4 — "Show on category pages" fieldset, the multi-select `CategoryPicker` with inline create-customCategory; render via `getPlacedProductPagesForCategory(slug)` in [lib/sanity/queries/product-placements.ts](lib/sanity/queries/product-placements.ts) — non-CDN `cachedClient` + `categoryTag(slug)`, NOT `PRODUCT_PAGES_TAG`, gated by the edited set whose control query now collects `productPage.addToCategories[]`; merged via `mergeCategoryProducts.placedProductPages` + the curated-mode filter overlay; the webhook busts each payload `addToCategories` slug + `CATEGORY_CONTROL_TAG` — **⚠️ detaches need the before()∪after() webhook Projection upgrade**, see [docs/sanity-webhook-setup.md](docs/sanity-webhook-setup.md)), or the category-side `categoryOverride.addedProducts` (batch 3, the advanced alternative — see categoryOverride above). Both normalize to sku `custom-<_id>`, so a both-ways attach renders ONCE. Patrick picks the categories; nothing is automatic. Route ([app/products/[slug]/page.tsx](app/products/[slug]/page.tsx)): `generateStaticParams` prebuilds all published slugs, `dynamicParams=true` + `revalidate=false` (new publishes render on-demand; `false` would 404 them until a deploy), emits Product JSON-LD (AggregateOffer from the real tiers, no fabricated availability/reviews), BreadcrumbList, `CustomSchemaJsonLd`, full OG/Twitter via `socialMeta`. **Static-safety:** the gallery ([components/products/ProductPageGallery.tsx](components/products/ProductPageGallery.tsx)) is a client component whose initial render (first color) is the prerendered HTML — no `useSearchParams`/URL reads; the page's auto related top-up passes `includeCustom:false` (the untagged `getAllCustomProducts()` read would flip the route dynamic — manual customProduct refs still render via the page's tagged GROQ deref). **Freshness:** all reads non-CDN `cachedClient` + `PRODUCT_PAGES_TAG` + `productPage:<slug>` ([lib/sanity/cache-tags.ts](lib/sanity/cache-tags.ts)); the webhook busts both + revalidates `/products/<slug>`, `/new-products`, the live search delta, and the sitemap. **⚠️ `productPage` must be manually ADDED to the Sanity webhook Filter on staging + production** (new type; Projection unchanged — [docs/sanity-webhook-setup.md](docs/sanity-webhook-setup.md)). Sitemap lists every `/products/<slug>` with its first image as an image entry. `products` is a reserved slug (lib + both schema mirrors). **P2-CP-004 batch 1 (2026-07-09):** (1) the active tier highlight is brand GREEN and every tier cell is a clickable button that sets the quantity to that tier's `minQty` (estimate still recalculates ONLY through `estimateForQuantity` — never a second formula); quote-button copy is "We'll quickly confirm exact pricing and inventory."; (2) `decorationMethods` is an array of `{method, upcharge?, setupCharge?}` objects (per-unit upcharge + per-method one-time setup charge as of Q-100; legacy string entries normalize via `productPageDecorations()` and estimate/render fine but show invalid in Studio — re-add to edit) and the estimate = `qty × (unitPrice + selected decoration upcharge) + effectiveSetupCharge(...)` — `DecorationOption`/`decorationUpchargeFor`/`effectiveSetupCharge`/`decorationLabel` (consolidated from the two components in Q-100) live in [lib/products/quote-estimate.ts](lib/products/quote-estimate.ts); a multi-color job is its own decoration entry (e.g. "Screen Print, 2 Colors" with its own setup fee), deliberately NO color-count multiplier; bulk import takes "Decoration N Setup" columns (0 imports as 0, blank stays blank); the lead POST annotates `selectedDecoration` as "Pad Print (+$0.50/unit)" (no `/api/leads` change); (3) the page-builder `portableBody` link annotation carries an **`openInNewTab` toggle** (shared by page/landing/productPage bodies; renderer in [components/page-sections/portable-text.tsx](components/page-sections/portable-text.tsx)) and **AI-placed 'page'-shape links default it TRUE** ([lib/ai/place-internal-links.ts](lib/ai/place-internal-links.ts) + [lib/portable-text/build-page-body.ts](lib/portable-text/build-page-body.ts) carry the flag; richAnswer stays href-only); (4) **Related Videos + Related Blogs strips** below Related Products — manual `relatedVideos[]`/`relatedBlogs[]` reference fields (projected as slugs) + auto keyword matches via `suggestLinksForKind()` ([lib/ai/internal-links.ts](lib/ai/internal-links.ts), suggestions now carry `docId`); card data loads through order-preserving tag-cached `getVideoSummariesBySlugs` (VIDEOS_TAG) / `getBlogSummariesBySlugs` (RELATED_BLOGS_TAG, non-CDN `cachedClient`) so a video/blog publish revalidates the strips with NO webhook change; the AI action pre-fills the refs only-if-empty (route returns `relatedVideoIds`/`relatedBlogIds`); (5) **Logistics fieldset** (`unitsPerCarton`, `cartonWeight` lbs, `cartonWidth`/`cartonHeight`/`cartonDepth` in, `fobZip`/`fobCity`/`fobState`) renders compact set-only lines under production time and emits GMC-ready **`offers.shippingDetails`** (OfferShippingDetails: LBR/INH QuantitativeValues + DefinedRegion `shippingOrigin`) — deliberately NOT Product-level weight/dims, since carton facts describe the package, not one unit.

**Product structured data, and the price Google is told (FIX-830 task 1, 2026-08-14).** Google Search Console's Merchant Listings report showed 42 valid items with four things missing across nearly all of them: a return policy, shipping details, a product identifier, and a description. A live audit of all 78 `/products/` URLs in the sitemap found description, image and offers present on 78/78 and **`sku` emitted on 0/78** - the real item number was on every one of the 86 published docs and simply was never projected into the schema. The rules now live in the pure client-safe [lib/products/product-schema.ts](lib/products/product-schema.ts) (16 vitest cases) and the offer replaced the old per-unit `AggregateOffer`:

- **Price = the total for ONE FULL MINIMUM ORDER, including setup.** Patrick's decision, verbatim: *"if 100 quantity is the minimum at $2.00 each plus a $50 setup, it would show $250."* A bare `price: 250` would be read by Google as the price of one unit, so the figure **always carries the quantity it buys**, twice: `eligibleQuantity` on the `Offer` and a `UnitPriceSpecification` with a `referenceQuantity` (both `QuantitativeValue`, `unitCode: 'C62'`). The number is NOT re-derived - `buildMinimumOrderOffer` calls the configurator's own `estimateForQuantity` + `effectiveSetupCharge` + `decorationUpchargeFor` with the page's own defaults (minimum order quantity, FIRST decoration method), which is exactly `ProductSelectionProvider`'s initial state. So the amount in the markup is the **Estimated total the visitor can see rendered on the page**, not a private calculation: verified end to end on `/products/1785-illini`, where 80 × $6.06 + $45 setup = **$529.80** appears both in the JSON-LD and as on-page text. **A product with no usable tier gets NO offer**, never a guessed price. If the panel's defaults ever change, change them in one place and both follow.
- **Identifier:** `sku` from the doc's existing "Item / SKU number" field (populated on all 86). **`gtin`/`mpn` are deliberately absent** - promotional blanks carry no GTIN and nothing on the document asserts these numbers are manufacturer part numbers, so GSC's "missing gtin" warning is expected to persist. Emitting `mpn` is a one-line follow-up **if Patrick confirms** the item numbers are supplier part numbers; that is his call, not ours.
- **Return policy:** `hasMerchantReturnPolicy` = `MerchantReturnNotPermitted` + `applicableCountry: 'US'` + `merchantReturnLink` to `/returns`. Patrick's decision: *"decorated items can't be returned unless there is an error on Perfect Imprints part."* Every `/products/` page sells decorated goods. **`merchantReturnDays` and any restocking fee are deliberately NOT emitted** - the 14-day window and the fee on `/returns` are the BLANK-goods terms, and stating them from a product page would promise a returns window on custom printed work. Structured data has no vocabulary for the error-on-our-part exception, which is exactly why the link is there.
- **Availability:** new editable `productPage.availability` dropdown (Details fieldset, values mirrored inline in the schema because the Studio bundler cannot import `lib/`), default `InStock`. The business tracks no stock, so this is a stated position, never a measured one. **A non-default value also renders on the page** as an "Availability:" line beside production time, so page and markup cannot disagree; the default renders nothing.
- **Item condition:** `NewCondition`, hardcoded and true.
- **Schema images are forced to `format('jpg')`** via the new `buildSchemaImages()` rather than reusing the gallery URLs: Google's supported structured-data image formats are BMP/GIF/JPEG/PNG/WebP/SVG, **AVIF is not among them**, and 2 of the 78 live pages were serving AVIF schema images. The on-page gallery keeps its own modern formats, so nothing gets slower (verified: the `fm=jpg` URL responds `Content-Type: image/jpeg`).
- **Shipping: reported, NOT fabricated.** The existing `offers.shippingDetails` (carton weight/dims + FOB `shippingOrigin`) is emitted on all 78 and is kept unchanged. It does NOT satisfy Google's merchant-listing shipping, which wants a `shippingRate` + `shippingDestination` + `deliveryTime` - the business quotes shipping instead of publishing a rate, so there is no honest value to emit. **Publishing a flat rate is a business decision for Patrick**, not a code fix.
- **Product Snippets (43 valid, 0 invalid) are preserved:** a single `Offer` carrying `price` + `priceCurrency` + `availability` + `url` satisfies the snippet requirement at least as well as the `AggregateOffer` it replaced. Both the new Product block and the video block validate with **0 errors** against `validator.schema.org` (Google's own validator).
- **Merchant listings eligibility caveat, already known to Patrick:** Google documents merchant listings as eligible on pages where a shopper can buy directly, and this site is quote-based by design. That still holds; these fields make the markup correct and complete, they do not turn a quote page into a checkout.

**categoryOverride.** Per-category curation override (M5-504 part 1), keyed by the `/cat/...` URL slug. **Pinned SKUs (Q-180, 2026-08-07): the `pinnedSkus[]` field (ProductSkuPicker, drag-to-order) shows chosen products FIRST in the category's default view, in Patrick's arranged order. It is a pure REORDER whose single source of truth is `applyPinnedOrder` ([lib/products/pin-order.ts](lib/products/pin-order.ts), pure + vitest-covered), applied ONLY at the end of `mergeCategoryProducts`, the one function BOTH render paths (the static [app/cat/[...slug]/page.tsx](app/cat/[...slug]/page.tsx) page and [/api/category-products](app/api/category-products/route.ts)) assemble their list through, so the two paths structurally cannot disagree. Never a membership change: a pinned SKU not in the category is ignored (never added), a SKU also in `hiddenSkus` stays hidden (hides run before the reorder), facet counts / filter options / total count are untouched. Behavior under interaction: pins land on page 1 (front of the list feeds both paginations); a filter drops a non-matching pinned product like any other; the default 'best-sellers' sort preserves input order so matching pins stay first under filters, while an explicit visitor-chosen sort re-orders everything (visitor wins). Freshness rides the EXISTING `cat:<slug>` tag + webhook categoryOverride branch: no new tag, webhook, or env.** (`categorySlug` — the path after `/cat/`, e.g. `water-bottles` or `water-bottles/color/blue`; **picked with `OverrideCategorySlugInput`** — the searchable single-select `CategorySlugInput` wrapped with a duplicate check, so selecting writes the exact slug and a typo can't silently target nothing). **ONE override per category (guard added 2026-08-05** after duplicate `bags` overrides made Patrick's edits silently no-op — the render path reads one override per slug, so a second doc for the same `categorySlug` does nothing): the schema's async uniqueness validation **blocks publishing** a duplicate ("open and edit the existing override instead"), the picker shows a red notice with the existing override's added-product count + Replace state + an edit link the moment a taken slug is selected, and `getCategoryOverride` orders `_updatedAt desc` so if a pre-guard duplicate still exists the most recently edited doc wins instead of an arbitrary `[0]` pick. Fields: `forceCTA` (bool), `forceProducts` (bool), `replaceProducts` (bool — "Replace products (show only what I add)"), `pinnedSkus[]` (show-first ordering, Q-180 — see above), `hiddenSkus[]`, `addedSkus[]` (existing Geiger SKUs), `addedProducts[]` (references to `customProduct` **or `productPage`** — P2-CP-004 batch 3: an attached productPage renders as an INTERNAL card linking to `/products/<slug>` via `productPageToGeigerProduct()`'s `detailUrl`, participates in the filter overlay with colors from its `colorVariants`, and its projection branch reuses the exported `PRODUCT_PAGE_CARD_FIELDS`; the discriminated union is `CategoryOverrideAddedProduct`. **Freshness:** the attached doc is deref'd inside the `cat:<slug>`-tagged read, so the webhook's `productPage`/`customProduct` branches run a `references($id)` lookup over categoryOverrides and bust each embedding `categoryTag` — **requires `_id` in the webhook Projection** (manual, both environments; productPage has a slug-deref fallback until then, customProduct does not — [docs/sanity-webhook-setup.md](docs/sanity-webhook-setup.md))). It is the manual escape hatch on top of the automatic CTA rules (Section 3) — and the **chosen mechanism for fixing off-topic categories** (set `forceCTA` to hide a wrong grid, `hiddenSkus` to prune wrong items, `forceProducts` to surface a category the `full-capped-60` rule suppressed, `replaceProducts` to discard a wrong Geiger fallback set and show only the SKUs/products Patrick adds). `replaceProducts` makes `mergeCategoryProducts` treat the baked `productSkus` as empty (hides/removes still apply), so an empty/off-topic category (e.g. a `full-capped-60` one returning ~60 unrelated SKUs) renders ONLY the added items — turn it on, add the correct SKUs, publish; no `forceProducts` needed since a non-empty replaced list shows the grid on its own. **Replace-products filters (curated mode):** the added products aren't in this category's scraped `facet-memberships.json`, so without special handling the sidebar's attribute filters ignore them and (worse) a single-facet click navigates to a baked child facet page that serves the ORIGINAL Geiger products. When `replaceProducts` is on, the render path builds an **attribute overlay** ([lib/filters.ts](lib/filters.ts) `buildAddedAttrOverlay` + a lazily-cached reverse SKU→facet-value index over the membership file): pinned Geiger SKUs recover their real Geiger Color/Material/Brand/Feature/Type tags + Made-in-USA/Eco/Closeout flags **regardless of which category they were pinned from** (`GeigerProduct` itself carries no color/material — only brand — so the reverse index is required); custom products use their own `colors[]`/`material`/`brand`/`features[]`/`types[]` + the `madeInUsa`/`ecoFriendly`/`closeout` toggles off the doc (the earlier "Made-in-USA/Eco/Closeout stay Geiger-only" caveat is REMOVED — closeout also honors the CLOSEOUT badge). `buildSidebarData` folds the overlay into the Color/Material/Brand/Feature/Type values + counts (set-based so a SKU in both membership AND overlay isn't double-counted) and the refine-by counts, and **nulls every facet value's `staticUrl`** so a filter click STAYS on `/cat/<slug>?…` (hitting the override-aware [/api/category-products](app/api/category-products/route.ts)) instead of navigating away; `applyFiltersAndSort` keeps a product if it's in the scraped membership set **OR** the overlay matches, so added products aren't dropped when a filter is applied. Wired identically in both [app/cat/[...slug]/page.tsx](app/cat/[...slug]/page.tsx) (sidebar) and the API route (filtering). Triggered ONLY when `replaceProducts` is on — non-replace categories keep their static facet-URL navigation (SEO) and counts unchanged. (**Sanity-owned customCategory pages reuse this exact curated-mode machinery for their own filter sidebar** — P2-CC-001, see the customCategory entry above.) No new Sanity field/read/webhook/cache-tag (reuses the already-fetched `override`), and `/cat` stays static (the overlay reads only `facet-memberships.json` via `fs`, adds no `searchParams`/uncached fetch). Price / Min-Qty / New-Items already worked on added products (they read fields off the product object, not the membership map). Render-time precedence (highest first): `forceCTA` → `replaceProducts` (grid when the replaced list is non-empty, else CTA — overrides the automatic CTA rule incl. `full-capped-60`) → `forceProducts` → original `shouldShowEmptyStateCTA` (empty-skus / full-capped-60 / JSON forceCTA). When products show, `hiddenSkus` (remove) and `addedSkus`/`addedProducts` (prepend) feed the unified resolver `mergeCategoryProducts()` in [lib/sanity/queries/category-overrides.ts](lib/sanity/queries/category-overrides.ts) (added SKUs resolved via `resolveProductsBySku`, custom products via `customProductToGeigerProduct`). The precedence is wired inline in [app/cat/[...slug]/page.tsx](app/cat/[...slug]/page.tsx). The Sanity webhook revalidates `/cat/<categorySlug>` on publish (the webhook GROQ projection must include `categorySlug`). No bulk JSON is rewritten — overrides are read at render time. This is the **category-first** editing direction; the **product-first** direction is `productPlacement` (below). Both merge through `mergeCategoryProducts`.

**productPlacement.** Product-side placement (M5-504 Part 2), keyed by a Geiger `sku` (string), the complement to `categoryOverride`. Fields: `sku` (picked with the searchable single-select `ProductSkuInput`, see below), `addToCategories[]` and `removeFromCategories[]` — both arrays of category slugs picked with the searchable `CategoryPicker`. Lets Patrick attach/detach one product to/from many categories. Query in [lib/sanity/queries/product-placements.ts](lib/sanity/queries/product-placements.ts) (`getPlacementSkusForCategory`). The webhook revalidates every `/cat/<slug>` in both lists on publish (projection must include `addToCategories` + `removeFromCategories`).

**Unified category product resolver** (M5-504 Part 3, `mergeCategoryProducts` in [lib/sanity/queries/category-overrides.ts](lib/sanity/queries/category-overrides.ts)). Produces a category's final grid by merging both edit directions: (1) baked `productSkus` (treated as empty when `categoryOverride.replaceProducts` is on), (2) `+ categoryOverride.addedSkus`/`addedProducts`, (3) `+ productPlacement.addToCategories`, (4) `− categoryOverride.hiddenSkus`, (5) `− productPlacement.removeFromCategories`. **Removal wins over add**; de-duped by SKU; all SKUs resolved **live** by `resolveProductsBySku` so placements reference products by SKU and survive the weekly/monthly Geiger re-scrape (a discontinued SKU resolves to nothing). Reuses the lookup layer — no duplication. Both editing entry points reach the same result on `/cat/<slug>`.

**Searchable Studio pickers (category + product).** The 22,180 category pages and ~7,957 catalog products are build-time JSON, not Sanity docs, so normal reference fields can't target them. Two picker modules cover this, both Studio-only, plain React + the `sanity` form API (no `@sanity/ui`):

- **Category** ([sanity/components/CategoryPicker.tsx](sanity/components/CategoryPicker.tsx), hook `useCategoryOptions`): `CategoryPicker` (multi-select → `productPlacement.addToCategories`/`removeFromCategories`) and `CategorySlugInput` (single-select → `categoryOverride.categorySlug`). Source: `category-list.json` (slug + title for all 22,180, from `data/pi-urls/category-urls.json` via `pnpm build:category-list`) **plus** live `customCategory` slugs; supports **create-new** (creates a `customCategory` at that slug).
- **Product** ([sanity/components/ProductPicker.tsx](sanity/components/ProductPicker.tsx), hook `useProductOptions`): `ProductSkuPicker` (multi-select → `customCategory.productSkus`, `categoryOverride.addedSkus`/`hiddenSkus`) and `ProductSkuInput` (single-select → `productPlacement.sku`). Search the catalog by name / SKU / brand and click to add the **SKU** (no more typing numbers from memory). Source: `product-list.json` (sku + name + brand for all ~7,957, from `data/geiger/products.json` via `pnpm build:product-list`).

**Result ranking (2026-08-10 bugfix — read this before touching either picker's `results` memo).** Both pickers used to walk their list in file order and stop at the first 30 substring matches. With 22,180 alphabetically-sorted categories that hides the obvious answer: typing `pens` matched **246** entries and the first 30 were `apparel/supplier/hubpens`, `carpenter-pencils/supplier/hubpens` and **28 `dispensary-containers/*` rows** — because "dis-**pens**-ary" contains the query. The root `pens` category sat at **position 50** and was never rendered, so Patrick could not create a `categoryOverride` for it at all (he reported it as "the category won't show in the search"; verified in Sanity — no `pens` override doc existed, only an unrelated `pens/feature/nfc-enabled`). The same shape hits **any short slug that lives inside longer ones** (`bags`, `hats`, `mugs`, `pins`, `caps`), and the identical loop was in `ProductPicker`, where an exact SKU or brand could sit behind dozens of incidental hits. Fix: both pickers now score **every** match and keep the best 30 via the shared pure [sanity/components/rank-matches.ts](sanity/components/rank-matches.ts) (`rankMatches` / `matchScore`) — tiers are exact-primary → exact-label → primary-starts-with-on-a-boundary → label-whole-word → primary-whole-word → plain-substring, ties broken by slug depth (roots outrank their own facet children), then length, then alphabetically. A "boundary" is a non-alphanumeric char or the string edge, which is what separates `gel-pens` from `dispensary` without a word list. Scoring all ~22k entries costs a few ms and runs behind the existing 180 ms debounce, so no index is needed. Both dropdowns also render a **"Showing the 30 closest of 246 matches"** footer — the silent truncation is what made the old picker read as "the category isn't there" instead of "it's further down". Verified against the real `category-list.json`: `pens` → #1, `bags` → #1, and SKU search `bic` → BIC products first. `rank-matches.ts` is Studio-only and dependency-free (the standalone Studio bundler cannot import the app's `lib/`).

Both lists are built in `prebuild` and written to **BOTH `public/` (served by the embedded Studio at `/admin3773752`) and `sanity/static/` (served by the standalone `sanity dev` Studio, which does NOT serve Next's `public/`)** — the shared loader [sanity/components/load-json.ts](sanity/components/load-json.ts) (`loadStudioJson`) tries `/<file>` then `/static/<file>`, requires a JSON content-type (rejects SPA index.html fallbacks), and surfaces a clear "couldn't load" message instead of a misleading "no match" when neither resolves. `sanity/static/` is git-ignored (regenerated by `prebuild`). Studio-only — these lists are never loaded by the live site.

**blogPost.** Fields: slug, title, header image, body (rich text / portable text), inline images, author, publish date, updated date, categories reference, `relatedBlogs` (manual override reference array — when set, takes over the "See Related Blogs About …" list on the post page in editor-chosen order; when empty, the post page auto-derives by shared category slugs, newest first), `ctaTopic` (optional VERBATIM heading for the bottom "Order Custom … Today" CTA block only — blank = automatic "Order Custom [category] Today"), `ctaBody` (optional VERBATIM paragraph under that CTA heading — blank = the default copy in `DEFAULT_CTA_BODY` [lib/blog/cta-defaults.ts](lib/blog/cta-defaults.ts), which `OrderTodayCTA` and the one-time backfill script both import so stored text and rendered default never drift; every existing published post was backfilled 2026-07-13 via `pnpm backfill-blog-cta-body`, published-only + idempotent, so the current wording is stored + editable per post), meta title, meta description. Body also accepts an inline `blogProducts` block (see below) so editors can drop a row of product cards between paragraphs the way the legacy MPower blogs did.

**blogProduct (shared product-strip entry).** The single object type behind EVERY product strip. Its `sku` field is a **search-and-pick dropdown** (Q-170 improvement 1, 2026-08-02): `components: { input: ProductSkuInput }` on the field in [sanity/schemas/objects/blog-products.ts](sanity/schemas/objects/blog-products.ts). The field `type` is still `'string'` and `ProductSkuInput` emits `set(<bare sku>)` / `unset()`, so the stored value is byte-identical to hand-typing and **there is NO data migration**: every already-stored entry keeps working. Attaching it on the shared type gives the picker to all five surfaces at once: `blogProducts` blocks in blog bodies, the page-builder `productStrip` section, `video.relatedProducts`, `landingPage.relatedProducts`, and `productPage.relatedProducts` (which inlines `{ type: 'blogProduct' }`). `catalogPage` is unaffected: it uses `addedSkus`, which already had the array picker. The programmatic writers (the AI generate routes, Studio actions, `seed-landing-pages`) write `{_type:'blogProduct', sku}` and are untouched. **The picker is never the only way in:** `ProductSkuInput` gained an "Enter a SKU manually" plain-text fallback that opens automatically when `product-list.json` fails to load, so a build-artifact miss or a SKU newer than the last catalog scrape can never block an author (this also improves `productPlacement.sku` and the quote Geiger line, which share the input).

**blogProducts (body object).** Insertable anywhere in `blogPost.body` from the Studio insert menu. Fields: optional `heading` (string, rendered as an H2 above the row) and `products` (array of `{ sku?, title?, image?, url? }`). Each product is either SKU-backed (live price/image/affiliate URL pulled from the Geiger catalog at render time) or fully manual (title + image + external URL). SKUs are resolved server-side in [app/blog/[slug]/page.tsx](app/blog/[slug]/page.tsx) via `resolveProductsBySku` (`lib/categories.ts` reads `products.json` from disk, so the lookup cannot run inside the synchronous PortableText renderer) and handed to `BlogBody` as a `Map<string, GeigerProduct>`. SKU-backed entries render via the standard `components/category/ProductCard.tsx`; manual entries render a parallel card and route their URL through `lib/affiliate-url.ts` only when the host is Geiger.

**Freshness sweep (Q-175, 2026-08-05), the SIXTH through TWELFTH fixes of the same defect.** After the blog-detail bug below, a repo-wide sweep found the identical pattern on every remaining Sanity read path: a CDN `client` read (`useCdn: true`, untagged) inside a route that rebuilds on publish and then does not refresh again, so the rebuild fires at the exact moment the Sanity CDN is stalest and freezes that copy. All of them were converted to the standing pattern (non-CDN `cachedClient` + a cache tag + a matching `revalidateTag` in the webhook). **`useCdn:false` is what removes the race; the tag is what keeps the read CACHED so the route stays static. Swapping the client without adding a tag makes the fetch uncached and turns the route DYNAMIC, which on this site is catastrophic. The two are one change, never two.** What was fixed, and the new tags in [lib/sanity/cache-tags.ts](lib/sanity/cache-tags.ts):
- **Home page** (`HOME_TAG`, + `SETTINGS_TAG` for the CTA banner, whose copy lives on `globalSettings` not `homePage`). `/` is `force-static` with no `revalidate`, so it never self-refreshes; the webhook's `revalidatePath('/')` was rebuilding it straight into a stale read. Highest impact because Patrick edits it most.
- **Blog category pages** (`BLOG_LIST_TAG`). These were broken TWICE: CDN reads, AND `blogCategory` was handled in no webhook branch and in no type set, so `/blog/cat/<slug>` (`revalidate = false`) was frozen until the next deploy no matter what anyone published. The `/page/N` variants are not prebuilt either, so a deploy did not even clear those. The webhook's `blogPost` branch now busts `BLOG_LIST_TAG`, and that, not path revalidation, is what refreshes them, because a post can move between categories and the webhook payload carries only its slug, never its categories. A new `blogCategory` branch handles the type itself. **⚠️ `blogCategory` must be manually ADDED to the Sanity webhook Filter on staging + production** ([docs/sanity-webhook-setup.md](docs/sanity-webhook-setup.md)); until then a blog POST publish still refreshes those pages (tag), but renaming a CATEGORY does not.
- **`/deals`, `/new-products`, `/rush-products`** (copy via `SETTINGS_TAG`, placements via `CUSTOM_PRODUCTS_TAG`). These carry the hidden/pinned SKU levers, so a lost race meant a hidden product kept showing for up to the route's ONE WEEK revalidate. Intervals kept as the backstop.
- **Blog index + pagination, the sitemap, and the live search delta** (`BLOG_LIST_TAG`, `CUSTOM_PRODUCTS_TAG`, `CUSTOM_CATEGORIES_TAG`). `CUSTOM_CATEGORIES_TAG` is deliberately its own tag rather than reusing `CATEGORY_CONTROL_TAG`, which every `/cat` page reads; widening what busts that would be a real cost across 22,180 pages.
- **Tag granularity, chosen deliberately:** `blogPost:<slug>` is per slug so publishing one post does not invalidate the other 644; `BLOG_LIST_TAG` is one list tag because list results change in ways the webhook payload cannot identify. Blast radius of a blog publish is the blog index + its pagination, the blog category pages + theirs, the home preview, the sitemap and the search delta, and **never `/cat`**, which reads related blogs through the separate `RELATED_BLOGS_TAG`.
- **Deleted the dead `previewClient` + `getClient()`** from [lib/sanity/client.ts](lib/sanity/client.ts). Nothing imported either (verified by search across app/, components/, lib/, scripts/, sanity/; the `getClientIp` and `context.getClient` matches are unrelated). Not tidiness: that client carried a write token and `perspective: 'previewDrafts'`, so wiring it into a render path by mistake would have published Patrick's unpublished drafts. Deleting it removes the possibility.
- **`/cat` was NOT touched**: every category read was already non-CDN, tagged and webhook-busted. Verified by [scripts/quick-quote/verify-q175.ts](scripts/quick-quote/verify-q175.ts), whose category-page check runs FIRST and aborts the run before writing anything, then runs again after all publishing so a tag blast radius reaching the category pages would also be caught.
- **Known, deliberately unfixed:** `app/api/sanity/bulk-import/route.ts` and `lib/quotes/numbering.ts` still read via the CDN client. Neither is a render path (both are `force-dynamic` / Studio-side), and the numbering allocator's revision-guarded compare-and-set makes a stale read fail and retry rather than issue a duplicate.

**Blog detail freshness (2026-08-05 bugfix, read this before touching `lib/sanity/queries/blogs.ts`).** `/blog/<slug>` is `revalidate = false`, so it is generated once and NEVER refreshes on its own; only the webhook can rebuild it. `getBlogPostBySlug` used to read through the plain CDN `client` (`useCdn: true`, untagged), which made every publish a race the page could lose **permanently**: the webhook fires within a second, the page regenerates immediately, the Sanity CDN is still serving its own ~60s copy from BEFORE the publish, and that pre-publish body is baked in and frozen forever. Reported by Patrick when a `blogProducts` strip he had deleted and published kept rendering (the published doc had zero strip blocks; the page had one). It is a RACE, so it presents intermittently: his ADD won the race and appeared, his DELETE lost it. Fix is the FAQs / videos / brands / globalSettings pattern: `getBlogPostBySlug` now reads through the non-CDN `cachedClient` tagged **per slug** (`blogPostTag(slug)` in [lib/sanity/cache-tags.ts](lib/sanity/cache-tags.ts), per slug on purpose so publishing one post does not invalidate the other 644), and `getRelatedBlogsForPost`'s auto query got the same treatment tagged `RELATED_BLOGS_TAG` (its answer depends on OTHER posts publishing). The webhook's `blogPost` branch now busts `blogPostTag(slug)` alongside the `revalidatePath('/blog/<slug>')` it already did. **Without that bust the path revalidation would just rebuild from the tag-cached copy and the edit would still never appear.** `useCdn:false` is what removes the race; the tag is what keeps the read cached so the route stays static instead of flipping dynamic. **Known remaining gap, deliberately not fixed:** `getAllBlogCategories` (the sidebar list) is still a CDN read, but `blogCategory` is not in the webhook's SEARCH_TYPES either, so tagging the read alone would achieve nothing; fixing it means adding the type to the webhook too, which is a separate change.

**blogCategory.** Taxonomy. Fields: slug, title, description.

**author.** Fields: name, bio, image.

**homePage.** Singleton. Fields: `heroText` (M5-501 follow-up — the EDITABLE text hero at the top: `eyebrow`/`headline` (H1)/`subheadline`, rendered by [components/home/Hero.tsx](components/home/Hero.tsx); kept text-only — no image — so the LCP element stays text-bound and fast. Seeded with Patrick's exact copy via `pnpm seed-home-content` ([scripts/seed/seed-home-content.ts](scripts/seed/seed-home-content.ts)). The legacy image-based `heroBanner` object is retained collapsed but NOT rendered on the home page; `getHomePage` falls back to its headline/subheadline only if `heroText` is blank), `bannerRowHeading` (H2) + `bannerRowSubheading` (editable section heading rendered directly above the banner row — seeded "Featured Product Categories" / "Seasonal promos your customers and team will love and use right now!"), `bannerRow` (M5-506 — an editable row of up to three equal-size banner images, each `image` + `link` (url) + `alt`, rendered below the hero via [components/home/BannerRow.tsx](components/home/BannerRow.tsx); banners keep their own aspect ratio so uniformity comes from consistently-sized uploads, and an empty array renders nothing. **Pre-filled** from PI's OWN live home page — the Sunglasses / Event Tents / Hats small-banner row — with each image downloaded into a SELF-HOSTED Sanity asset and linked to the equivalent preserved `/cat/<slug>` route (`/cat/sunglasses`, `/cat/canopy-tents`, `/cat/caps`); reproduce with `pnpm seed-home-banners` ([scripts/seed/seed-home-banners.ts](scripts/seed/seed-home-banners.ts)). Patrick can swap any banner in Studio), `valueProps` (Value Pillars — `title` + `body`; **`body` is portable text (rich text), NOT a plain string**, so Patrick can add hyperlinks — e.g. link "Rush Production Available" to `/rush-products`. Links render in **brand red** with underline-on-hover, external links open in a new tab with `rel="noopener noreferrer"`, via the shared `pillarComponents` in [components/home/PillarCard.tsx](components/home/PillarCard.tsx). **Render mode is count-driven (M5-501 Part 6):** ≤3 pillars render as the original static row; **>3 pillars rotate in a carousel** — up to 3 visible at a time on desktop / 1 on mobile, snap-scroll + prev/next + auto-advance (paused on hover/focus, disabled for `prefers-reduced-motion`) via the client [components/home/ValuePillarsCarousel.tsx](components/home/ValuePillarsCarousel.tsx). The `valueProps` validation was relaxed from "exactly 3" to "min 1" so Patrick can add more. `seed-home-content` migrates any legacy string bodies to portable text, preserving the text; the one-off `pnpm fix-pillar-links` ([scripts/migrations/fix-pillar-inline-links.ts](scripts/migrations/fix-pillar-inline-links.ts)) rewrites any pillar body where Patrick pasted **literal `<a href>` HTML** into a real `link` mark — RUN 2026-06-28, fixed the Rush pillar, idempotent), `newProductsHeading` + `rushProductsHeading` (rail headings — the New & Trending rail and the **Rush Production rail** below it, both rendered by [components/home/NewProductsRail.tsx](components/home/NewProductsRail.tsx); the Rush rail is fed by `getRushProducts(12)` from [lib/rush-products.ts](lib/rush-products.ts), same scraped data file as `/rush-products`, no new scrape), `testimonialsHeading` (editable, default "What Our Customers are Saying") + `testimonials` (now a **3-up horizontal carousel** — [components/home/Testimonials.tsx](components/home/Testimonials.tsx) is a client component: snap-scroll showing 3 per view on desktop / 1 on mobile, prev/next + native swipe + auto-advance, dark `bg-brand-ink` styling kept), featured image blocks (six of them), text content sections, brands grid reference.

**megaMenu.** Singleton (`_id: megaMenu`). Drives the live primary navigation (M5-503). Field `items[]` is the ordered top-level menu, left to right. Each item has a `kind`: `link` (plain link — `href`), `dropdown` (flat list — `links[]` of `{label, href}`, e.g. Services), or `megaPanel` (columned panel — `columns[]`, each `{label, href?, nonClickable, links[]}`). A `megaPanel`'s `variant` selects the renderer: `cascade` (hover-reveal columns, "Shop by") or `grid` (full-width grid popover, "All Categories"). Reserved-but-unrendered fields `featured` + `productRefs` are retained for a future featured-products panel. Seed/reset from code with `pnpm seed-mega-menu` (reads `lib/nav-data.ts`); the header reads it via `getMegaMenu()` in [lib/sanity/queries/mega-menu.ts](lib/sanity/queries/mega-menu.ts) (**non-CDN `cachedClient` + `MEGA_MENU_TAG` cache tag** as of the 2026-07-01 freshness fix — see the globalSettings "Read path" bullet). Reorder/rename/hide/add/**remove**/delete in Studio reflects live in seconds (the webhook `revalidateTag(MEGA_MENU_TAG,'max')`s + `revalidatePath('/', 'layout')` on `megaMenu` publish). No hard-coded fallback — Sanity is the sole source.

**globalSettings.** Singleton (`_id: globalSettings`). Fields: legacy flat `phoneNumber` (default `800-773-9472`) / `contactEmail` / `mailingAddress` (kept only as fallbacks), **`hoursOfOperation` (AUTHORITATIVE as of 2026-08-05** — resolved into `SiteContact.hours` by `getSiteSettings()` and rendered in the footer Contact column (multi-line, hidden when blank) AND interpolated into the red `CTABanner`'s "Talk to a product specialist …" line, so the two can never disagree; it was previously never fetched and both spots were hardcoded — with two DIFFERENT hardcoded hour strings — which made the Studio field appear broken, Patrick's 2026-08-05 report), `contact` group, `socialLinks[]`, footer columns content, copyright text, CTA banner text, plus per-aggregator curation objects:

- **`socialLinks[]` — Sanity-controlled socials (no hardcoded URLs).** Each item: `platform` (dropdown: Facebook / Instagram / LinkedIn / YouTube / X (Twitter) / Pinterest / TikTok / Other), `label` (optional a11y/display name, required for "Other"), `url` (validated http/https), `customIcon` (optional image — used for "Other" or to override the built-in icon), `enabled` (boolean, default true). **`enabled: false` hides the link EVERYWHERE and excludes it from the Organization `sameAs`.** Known platforms render a built-in inline-SVG icon ([components/icons/social-icons.tsx](components/icons/social-icons.tsx), `SocialIcon` + `SOCIAL_ICON_MAP`, keyed by the platform value — keys are duplicated there, not imported, because the standalone Studio bundler can't import this app dir); "Other"/`customIcon` renders the uploaded image; unknown + no icon → a generic globe so nothing breaks. Patrick just picks a platform and pastes the URL.
- **`contact` group — authoritative phone/email/address.** `phones[]` (one or more; first is the primary schema telephone), `email` (validated), `address` object (`street`, `city`, `region`, `postalCode`, `country`). The footer + Organization JSON-LD read from `contact`, falling back to the legacy flat fields only when `contact` is blank.
- **`footerColumns[]` — Sanity-driven footer nav columns (M-SEO3 follow-up).** The three link columns left of Contact (About Us / Popular Links / Customer Service). Each `footerColumn` = `heading` + `links[]` (`link`: `label`, `href`, `external`). `getSiteSettings()` resolves them (keeping only columns with a heading + ≥1 valid link); [components/layout/Footer.tsx](components/layout/Footer.tsx) renders from `footerColumns` when present and **falls back to its hardcoded `NAV_COLUMNS`** when empty, so the footer is never blank. `external` links open in a new tab (`rel="noopener noreferrer"`); internal use `<Link>`. **Internal hrefs are slash-tolerant:** a bare internal path entered without a leading slash (e.g. `llm-info-perfect-imprints`) is auto-prefixed to `/llm-info-perfect-imprints` by the shared [lib/sanity/normalize-href.ts](lib/sanity/normalize-href.ts) `normalizeHref()` (external `http(s)://` / `mailto:` / `tel:` / `#anchor` hrefs are left untouched; a link flagged `external` keeps its href verbatim and must include the full scheme). The label+href non-empty drop condition is unchanged — normalizing only fixes bare internal paths, it does not start rendering empty links. The **same `normalizeHref` is applied in the mega-menu resolver** ([lib/sanity/queries/mega-menu.ts](lib/sanity/queries/mega-menu.ts)) for item `href`, dropdown `links[].href`, and column `href` + `links[].href`. Resolver-only change — no schema/webhook change. The dedicated **Contact column** (phones/email/address from `contact`, the hours line, Contact Form link) and the **social row** are NOT part of `footerColumns` — they stay separate. Seed idempotently (writes only when empty, never overwrites edits) with `pnpm seed-footer-columns` ([scripts/seed/seed-footer-columns.ts](scripts/seed/seed-footer-columns.ts)) — values mirror `NAV_COLUMNS`, so the live footer is identical after wiring. **Ran 2026-06-28** (wrote 3 columns / 14 links; re-run is a no-op).
- **`categoryCtaBar` — the category CTA bar (P2-CTA-001, 2026-07-12).** `enabled` (kill switch, default on) + `heading`/`body`/`buttonLabel`. Drives [components/category/CategoryCtaBar.tsx](components/category/CategoryCtaBar.tsx), the slim "Not finding the exact [category] you're looking for?" bar rendered **below the products and above the FAQs** on every product-bearing category/facet page — the baked route's `!showCTA && sidebar` branch in [app/cat/[...slug]/page.tsx](app/cat/[...slug]/page.tsx) (facet + paginated pages are the same route → automatic) and the `products.length > 0` branch in [components/category/CustomCategoryView.tsx](components/category/CustomCategoryView.tsx). EmptyStateCTA-mode pages NEVER get it (they already carry the CTA in the grid slot — no double CTA). The `{category}` token in heading/body is replaced with the category name at render; token removed → copy renders without it. **Blank field → Patrick's default copy from code** (`CATEGORY_CTA_BAR_DEFAULTS` in [lib/sanity/queries/global-settings.ts](lib/sanity/queries/global-settings.ts) — required because schema `initialValue` never retro-fills the already-existing singleton doc); hiding is `enabled`'s job, not blanking. The button reuses `EmptyStateCTAButton` (new optional `label` prop, default unchanged) → the same lazy "Find Products for Me" `LeadFormModal` with `categoryTitle` + `sourceUrl`, identical lead flow. **Static-safety + cost:** async SERVER component (copy is in the static HTML); its read is the same per-request React-`cache()`d `getSiteSettings()` the layout Footer already performs in the same render → zero extra Sanity fetches on any page, `/cat` stays `●`. Freshness rides `SETTINGS_TAG` + the existing webhook `globalSettings` case — no webhook Filter/Projection change. A dedicated cache tag for the CTA copy was evaluated and REJECTED: every page already carries `SETTINGS_TAG` via the layout Footer read (plus the webhook's `revalidatePath('/','layout')`), so ANY globalSettings publish already invalidates the whole site — the bar adds zero new blast radius and a separate tag would not reduce it. **`videoCtaBar` — the video variant (P2-CTA-001 extension, 2026-07-12, goodwill):** sibling object (same enabled/heading/body/buttonLabel shape, code defaults `VIDEO_CTA_BAR_DEFAULTS`, both resolved by one `resolveCtaBar()`), rendered by the SAME `CategoryCtaBar` component — `variant="video"` selects the copy, `inline` drops the page `<Container>` for the video article's max-w-4xl column (do NOT fork a second component) — on `/videos/<slug>` **below the related-products strip, above Related Videos, deliberately even when the strip is empty**. Default heading verbatim from Patrick: "Need help choosing the right Promotional Products? We're here."; default body trimmed to "Contact us and we'll search through our database of over 1,000,000 promotional items." No `{category}` token on video pages — typed in anyway, it substitutes a generic "promotional products", never a raw token. The video page previously had NO CTA and does NOT render `CTABanner` (whose heading is near-identical) — keep it that way, never both on one page. The button passes `categoryTitle={video.title}` + `sourceUrl=/videos/<slug>` to the same lead modal; `/videos/[slug]` stays SSG (same deduped `SETTINGS_TAG` read, no new webhook/tag/env).
- **Read path + freshness fix (2026-07-01).** Both the footer ([components/layout/Footer.tsx](components/layout/Footer.tsx)) and the Organization JSON-LD ([components/seo/OrganizationJsonLd.tsx](components/seo/OrganizationJsonLd.tsx) → `organizationSchema(settings)` in [lib/seo/schema-generators.ts](lib/seo/schema-generators.ts)) consume `getSiteSettings()` ([lib/sanity/queries/global-settings.ts](lib/sanity/queries/global-settings.ts)) — React-`cache()`d for per-request dedup so they share one fetch, resolves enabled-only socials + custom-icon URLs + contact + footerColumns. **The read now goes through the non-CDN `cachedClient` with the `SETTINGS_TAG` (`global-settings`) cache tag** ([lib/sanity/cache-tags.ts](lib/sanity/cache-tags.ts)), and the webhook `revalidateTag(SETTINGS_TAG,'max')`s on `globalSettings` publish IN ADDITION TO the existing `revalidatePath('/', 'layout')` — so footer/schema edits go live deterministically in seconds while `/cat` stays static (tagged, not `no-store`). **This fixed a real freshness bug:** the previous plain CDN `client` read (`useCdn:true`, untagged) stayed stale on edits — REMOVING a footer link kept rendering even after the webhook logged `200 {scope:"layout"}`, because the CDN serves its own ~60s copy and the untagged fetch wasn't reliably busted by the path-only revalidation (same defect class as the earlier FAQs / videos / brands fixes). **`getMegaMenu()` had the identical defect and got the same fix** — non-CDN `cachedClient` + `MEGA_MENU_TAG` (`mega-menu`), webhook `revalidateTag(MEGA_MENU_TAG,'max')` on `megaMenu` publish (so add / reorder / **remove** all reflect in seconds, not just after the CDN TTL). No webhook Filter change needed — `globalSettings` + `megaMenu` are already in the Filter; the fix is code-side (client + tag + `revalidateTag`). The footer renders only enabled socials (new tab, `rel="noopener noreferrer"`, `aria-label`) — no more `#` placeholders. Seed contact (and socials, if found) from PI's own site with `pnpm seed-social-contact` ([scripts/seed/seed-social-contact.ts](scripts/seed/seed-social-contact.ts)) — phones `800-773-9472` + `850-200-4020`, email `cs@perfectimprints.com`, address `913 Beal Pkwy NW, Ste A153, Fort Walton Beach, FL 32547`. **No real social URLs were seedable — PI's live footer social icons are all `#` placeholders — so Patrick adds socials in Studio.**
- **`siteSearch.hiddenSkus[]`: hide a Geiger SKU from SITE SEARCH, site wide (Q-170 improvement 2, 2026-08-02).** Picked with the searchable `ProductSkuPicker`. Read at REQUEST time by every search read path, so an edit takes effect on publish with NO rebuild, and removing a SKU brings the product straight back. **Search visibility only**: the product stays on its category pages, in the filter counts, in the sitemap, and on every aggregator; nothing outside `lib/search` + `lib/sanity/queries/global-settings.ts` reads the list (asserted structurally by the verification script). **There are exactly THREE search read paths and all THREE are covered** (filtering one produces a feature that looks fixed in the overlay and still shows the product on the results page): (1) `lib/search/load-index.ts`, the client index behind the header autocomplete AND the `/search` "Also matching" strip, which filters the MERGED static-bulk + delta set in `recomputeItems()`; (2) `lib/search/server-search.ts` `searchProducts()`, the `/search` results grid, whose only importer is [app/search/page.tsx](app/search/page.tsx) (facets are built from the FILTERED list, so a hidden product leaves no trace in the sidebar counts either); (3) the build-time static bulk `public/search-index.json`, deliberately NOT filtered at build, because filtering it would need a redeploy per edit, which is the whole thing this feature avoids, so it is filtered at read time by path 1. The rule itself lives once in the pure, dependency-free [lib/search/hidden-skus.ts](lib/search/hidden-skus.ts) (`normalizeSearchSku` trims + upper-cases but PRESERVES the internal space in a real item number like `501014 90A`; entries with no `sku`, meaning categories, brands, blogs, videos, FAQs and customProducts, can never be touched), imported by the client module, the server module and the route so they cannot drift; 12 vitest cases. **Transport:** the list rides on the LIVE delta response as `SearchIndexFile.hiddenProductSkus` ([app/api/search-index/route.ts](app/api/search-index/route.ts), reusing the already-deduped `getSiteSettings()`), because that is the only search surface a Sanity edit can reach without a rebuild. **Webhook (the gap Q-000 flagged):** the `globalSettings` branch of [app/api/sanity/revalidate/route.ts](app/api/sanity/revalidate/route.ts) now ALSO calls `revalidatePath(SEARCH_INDEX_ROUTE)`, because `SETTINGS_TAG` alone busts the settings FETCH, not the cached RESPONSE of the ISR route that calls it, so without this a hide would have sat behind that route's 1-week floor. **No webhook Filter/Projection change** (`globalSettings` was already in the Filter), no new cache tag, no new env var, no new dependency, and `/search` was already dynamic so nothing became less static. **Honest limitation:** the client never blocks on the delta, so a search fired in the gap before it lands runs against an empty hide list and can briefly show a hidden product in the OVERLAY; the two fetches now start in PARALLEL rather than the delta being chained behind the much larger static file, so the gap is usually negative, it self-corrects on the next keystroke, and the server-rendered results page is never affected.
- `dealsPage` for `/deals`: `heading`, `intro`, `metaTitle`, `metaDescription`, `hiddenDealSkus[]` (blocklist — remove scraped products), `pinnedDealSkus[]` (allowlist — promote any existing Geiger SKU to `/deals` even if Geiger's weekly scrape did not include it; resolved against `data/geiger/products.json`).
- `newProductsPage` for `/new-products`: mirror shape with `hiddenNewProductSkus[]` + `pinnedNewProductSkus[]`.
- `rushProductsPage` for `/rush-products`: mirror shape with `hiddenRushSkus[]` + `pinnedRushSkus[]`.

The product list on all three pages is auto-scraped weekly from Geiger (see Section 16 Phase F + Phase G + Phase H). The Sanity layer adds three editorial levers without touching the scraper: hide a scraped SKU, pin an existing Geiger SKU, or add a fully custom non-Geiger product via the `customProduct` document with `placements.onDeals`/`placements.onNewProducts`/`placements.onRush` set. Merge order on each page: custom products → newly-pinned Geiger SKUs → scraped Geiger products. Augmentation logic lives in [lib/products/augment.ts](lib/products/augment.ts) (pure, no I/O); orchestration in [lib/deals.ts](lib/deals.ts) (`getAugmentedDealsData()`), [lib/new-products.ts](lib/new-products.ts) (`getAugmentedNewProductsData()`), and [lib/rush-products.ts](lib/rush-products.ts) (`getAugmentedRushProductsData()`). Custom products participate in the Brand/Color/Material filter sections (their tags are injected into the corresponding facet values' SKU arrays) and the synthetic Category section (via `parentCategory` ref).

**faq.** Reusable FAQ items. Fields: `question`, `answer` (**rich text — the `richAnswer` Portable Text type as of M5-516/Task B, so answers can contain links**; rendered via the shared [components/portable-text/RichAnswer.tsx](components/portable-text/RichAnswer.tsx) — internal paths use `next/link`, external/`#hash`/`mailto:`/`tel:` render as `<a>`, Geiger URLs route through `lib/affiliate-url.ts`, external http(s) open in a new tab. The FAQPage JSON-LD + answered-filter + search all use a **plain-text extraction** `portableTextToPlain()` ([lib/portable-text/to-plain.ts](lib/portable-text/to-plain.ts)) — Google wants plain text in `acceptedAnswer.text`. NOT required as of M5-506 — the library is seeded with question stubs and the answer is filled later), `categoryTags` (references to curated/custom categories, for the per-`/cat` Related-FAQs section), and `faqCategory` (M5-506 — the FAQ-library taxonomy: one of 7 ordered categories, canonical list in [lib/faqs/categories.ts](lib/faqs/categories.ts), mirrored inline in the schema because the standalone Studio bundler can't import `lib/`). The **`/faq` library page** ([app/faq/page.tsx](app/faq/page.tsx)) — slug is singular `/faq`, matching the live site — groups ANSWERED faqs (`defined(answer) && count(answer) > 0`, since `answer` is now a Portable Text array) by `faqCategory` into `<details>` accordions (section anchor id = the category value) and emits FAQPage JSON-LD via `faqPageSchema()` in [lib/seo/schema-generators.ts](lib/seo/schema-generators.ts); footer "FAQs" links here. Answered faqs are also in the **live search delta** (`faq` type → `/faq#<category>`; [lib/sanity/queries/faqs.ts](lib/sanity/queries/faqs.ts) → `getAllFaqSearchEntries` → [lib/search/sanity-index.ts](lib/search/sanity-index.ts)); the webhook revalidates `/faq` + the search delta on faq publish. **Freshness (M5-516 follow-up):** the FAQ reads go through the **non-CDN `cachedClient`** with the `FAQS_TAG` (`faqs`) cache tag ([lib/sanity/cache-tags.ts](lib/sanity/cache-tags.ts)), and the webhook `revalidateTag('faqs','max')`s on `faq` publish — so an edit goes live deterministically in seconds instead of intermittently re-caching a stale CDN copy (`/faq` stays ISR-static, tagged not `no-store`). **The Sanity webhook FILTER must include `faq`** (it handles the type but was originally excluded — see [docs/sanity-webhook-setup.md](docs/sanity-webhook-setup.md)); without it, faq publishes never revalidate. Seed/slot starter questions (answers blank — never fabricated) with `pnpm seed-faqs` (idempotent; slots an existing matching faq into its category instead of duplicating). **Answers** were filled from PI's OWN content (the live FAQ page + the migrated policy pages — Sample Policy, Shipping, Returns, Terms, Contact) and PI's service offerings via `pnpm fill-faq-answers` ([scripts/seed/fill-faq-answers.ts](scripts/seed/fill-faq-answers.ts)), which publishes each answered faq (and preserves any answer Patrick already wrote, e.g. the minimum-order-quantity one); nothing is fabricated. The one question with no PI source — "Do I need an account to place an order?" — was left blank (unpublished draft) for Patrick. Query helpers in [lib/sanity/queries/faqs.ts](lib/sanity/queries/faqs.ts).

**video.** Fields: title, slug, `embedUrl` (single URL covering YouTube/Shorts, Vimeo, Instagram, Facebook - provider auto-detected, no dropdown), optional custom `thumbnail` (image), `description` (**rich text - the `richAnswer` Portable Text type as of M5-516/Task B, so descriptions can contain links**; rendered via the shared [components/portable-text/RichAnswer.tsx](components/portable-text/RichAnswer.tsx). The VideoObject JSON-LD `description` AND the meta/OG/Twitter descriptions AND the card teaser all use the **plain-text extraction** `portableTextToPlain()` - those must stay plain strings), `relatedProducts` (P2-AI-003 - array of the shared `blogProduct` entry, rendered as a "Featured Custom Promotional Products" strip under the description on `/videos/<slug>` via [components/videos/VideoRelatedProducts.tsx](components/videos/VideoRelatedProducts.tsx); SKUs resolve server-side from `products.json` like the blog strips, page stays SSG; AI-prefilled by the "Generate Video Details with AI" action - see the Phase 2 AI Video tool note in Section 23), `categories[]` (**multi-category as of Q-180, 2026-08-07**: an array of references to the shared `blogCategory` taxonomy, replacing the legacy single `category` reference. **Backward-compatible read, never a destructive migration:** every read path projects BOTH fields (`categories` + the legacy ref as `legacyCategory`) and normalizes through the pure `effectiveVideoCategories` / `videoCategoriesOf` ([lib/video/video-categories.ts](lib/video/video-categories.ts), vitest-covered), where the new list wins when non-empty, else the legacy single value as a one-item list; the legacy schema field stays readOnly + hidden-when-empty. The separate idempotent migration script [scripts/migrations/migrate-video-categories.ts](scripts/migrations/migrate-video-categories.ts) (`--dry-run` supported, NOT run at build) moves legacy values into the list. Consequences wired everywhere: the `/videos` index filter shows a video under EACH of its categories and exactly once when cleared (the filter narrows the card list, it never expands it); `getRelatedVideos` matches ANY shared category and ranks more-shared-categories higher via the pure `rankRelatedVideos` (ties keep newest-first; single-category data reduces to the old rule); the search delta emits ONE entry per video with the category titles joined into the single `category` search key), publishDate, an optional `seo` object (the shared `seo` type: Meta Title / Meta Description / OG Image), plus Studio-only AI inputs (`aiScript`, `aiTopicKeywords`, `aiSuggestedLinks`). The detail page prefers `seo` when set and otherwise auto-derives SEO from Title → meta title, plain-texted Description → meta description, and the thumbnail → social image. Patrick adds a video by pasting a link (embed, never an upload) - the player stays on the source platform; we only store the URL. Embed parsing lives in [lib/video/embed.ts](lib/video/embed.ts) (`parseVideoEmbed()` → `{ provider, embedSrc, aspect }`; Shorts/reels render vertical 9:16, the rest 16:9) and renders via the client [components/videos/VideoEmbed.tsx](components/videos/VideoEmbed.tsx). YouTube/Shorts and Vimeo embed cleanly; Instagram/Facebook are best-effort (their embeds can fail on privacy settings) - set a custom thumbnail and test those. Card thumbnail priority: custom `thumbnail` → YouTube auto (`img.youtube.com/vi/<id>/hqdefault.jpg`) → brand-tinted placeholder. Drives `/videos` (index: card grid + client-side category filter via [components/videos/VideosBrowser.tsx](components/videos/VideosBrowser.tsx)) and `/videos/[slug]` (detail: responsive player + related videos in the same category + VideoObject JSON-LD via `videoObjectSchema()` in [lib/seo/schema-generators.ts](lib/seo/schema-generators.ts)). On-demand SSG (`dynamicParams=true`, `revalidate=false`; `/videos` index is `force-static` + 1h ISR), webhook revalidates `/videos` + `/videos/<slug>` on publish. **Freshness (M5-516 follow-up):** all video reads go through the **non-CDN `cachedClient`** with the `VIDEOS_TAG` (`videos`) cache tag, and the webhook `revalidateTag('videos','max')`s on `video` publish - deterministic instant updates, no stale-CDN re-cache (same pattern as `faqs`). **VideoObject correctness (FIX-830 task 2, 2026-08-14).** Google's Videos enhancement report showed **2 valid items** against 71 published videos. A live check of ALL 71 `/videos/<slug>` pages found the markup COMPLETE on 71/71 - `name`, `description`, `thumbnailUrl` and `uploadDate` (Google's four required fields) present everywhere, 0 JSON parse errors, all 71 thumbnails resolving 200 with real image bytes, the player `<iframe>` in the static HTML, no `noindex`, self-canonical, and all 71 URLs in the sitemap. **So the markup is not what limits the count**; the gap is crawl/verification coverage, and nothing in the code can be pointed at as the cause of 2-vs-71. What WAS genuinely wrong, on 71 of 71: **`contentUrl` was the YouTube watch/Shorts page**. Google's reference is explicit - *"A URL pointing to the actual video media file... Don't link to the page that the video is embedded in"* - and Google FETCHES `contentUrl` to verify the video, so it was being handed HTML where it expected a media file. `videoObjectSchema()` now passes it through the new pure `isDirectMediaUrl()` gate (a recognised media extension, optional query string) and **omits it otherwise**; `embedUrl` alone is complete and correct for third-party-hosted video, so nothing is lost. The schema also gained `url` = the page canonical. **`duration` is still absent and stays absent** - it is recommended by Google, nothing in Sanity records it, and inventing one would be worse than omitting it; the only honest sources are Patrick or the YouTube API. **Videos embedded elsewhere emit no VideoObject and should not**: the page-builder `videoEmbed` section has only an optional heading and caption and NO upload date, and blog-body embeds carry no metadata at all, so emitting one would mean fabricating `uploadDate`. Reported, deliberately not "fixed". Query helpers in [lib/sanity/queries/videos.ts](lib/sanity/queries/videos.ts). Video title + category are in the search index (`video` type, internal route - title weighted, category as a secondary key). M5-507.

**brand.** Fields: name, slug, logo image, optional URL, description, `featured` (bool). Auto-populated from Geiger products on first scrape (logos scraped from Geiger brand pages in Phase E), manually editable thereafter. **`featured == true` surfaces a brand in the "Featured Brands" strip at the TOP of `/brands`** (Task F — a highlighted row above the full A–Z grid, ordered by name; featured brands still also appear in the A–Z grid; the strip renders nothing when no brand is featured). Rendered server-side in [app/brands/page.tsx](app/brands/page.tsx) via `getFeaturedBrands()` ([lib/brands.ts](lib/brands.ts)). **Freshness:** all brand Sanity reads go through the **non-CDN `cachedClient`** with the `BRANDS_TAG` (`brands`) cache tag ([lib/sanity/cache-tags.ts](lib/sanity/cache-tags.ts)) and `getAllBrands()` is wrapped in React `cache()` (per-request dedup only, NO cross-request module memo), so a `featured` toggle isn't blocked by a stale memo or CDN race; the webhook `revalidateTag('brands','max')` + `revalidatePath('/brands')` (+ `/brands/<slug>`) on `brand` publish/delete, so the strip/grid update in seconds (`/brands` stays static/ISR). **⚠️ The Sanity webhook FILTER must include `brand`** — it was originally EXCLUDED to save deliveries (like `faq` before it); add it on staging now + production at launch or the toggle never revalidates ([docs/sanity-webhook-setup.md](docs/sanity-webhook-setup.md)).

**leadSubmission.** Read-only document type written by the lead form API route. Fields: name, email, phone, quantity, date needed, looking-for, source page, timestamp, `recipient` ("Lead sent to" — set on landing-page (P2-AI-005) and product-quote (P2-CP-002) submissions: the email the lead notification actually went to, i.e. that page's/product's `leadRecipient` or the site default), **product-quote fields (P2-CP-002): `company`, `shippingZip`, `comments`, `productTitle` (server-resolved), `productSlug`**, **catalog-request fields (P2-CAT-002): `catalogTitle`/`catalogSlug` (server-resolved — the catalog whose gated link was emailed)**, **configurator selection fields (P2-CP configurator): `selectedColor`, `selectedSize`, `selectedDecoration`, `estimatedTotal` (the LABELED estimate the customer saw — never a firm price)**, and `attachments` (array of `file` — optional logo/artwork the visitor uploaded; emailed to Patrick AND stored here as Sanity assets so they're viewable in Studio). The lead form route ([app/api/leads/route.ts](app/api/leads/route.ts)) accepts `multipart/form-data` (optional file uploads — up to 3 files, ≤10MB each / ≤20MB total, types `.pdf/.png/.jpg/.jpeg/.gif/.svg/.ai/.eps`, re-validated server-side), attaches the files to Patrick's email (Nodemailer), and uploads them as Sanity assets (non-fatal if upload/write fails — email still sends). It also verifies a Cloudflare Turnstile token (auto-detect CAPTCHA, no-ops without env keys — see Section 14) alongside the honeypot + rate limit. **Landing-page routing (P2-AI-005 part 2):** an optional hidden `landingSlug` in the POST maps — server-side only — to that landing page's stored `leadRecipient` as the destination (never a client-supplied address; unknown slug = normal behavior) and triggers an automatic customer confirmation email (non-fatal, landing-only). **Product-quote routing (P2-CP-002):** an optional hidden `productSlug` does the same against the `productPage` doc (`getProductLeadInfo` — recipient AND title resolved server-side; landing wins if both slugs are present; quote submissions require `company` instead of `lookingFor`, get a product-named email subject, and also send the customer confirmation); all other forms behave exactly as before. **Form-builder routing (P2-FB-001):** an optional hidden `formSlug` branches the route into `handleBuilderFormSubmission()` — the `form` doc is re-resolved server-side (recipient, confirmation toggle, AND the field definitions, against which required/type/option validation re-runs — a crafted payload can't skip a required field), the lead email carries the form title as subject + a label:value block of all answers, and the record gets `formTitle`/`formSlug`/`answers[]` (`{label, value}` in field order) with name/email/phone/company filled by label/type heuristics; all non-builder forms never send `formSlug` and stay byte-identical. **Catalog-request routing (P2-CAT-002):** a builder submission may additionally carry a hidden `catalogSlug` (the catalog landing CTAs) — validated server-side against a published `catalogPage`; on a match the record gains `catalogTitle`/`catalogSlug`, the lead email a first "Catalog" row, and the customer receives the **gated catalog link email** (cc the resolved recipient) instead of the generic summary — see Section 7 catalogPage "Lead capture".

**form.** Reusable form builder (P2-FB-001/002) — Patrick builds ANY quote/contact form himself; a page CTA opens it in a modal. Fields ([sanity/schemas/documents/form.ts](sanity/schemas/documents/form.ts)): `title` (heading + lead-email subject), `slug` (the form's stable id — buttons reference it; the submit route resolves the recipient from it SERVER-SIDE, so the browser never carries an address — no open relay, and the recipient is stripped before the definition is passed to the client island via `toFormRenderDef`), `recipientEmail` (email-validated, default patrick@), `sendCustomerConfirmation` (default true — customer gets a summary copy, non-fatal), `intro`, `fields[]` (label + fieldType `shortText`/`longText`/`email`/`phone`/`number`/`date`/`dropdown`/`checkbox`/`checkboxGroup`/`fileUpload` + options (min-1 validated for dropdown/checkboxGroup) + required + placeholder + **`width` ('full' default / 'half' — two consecutive half-width fields share a desktop row, e.g. First Name + Last Name; an unpaired half still takes the full row; mobile always one column. EDITOR-set only — the renderer never auto-pairs by field type, since a form can carry any number of unknown fields; paired cells bottom-align their inputs so a wrapping label doesn't misalign the row)**), `submitButtonLabel`, `successMessage`. Rendered by the fully generic client island [components/forms/FormRenderer.tsx](components/forms/FormRenderer.tsx) (+ [FormModal](components/forms/FormModal.tsx)/[FormModalButton](components/forms/FormModalButton.tsx), modal lazy `ssr:false`) — input names + validation derive from the shared pure [lib/forms/form-def.ts](lib/forms/form-def.ts) (`fieldName`/`validateAnswers`), imported by BOTH the client and the `/api/leads` route so they can never drift; file uploads reuse the shared attachment limits; honeypot + rate limit + Turnstile identical to LeadForm. Emails: pure builders in [lib/leads/form-lead.ts](lib/leads/form-lead.ts) + the generic `sendBuiltEmail` (gmail-smtp). **CTA wiring:** optional `formSlug` on ctaBlock buttons / `ctaFormSlug` on heroBanner — [CtaBlock](components/page-sections/CtaBlock.tsx)/[HeroBanner](components/page-sections/HeroBanner.tsx) are async server components resolving the form via the tag-cached `getFormBySlug` ([lib/sanity/queries/forms.ts](lib/sanity/queries/forms.ts)) and rendering the modal button; unresolved/unpublished → falls back to the button's href (never dead-ends). **Freshness:** non-CDN `cachedClient` + `FORMS_TAG`/`form:<slug>` ([lib/sanity/cache-tags.ts](lib/sanity/cache-tags.ts)); webhook `form` case busts both + revalidates the four `/services/<slug>` (tag invalidation covers any other embedder) — **⚠️ `form` must be manually ADDED to the Sanity webhook Filter on staging + production** (new type; Projection unchanged — [docs/sanity-webhook-setup.md](docs/sanity-webhook-setup.md)). Embedding pages stay static (the island reads no searchParams; sourceUrl post-mount). **Four service quote forms** (P2-FB-002) seed as DRAFTS via `pnpm seed-service-forms` ([scripts/seed/seed-service-forms.ts](scripts/seed/seed-service-forms.ts) — skips already-published forms; also idempotently patches the four service pages' "Request a Quote" ctaBlock buttons with their `formSlug`, keeping `/contact` as the href fallback, **and backfills the seed's half-widths onto EXISTING published/draft forms — label-matched, only where `width` is still unset, never overriding an editor's explicit choice; ran 2026-07-11 against live**): `kitting-quote`, `company-stores-quote`, `custom-products-quote` (file-upload field), `popup-stores-quote` (shipping dropdown); all email patrick@ + confirm to the customer; common block Company/First/Phone/Email required, Last optional. Publish each form to flip its button from the /contact link to the popup.

**page.** Generic, section-based content page powering the website-builder (M5-506b). Fields: `title`, `slug` (the route segment, e.g. `kitting` → `/services/kitting`), `seo` (reuses the shared `seo` object), and `sections[]` — an ordered array of polymorphic section objects. The array gives Patrick website-builder behavior in Studio: drag-reorder, insert any section type, delete, and **hide-without-deleting** (every section has a `hidden` boolean that the `SectionRenderer` skips). Section types (all in [sanity/schemas/objects/page-sections.ts](sanity/schemas/objects/page-sections.ts), all reusable): `heroBanner` (heading/subheading + optional CTA + banner image; `overlayText` toggles between text-overlaid-on-image and the default text-on-top-with-full-image-below), `richText` (portable text — headings/sub-headings, paragraphs, bullet + numbered lists, bold/italic, quotes, inline links (Geiger links auto-route through `lib/affiliate-url.ts`), **and INLINE images dropped between paragraphs** (the `portableBody` array's `of` includes `{type:'image'}`; rendered by the `image` handler in [components/page-sections/portable-text.tsx](components/page-sections/portable-text.tsx)) — plus an optional `anchorId` making the section an in-page jump target), `imageText` (vertical stack: heading → full-width image → portable text; never overlays text on the image; its body also supports inline images), `infographic` (full-width image + caption), `iconFeatures` (icon/heading/text columns), `statBanner` (colored stat banner), `cardGrid` (cards with optional image + CTA), `ctaBlock` (heading + buttons), `eventList` (city/venue/date/time rows), `videoEmbed` (**paste a YouTube/Shorts/Vimeo/Instagram/Facebook URL → responsive player**; provider auto-detected via `parseVideoEmbed` in [lib/video/embed.ts](lib/video/embed.ts), rendered through the shared client [components/videos/VideoEmbed.tsx](components/videos/VideoEmbed.tsx) by [components/page-sections/VideoEmbedSection.tsx](components/page-sections/VideoEmbedSection.tsx); optional heading + caption; embed-only, nothing hosted), `faqAccordion` (Q&A with FAQPage schema), `productStrip` (P2-AI-004 — **live product strip on any page**: `heading?` + `products[]` of the shared `blogProduct` entry (SKU-backed or manual title/image/url); [components/page-sections/ProductStrip.tsx](components/page-sections/ProductStrip.tsx) resolves SKUs SYNCHRONOUSLY from disk via `resolveProductsBySku` + renders the shared ProductCard grid with the VideoRelatedProducts-style manual-fallback card — no `searchParams`, no uncached read, so every page route stays SSG; the fields ride the existing `sections[]{...}` GROQ spread, no projection/tag/webhook change; also what "Generate Page with AI" pre-fills and what the P2-AI-005 landing pages will reuse). Image fields are paired — a Sanity `image` (preferred) plus an `imageUrl` string fallback; renderers prefer the asset. The type is intentionally generic so it powers the Services pages, the footer/legal static pages (M5-506), AND arbitrary custom top-level pages (Issue 2), not just Services. Rendered by slug via `SectionRenderer` ([components/page-sections/](components/page-sections/)); the four Services pages render at the existing `lib/nav-data.ts` Services routes (`/services/kitting`, `/services/company-stores`, `/services/popup-stores`, `/services/custom-products`) through [app/services/[slug]/page.tsx](app/services/[slug]/page.tsx) (on-demand SSG: `dynamicParams=true`, `revalidate=false`, webhook revalidation on publish). The **footer/legal pages** (M5-506 — `/about`, `/contact`, `/sample-policy`, `/shipping-policy`, `/returns`, `/privacy-security`, `/company-core-values`, `/terms`; slugs mirror the live site exactly for SEO preservation) render through the shared [components/page-sections/StaticPage.tsx](components/page-sections/StaticPage.tsx) (`StaticPage` + `staticPageMetadata`) from thin top-level route files; `/contact` additionally renders the lead form in-code. **Top-level custom pages (Issue 2):** any published `page` whose slug is NOT reserved also renders at `/<slug>` via the **root catch-all** route [app/[...slug]/page.tsx](app/[...slug]/page.tsx) (same `SectionRenderer` + `Breadcrumbs` + `CustomSchemaJsonLd` pipeline, on-demand SSG, `generateStaticParams` from `getAllPageSlugs()` minus reserved/service slugs, each slug `.split('/')` into segments) — this lets Patrick recreate arbitrary old-site top-level URLs (e.g. `/llm-info-perfect-imprints`) from Studio with no code change. **The route is a catch-all (`[...slug]`), so a page slug MAY contain slashes and renders at its full multi-segment path** (e.g. slug `industry/hvac` → `/industry/hvac`). It used to be a single-segment `[slug]` route, which silently 404'd every slash-containing slug even though the doc published fine (the schema reserved-guard only blocks single top-level segments, so the multi-segment slug passed validation but had no matching route); it was converted to `[...slug]` so multi-segment pages route. `params.slug` arrives as a `string[]` and is `join('/')`ed back into the doc slug, matched verbatim by `getPageBySlug` (`slug.current == $slug`) — the queries and sitemap already carried the full slash-containing slug, so only the route needed changing. **Reserved-slug guard:** a hard reserved set — every existing top-level/folder route + `api`/`admin3773752` + the 8 fixed static slugs — lives in [lib/reserved-slugs.ts](lib/reserved-slugs.ts) (`RESERVED_SLUGS`), mirrored inline in the `page` schema; the schema **rejects publishing a page at a reserved slug** ("This URL is reserved by the site; choose another slug"), and the route `notFound()`s reserved slugs + the Services page slugs (which live under `/services/`) so a page never shadows a real route or renders duplicate content. A **root catch-all is the lowest-priority match** — literal + dedicated folder routes always win, so `/cat/...`, `/blog/...`, `/services/*`, etc. stay intact and never reach it; the non-optional catch-all also does not match `/` (served by `app/page.tsx`). (Edge case: a page slug whose FIRST segment is a nested folder route — e.g. `blog/x` — would be shadowed by that folder route and never render via the catch-all; use a non-reserved first segment.) New pages are added to [app/sitemap.ts](app/sitemap.ts) (via `getAllPageSlugs()` minus reserved + service slugs, deduped against services/static, indexable). All `page` reads go through the non-CDN cache-tagged `cachedClient` (`PAGES_TAG` list-level + `page:<slug>` content-level, see [lib/sanity/cache-tags.ts](lib/sanity/cache-tags.ts)) so publishes revalidate deterministically while `/services/<slug>` + `/<slug>` stay static/SSG. The webhook's `page` case busts `PAGES_TAG` + `page:<slug>` and revalidates `/services/<slug>` + `/<slug>` + `/sitemap.xml` on publish (`page` is already in the webhook Filter with `slug` in the Projection — no manual change needed). **Cache-tag sanitizer (prod incident 2026-07-02):** because the root catch-all runs the tagged `getPageBySlug` fetch (`pageTag(slug)`) + `CustomSchemaJsonLd`'s `customSchemaTag('/'+slug)` for EVERY unmatched top-level path BEFORE it `notFound()`s, bot junk URLs (`/wp-login.php`, `%`-encodings, uppercase, dots, unicode) built a tag with a character invalid in an `x-next-cache-tags` header and the route **500'd instead of 404'ing** (~23 fails/5min). Fix: all slug/path tag builders in [lib/sanity/cache-tags.ts](lib/sanity/cache-tags.ts) (`categoryTag`/`pageTag`/`customSchemaTag`) now run their value through `sanitizeTagValue()` — lowercase, KEEP `/ _ -` (so existing `cat:water-bottles/color/blue` + `customSchema:/` tags are byte-identical → **no `/cat` freshness churn**), map every other char-run to `-`, trim, length-cap under 256; empty/all-invalid → `''` and the builder returns `''` so fetch sites drop it via `.filter(Boolean)` (a bare `base:` tag is never emitted). The webhook calls the SAME builders (via a `bustTag()` guard that skips `''`), so read+revalidate tags stay consistent and freshness is preserved — only invalid tags are prevented. Query helpers in [lib/sanity/queries/pages.ts](lib/sanity/queries/pages.ts). Seed Services drafts with `pnpm seed-service-pages` (structure-only placeholder copy); seed the footer pages with `pnpm seed-static-pages` (M5-506 — content reproduced **full and verbatim** from PI's OWN live site / Wayback of the same slugs: Sample Policy + Privacy verbatim; **Shipping/Returns/Core-Values refilled verbatim** in the 2026-06-26 pass — the earlier extraction had condensed them; **Terms of Service now seeds PUBLISHED with the complete verbatim "Terms & Conditions"** — 22 sections, previously a boilerplate draft; the seed also deletes the stale `drafts.page-terms` so the published perspective stays clean. **About** is the one exception: the live `/about` is Cloudflare-blocked and its only Wayback snapshot is a near-empty template — the seed keeps the verified "culture of family" line + PI's own Mission/Vision and drops the prior unverifiable founder/paramedic story; **flag for Patrick to paste current About copy**. A few PI source typos in Terms are preserved verbatim — e.g. "insignifanct", "the sue of its catalog", and a stray editor note in the Disputes clause — flag rather than silently rewrite Patrick's legal text).

**landingPage.** Local/topic landing page (P2-AI-005 part 1 — Patrick's Phase 2D flagship). A **FIXED high-converting template** (not the section builder): hero (green "Request a Quote" button anchoring to the bottom form) → `localIntro` → `optionsIdeas` + related-products strip → fixed-H2 "Why Perfect Imprints" + `whyUs` → FAQ accordion → lead form. Renders at its top-level slug (e.g. `/custom-beach-towels-destin-fl`) via the SAME root catch-all [app/[...slug]/page.tsx](app/[...slug]/page.tsx), **resolved BEFORE `page`** (landingPage wins a slug collision); same reserved-slug guard as `page` (third inline mirror of `lib/reserved-slugs.ts` — keep in sync). Fields — targeting inputs (drive the AI): `city`, `state`, `product`, **`landmarks[]` (must-include local landmarks — the generate route mechanically REJECTS a result missing one, via `findMissingLandmarks` in [lib/ai/landing-landmarks.ts](lib/ai/landing-landmarks.ts))**, `aiTopicKeywords[]`; content (AI-drafted, editable): `heroHeading`/`heroSubheading`, `localIntro`/`optionsIdeas`/`whyUs` (**the page-builder `portableBody` shape** — rendered via `pagePortableComponents`, links are the default href-only annotation; `optionsIdeas` carries its H2s IN the body, built by `buildPageSectionsBody` in [lib/portable-text/build-page-body.ts](lib/portable-text/build-page-body.ts)), `relatedProducts[]` (shared `blogProduct` entries → rendered through the P2-AI-004 `ProductStrip`), `faqs[]` (plain-text q/a → rendered through the page `FaqAccordion`, which emits the FAQPage JSON-LD — not double-emitted), `leadFormHeading` + `heroCtaLabel` (part 2 — editable CTA wording; blank → "Request a Quote in {City}, {State}" / "Request a Quote"; the AI pre-fills `leadFormHeading` only-if-empty), `leadRecipient` (part 2 — **this page's lead emails go here**, resolved SERVER-SIDE from the slug via `getLandingLeadInfo` in the `/api/leads` route: the browser posts only a hidden `landingSlug`, never a recipient address, so no open relay; invalid/blank → site default; the resolved destination is recorded on the `leadSubmission` doc's `recipient` field. Landing submissions ALSO send the customer an automatic confirmation email — `sendCustomerConfirmationEmail` in [lib/email/gmail-smtp.ts](lib/email/gmail-smtp.ts), content from the pure [lib/leads/landing-lead.ts](lib/leads/landing-lead.ts), non-fatal, landing-only — non-landing forms are byte-for-byte unchanged), `seo`, `aiSuggestedLinks`. Template: [components/landing/LandingPageTemplate.tsx](components/landing/LandingPageTemplate.tsx) (+ `landingServiceSchema()` Service JSON-LD when product+city exist). "Generate Landing Page with AI" action ([sanity/actions/generate-landing-with-ai.tsx](sanity/actions/generate-landing-with-ai.tsx), needs City+State+Product) POSTs to [app/api/sanity/generate-landing/route.ts](app/api/sanity/generate-landing/route.ts) (`nodejs`+`force-dynamic`); internal links (now suggesting **videos + other landing pages** too — [lib/ai/internal-links.ts](lib/ai/internal-links.ts)) are auto-placed across all three bodies in one pass ('page' link shape); never overwrites Patrick's inputs, never publishes. Freshness: non-CDN `cachedClient` + `LANDING_TAG`/`landing:<slug>` ([lib/sanity/queries/landing-pages.ts](lib/sanity/queries/landing-pages.ts)); webhook `landingPage` case busts both + `/<slug>` + sitemap — **⚠️ `landingPage` must be manually ADDED to the Sanity webhook Filter on staging + production** (new type; Projection unchanged — see [docs/sanity-webhook-setup.md](docs/sanity-webhook-setup.md)). Sitemap lists landing slugs (deduped, landing wins). Part 2 (DONE 2026-07-07): per-page recipient + landing-only customer confirmation + editable CTA (see the `leadRecipient`/`leadFormHeading`/`heroCtaLabel` field notes above).

**catalogPage.** Geiger themed-catalog lead page (P2-CAT-001, prompt 2 of 4 — Milestone 3). ONE doc drives TWO routes: the PUBLIC SEO landing page **`/shop-by-theme/<slug>`** (indexed, in the sitemap) and the GATED product page **`/shop-by-theme/<slug>/catalog`** (**noindex + excluded from the sitemap + never in a menu** — reached only via the link the P2-CAT-002 form will email; "hidden from Google" is the guarantee, NOT auth). Fields ([sanity/schemas/documents/catalog-page.ts](sanity/schemas/documents/catalog-page.ts)): Basics — `title`, single-segment `slug` (slash/uppercase rejected at publish — the app/[slug] lesson), **`catalogKey`** (which `data/geiger/catalogs.json` entry supplies the synced products/filters/Browse link — the Phase I scraper's STABLE slugs; the 2 manual-only catalogs Retail Collective/Trend Talk have an empty synced set, their adds are the whole grid), `browseCatalogUrl` (optional override — the default resolves from the data file at render, never duplicated into Sanity); Landing — `heroHeading`/`heroSubheading`/`heroImage`, `body` (the page-builder `portableBody`, inline sized images), `ctaHeading`/`ctaButtonLabel` (one copy set feeds all THREE CTA slots top/middle/end), `seo`; Gated products — `addedSkus` (`ProductSkuPicker`), `addedProducts[]` (refs to `customProduct`/`productPage` — the categoryOverride union; a productPage renders an internal `/products/<slug>` card and joins the filters), `hiddenSkus`; Related strips — `relatedKeywords[]`, `relatedBlogs[]`/`relatedVideos[]` refs (manual first, `suggestLinksForKind` auto top-up, card data via the tag-cached `getBlogSummariesBySlugs`/`getVideoSummariesBySlugs` so blog/video publishes refresh the strips with NO webhook change — the productPage-strips pattern; **rendered on BOTH routes** via the shared async server component [components/catalogs/CatalogRelatedContent.tsx](components/catalogs/CatalogRelatedContent.tsx) — the gated page's bottom AND the public landing page's bottom below the end CTA (P2-CAT-005, Patrick's SEO ask) — one resolution, no fork, empty → renders nothing, all reads tag-cached so both routes stay static). **Data:** [lib/catalogs.ts](lib/catalogs.ts) mirrors `lib/deals.ts` (parse-once per-slug Map over catalogs.json, `applyHiddenSkus` facet re-derive, `getAugmentedCatalogData` through the same `augmentAggregator` lane — synced + added − hidden with filter-tag injection); the facets are deals-shaped so the gated grid ([components/catalogs/CatalogProductsClient.tsx](components/catalogs/CatalogProductsClient.tsx)) REUSES `DealsFilterSidebar` + the deals `ClientPagination` + `applyDealsFilters` directly (no fork). **Lead capture (P2-CAT-002, prompt 3):** the three landing CTAs ([components/catalogs/CatalogCta.tsx](components/catalogs/CatalogCta.tsx)) open the seeded **`catalog-request` builder form** (First/Last/Company/Phone/Email required + optional Comments — `pnpm seed-catalog-form`, DRAFT, skip-if-published; `CATALOG_FORM_SLUG` in [lib/leads/catalog-lead.ts](lib/leads/catalog-lead.ts)) via the standard `FormModalButton`, which gained a generic **`hiddenFields`** prop carrying `{catalogSlug}` (set into the POST before `formSlug`/`sourceUrl` so it can never override them); the landing page resolves the form ONCE via the tag-cached `getFormBySlug` and shares it across the three CTAs (unpublished → /contact link fallback, never a dead end). The `/api/leads` builder branch validates the hidden `catalogSlug` server-side (`getCatalogLeadInfo` — published catalogPage only; unknown/forged slug degrades to a plain form submission) and on a match: the lead email + `leadSubmission` carry a first "Catalog" answer row + `catalogTitle`/`catalogSlug` fields, and **the customer is emailed the gated `/shop-by-theme/<slug>/catalog` link** (`buildCatalogConfirmationEmail`, pure, in catalog-lead.ts; URL built server-side from `NEXT_PUBLIC_SITE_URL` and **force-normalized to `https://` by `gatedCatalogUrl`'s `httpsOrigin()`** — P2-CAT-005: a scheme-less or `http://` env value can't leak an http link into the email; host untouched; **cc = the resolved lead recipient** via `sendBuiltEmail`'s new optional `cc`) INSTEAD of the generic summary confirmation — non-fatal, only to a valid address, and sent REGARDLESS of the form's `sendCustomerConfirmation` toggle (it is the deliverable, not a courtesy copy). No client-supplied recipient exists anywhere (the form doc's stored address, server-resolved, is the only destination). **Menu (P2-CAT-003):** `pnpm seed-shop-by-theme-menu` ([scripts/seed/seed-shop-by-theme-menu.ts](scripts/seed/seed-shop-by-theme-menu.ts)) upserts ONE "Shop by Theme" `dropdown` item (key `item-shop-by-theme`, links = published LANDING pages, inserted after "Deals") into the megaMenu singleton — targeted patch, other items untouched, draft patched too, no-op when zero catalogPages are published; note `pnpm seed-mega-menu` (full reset) DROPS it — re-run this script after. A newly published catalog is NOT auto-added (Patrick adds the link in Studio or re-runs the script). **"Generate Catalog Page with AI" (P2-CAT-004, prompt 4):** [sanity/actions/generate-catalog-with-ai.tsx](sanity/actions/generate-catalog-with-ai.tsx) → [app/api/sanity/generate-catalog/route.ts](app/api/sanity/generate-catalog/route.ts) (`nodejs`+`force-dynamic`, registered in [sanity/sanity.config.ts](sanity/sanity.config.ts); needs `title`) drafts EDITORIAL landing content only — hero heading/subheading + the long-form `body` (ONE page-shape Portable Text via `buildPageSectionsBody`, H2s IN the body, internal links auto-placed 'page' shape, 600-1000-word ask with a 450-word thin-floor 502 retry) + `seo.metaTitle`/`seo.metaDescription` (refreshed every run) and — **only-if-empty** — `relatedKeywords`/`aiSuggestedLinks`/slug. The prompt is GROUNDED with ~12 real product names from the `catalogKey`'s catalogs.json entry (names only — specs/prices forbidden; manual-only catalogs degrade to thematic copy). **HARD BOUNDARY (the productPage/landing rule): the AI never touches `catalogKey`, addedSkus/addedProducts/hiddenSkus, the browse link, or any commercial fact.** Studio-only `ai` fieldset (`aiTopicKeywords`/`aiBrief`/`aiSuggestedLinks`) read by no render path or webhook; no webhook/tag/env change (pure P2-AI-001 engine reuse; `catalogPage` already in the Filter). Never auto-publishes. **Catalog photos (both options):** Patrick uploads his own via `heroImage` + inline sized images in `body`; or `pnpm fetch-catalog-images <slug>|--all [--limit N]` ([scripts/catalogs/fetch-catalog-images.ts](scripts/catalogs/fetch-catalog-images.ts)) downloads the catalog's `<pdf.baseUrl>/Leaf_N.jpg` page rasters into the gitignored `tmp/catalog-images/<slug>/` for him to pick from (editorial choice — never auto-inserted, never bulk-uploaded to Sanity; per-page/per-catalog skip-and-warn since the CloudFront URLs are undocumented vendor internals; Retail Collective/Trend Talk have no fetchable pages → screenshot the flipbook). Both routes prebuild all published slugs (`dynamicParams=true`/`revalidate=false`, the /products pattern) and are static-safe (no searchParams, no uncached reads; filtering is in-memory client state over baked JSON). Landing page emits CollectionPage JSON-LD + BreadcrumbList + `CustomSchemaJsonLd` + full socialMeta (ogImage → hero → first scraped product). **Freshness:** non-CDN `cachedClient` + `CATALOG_PAGES_TAG`/`catalog-page:<slug>` ([lib/sanity/queries/catalog-pages.ts](lib/sanity/queries/catalog-pages.ts)); webhook `catalogPage` case busts both tags + both paths + sitemap, and `findEmbeddingContentDocs` includes `catalogPage` so an edit to a referenced productPage/customProduct in `addedProducts` refreshes the embedding catalog pages — **⚠️ `catalogPage` must be manually ADDED to the Sanity webhook Filter on staging + production** (new type; Projection unchanged — [docs/sanity-webhook-setup.md](docs/sanity-webhook-setup.md)). `shop-by-theme` is reserved in [lib/reserved-slugs.ts](lib/reserved-slugs.ts) + the page/landingPage schema mirrors.

**customSchema.** Custom structured data (JSON-LD) attachable to **any page** without taking the page over / pushing it to Sanity (Task C Part 2). Keyed by `pageUrl` (the exact site-relative path — `/cat/water-bottles`, `/blog/<slug>`, `/videos/<slug>`, `/about`, `/`), so it's the **same "one doc per page you want to touch"** model as `categoryOverride` (no bulk push). Fields: `pageUrl` (string, **picked with the searchable [sanity/components/PageUrlPicker.tsx](sanity/components/PageUrlPicker.tsx) `PageUrlInput`** — searches the all-URL `category-list.json` to target any of the 22,180 `/cat/...` pages AND accepts a manually typed path for non-category pages; validated: must start with `/`, no domain, no trailing slash except root), optional `label` (a note to tell entries apart), optional `schemaType` + `aiContext` (Task C-2 — only feed the AI generator, see below), and `jsonLd[]` (raw JSON-LD object strings — **each block is custom-validated at publish time as parseable JSON with `@context` + `@type`**, so a broken block can't be published; multiple blocks per page allowed, e.g. a WebApplication + a Product). **Two ways to fill `jsonLd[]`: (Option 1) paste a raw JSON-LD object from a schema-markup generator, or (Option 2, Task C-2) pick `schemaType` from the curated dropdown (+ optional `aiContext` notes) and click the "Generate schema with AI" Studio document action** — registered for `customSchema` in [sanity/sanity.config.ts](sanity/sanity.config.ts) ([sanity/actions/generate-schema-with-ai.tsx](sanity/actions/generate-schema-with-ai.tsx)), it POSTs to [app/api/sanity/generate-schema/route.ts](app/api/sanity/generate-schema/route.ts) (DeepSeek, reuses server-side `DEEPSEEK_API_KEY` — fails gracefully with a clear message if absent, returns ONE JSON-LD object of that type validated for `@context` + matching `@type`) and **appends** the result as a new (editable, removable, publish-validated) `jsonLd[]` block — never auto-publishes; on invalid output it shows an error dialog and adds nothing. The `schemaType` dropdown is a **curated list that deliberately excludes the schemas the site already auto-emits** (FAQPage, Organization, WebSite, BreadcrumbList, VideoObject, CollectionPage, BlogPosting) to avoid duplicates — the `jsonLd[]` field description carries the same don't-re-add caution. Rendered by the shared async server component [components/seo/CustomSchemaJsonLd.tsx](components/seo/CustomSchemaJsonLd.tsx) (`<CustomSchemaJsonLd path={…} />`), dropped into the public page templates: category route, blog detail, **blog index (`/blog`) + its pagination (`/blog/page/N`) + blog category (`/blog/cat/<slug>`) + its pagination (`/blog/cat/<slug>/page/N`)** (Task C-3 — listings were originally missed), video detail, `/videos` index, the `StaticPage` static/legal renderer, services (`/services/<slug>`), home, brands index + per-brand, deals/new-products/rush-products, `/promotional-products` (path-keyed to the clean canonical, which all filter/sort/page variants canonicalize back to), and `/faq`. Each call passes the page's exact canonical path so the `pageUrl` lookup matches. (Not injected on `/search` (noindex), the `/rush-promotional-products` legacy stub (canonicalizes to `/rush-products`, which is covered), or internal/admin routes.) It reads the doc(s) for the exact path via a **cache-tagged** fetch ([lib/sanity/queries/custom-schema.ts](lib/sanity/queries/custom-schema.ts) → `getCustomSchemaForPath`, tag `customSchema:<path>`, `revalidate: false`, never `no-store` — so `/cat` and every other host route stay statically prerenderable), emits each block as a `<script type="application/ld+json">` (with `<` escaped to prevent `</script>` breakout), and renders **nothing** when no doc matches (so pages without custom schema pay almost nothing). The webhook busts `customSchema:<pageUrl>` + `revalidatePath(pageUrl)` on `customSchema` publish/delete (filter + projection must include `customSchema` / `pageUrl`); Task C-2 adds NO new read surface or webhook change — it only fills the existing `jsonLd[]`, already covered by the render-time cache tag. Raw JSON-LD requires valid JSON; guided per-type editors can be layered on this raw-block + AI foundation later.

**quote.** Customer quotation (Q-110 data foundation, Q-130 Studio builder, Q-140 customer page - still NO PDF, send email, view notification, or accept/revise flow; those are later prompts). Patrick builds a quote in Studio: line items, customer block, rep block, dates, sales tax; the customer opens it READ-ONLY at the private link `/quote/<token>` (Q-140, see the end of this entry). Fields ([sanity/schemas/documents/quote.ts](sanity/schemas/documents/quote.ts)), by fieldset: Identity - `quoteNumber` (sequential "Q-1001" style, assigned by the one-click **"Assign quote number"** button in [sanity/components/QuoteInputs.tsx](sanity/components/QuoteInputs.tsx) `QuoteNumberInput`, which runs the concurrency-safe allocator `allocateQuoteNumber` in [lib/quotes/numbering.ts](lib/quotes/numbering.ts): a revision-guarded `ifRevisionId` compare-and-set on the invisible `quoteCounter` singleton (deliberately NOT registered in [sanity/schemas/index.ts](sanity/schemas/index.ts) - the siteRefreshAuth precedent; seeds itself via `createIfNotExists` at `lastNumber: 1000` so the first issued number is 1001; change the start/prefix via API/Vision BEFORE the first quote), bounded retry with jitter, and a LOUD `QuoteNumberAllocationError` on exhaustion - never a guessed, blank, or duplicate number; async dataset-query uniqueness validation on top), `slug` = the **private link token** (16 random bytes as 32 LOWERCASE hex chars, generated once at creation by the slug `initialValue`; lowercase is mandatory because `sanitizeTagValue` lowercases tag values, so mixed case would collapse two tokens onto one cache tag; server twin `generateQuoteToken()` in [lib/quotes/token.ts](lib/quotes/token.ts); token lives in the URL PATH, never a query param; never log a full token), `title` (internal label); Customer (company/name/email/phone/address - only email required, the link is emailed to it); Rep (name/email/phone, pre-filled from the logged-in Studio user + Global Settings. **The phone default reads `globalSettings.phoneNumber` FIRST and `contact.phones[0]` only as a fallback, with a hardcoded `800-773-9472` last (Q-200, 2026-08-07).** The order was reversed after Patrick reported new quotes defaulting to `850-200-4020`: `contact.phones` is the ordered footer LIST, whose first entry is a direct line, while `phoneNumber` is the single MAIN company line the site header advertises and the one a customer expects on a quotation. The fix is in the quote schema's `initialValue`, NOT in the settings document - reordering `contact.phones` would have moved the footer's primary number and the Organization JSON-LD `telephone` with it. **An `initialValue` runs once, at creation, so only NEW quotes are affected and no existing quote is rewritten**; the field stays editable per quote); Dates (`quoteDate` today, `expiryDate` +30 days, editable); `lineItems[]` of FOUR object types ([sanity/schemas/objects/quote-line-items.ts](sanity/schemas/objects/quote-line-items.ts)): `quoteGeigerLine` (SKU via `ProductSkuInput` + displayName/imageUrl/description - Geiger publishes only price RANGES, so Patrick types the cost), `quoteOwnProductLine` (reference to `productPage`; a pre-fill helper arrives next prompt), `quoteCustomLine` (manual name/image/description), `quoteChargeLine` (label + qty x unit price, e.g. art fee); the first three share ONE set of commercial fields (quantity, decorationMethod free text, unitCost, setupCharge, shipping, note). **Pricing snapshot rule (do not "optimise" away): unit cost/setup/shipping are STORED on the quote, never read live from the referenced product** - a sent quote must show the same numbers after Patrick edits the product; **totals are NEVER stored** - `salesTax` is a number Patrick TYPES (not calculated), and everything else (line totals, subtotal, shipping total, grand total) is computed by the pure client-safe [lib/quotes/quote-totals.ts](lib/quotes/quote-totals.ts) (draft-tolerant: missing/negative/non-finite input = 0, never throws; reuses `effectiveSetupCharge` + `formatUsd` from [lib/products/quote-estimate.ts](lib/products/quote-estimate.ts) - never a second formula; tests in quote-totals.test.ts), rendered read-only in Studio by `QuoteTotalsInput` and later consumed by the customer page/PDF/emails. `sentAt` (read-only, set by the future send flow) + a read-only `QuoteResponsesInput` listing the quote's responses newest first. NO editable status field: Patrick's side derives from `sentAt` + the latest response. **Freshness:** `QUOTES_TAG` + `quoteTag(token)` ([lib/sanity/cache-tags.ts](lib/sanity/cache-tags.ts)); read helper `getQuoteByToken` ([lib/sanity/queries/quotes.ts](lib/sanity/queries/quotes.ts), non-CDN `cachedClient`, tags + `revalidate:false`, returns null for unknown/malformed tokens, never throws; consumed by the Q-140 customer page); the webhook `quote` case busts both tags + `/quote/<token>`. **⚠️ `quote` must be manually ADDED to the Sanity webhook Filter on staging + production** (new type; Projection unchanged - the token IS the slug, already projected; [docs/sanity-webhook-setup.md](docs/sanity-webhook-setup.md)). `quote` is a reserved slug (lib + page/landingPage schema mirrors). Never in the sitemap. Do NOT duplicate a quote doc in Studio (it copies the number + token; uniqueness validation blocks publishing the copy). **Builder (Q-130, 2026-07-31)** - the Studio side of a quote, so a line is picked rather than typed. Each line type gets an object input from [sanity/components/QuoteLineInputs.tsx](sanity/components/QuoteLineInputs.tsx), and every rule they follow lives in the pure client-safe [lib/quotes/quote-prefill.ts](lib/quotes/quote-prefill.ts) (vitest-covered): (1) **Geiger line** - picking a SKU auto-fills `displayName`/`imageUrl`/`description` (truncated at `QUOTE_DESCRIPTION_MAX_CHARS` = 300, markup stripped, cut on a word boundary) from the EXTENDED [app/api/products/resolve/route.ts](app/api/products/resolve/route.ts) (Q-130 added `description`/`lowPrice`/`highPrice`/`minQty` to the existing name/brand/imageUrl payload - additive, so its original `SkuPreview` consumer is untouched; still public read-only catalog data, and the route exists precisely because `products.json` must never enter the Studio bundle). The price range + minimum render as REFERENCE FIGURES beside the cost field and are NEVER written into it (Geiger publishes a range, never a cost - Q-000), so `unitCost`/`setupCharge` stay Patrick's entry. (2) **Own-product line** - the explicit "Pull details from this product" button copies title/image/plain-texted description plus the `unitCost` for the line's CURRENT quantity and the `setupCharge` for the chosen decoration, computed through `estimateForQuantity` + `effectiveSetupCharge` + `productPageValidTiers`/`productPageDecorations` (unitCost = tier price + that decoration's per-unit upcharge, matching the live configurator); the panel auto-LOADS the product read-only to show tiers/minimum and offer the product's decoration methods as buttons, prefers the PUBLISHED doc (draft-only is flagged), and warns in plain English on no tiers / no quantity / quantity below the minimum / a decoration the product does not define. Two new optional snapshot fields `imageUrl` + `description` were added to `quoteOwnProductLine` to receive them (blank = fall back to the referenced product). **Because Studio code cannot import the `server-only` [lib/sanity/queries/product-pages.ts](lib/sanity/queries/product-pages.ts), its four pure pricing rules were MOVED DOWN into [lib/products/product-page-pricing.ts](lib/products/product-page-pricing.ts) and re-exported under the same names** - one definition of "which tiers count", zero call-site changes; do not re-implement them. **Snapshot rule restated: every pre-filled value is COPIED into the quote's own fields at that moment and frozen; no surface ever re-reads a price from the product at render time** (Q-111 proved the freeze). **Never overwrites Patrick's entry:** automatic fills touch blank fields only and re-check blankness at the moment the reply lands (a slow reply cannot clobber something typed in flight, and a SKU auto-fills once); the two deliberate buttons ("Refresh name, image and description", "Pull details") are the only paths that replace a value and both require an inline confirm naming the fields, never touching quantity/shipping/note. Also: per-line totals in the expanded line AND in the collapsed row subtitle (via the shared `quoteLineTotal`, e.g. `Geiger #501003 - 250 x $3.20 = $890.00`); the quote list previews `<number> - <company>` + sent state + grand total (computed in `prepare` from the selected `lineItems`, since totals are never stored) with a Newest-first default ordering; a desk **Quotes** group holds "All quotes (newest first)" + "Customer responses" ([sanity/desk-structure.ts](sanity/desk-structure.ts)); the **private link token is display-only with Copy customer link / Copy token buttons** (`QuoteTokenInput`, no form control at all on top of the existing `readOnly` - editing a token would kill a link already in a customer's inbox; URL built by the pure [lib/quotes/quote-link.ts](lib/quotes/quote-link.ts), which force-normalizes the scheme to https rather than trusting `NEXT_PUBLIC_SITE_URL`, the `gatedCatalogUrl` lesson); and publish validation now also requires at least one line item, with every message rewritten for a non-technical reader (all `Rule.required()`, so a half-built DRAFT still saves - only publishing is blocked). No new document type, route, cache tag, env var, or webhook change.

**Customer page (Q-140, 2026-08-01)** - the read-only page the customer opens, at [app/quote/[token]/page.tsx](app/quote/[token]/page.tsx) (route config + robots/referrer/sitemap handling documented in the Section 4 `/quote/[token]` entry). Rendered by the server-only [components/quote/QuoteDocument.tsx](components/quote/QuoteDocument.tsx): header (logo, quote number, quote date, expiry), the rep block with tappable `mailto:`/`tel:`, the customer block, the line items, the totals, a "how to reply" block, and a footer stating this is a quotation and not an invoice. **Every amount comes from the shared `computeQuoteTotals`** over the STORED line fields - no live price lookup, no stored total, so the Studio totals box, the quote list preview and this page cannot disagree. **Rendered vs withheld** (decided field by field, listed in the component's header comment): rendered are quoteNumber / quoteDate / expiryDate / customerCompany / customerName / rep name+email+phone / per line the display name, image, decoration, description, note, quantity, unit cost, setup, shipping, line total / subtotal, shipping, salesTax, grandTotal. **Withheld: `title` (the schema says it is never shown), `sentAt`, the Geiger line `sku` (a supplier item number invites price shopping the quote against the catalog - Patrick confirmed in Q-200 that it stays hidden), and any link to `/products/<slug>` (the product page shows LIVE pricing, which would contradict the frozen quote price).** **The customer's own `customerEmail`/`customerPhone`/`customerAddress` were withheld until Q-200 (2026-08-07) and are now SHOWN, on the page and in the PDF, as a "prepared for" address block: Patrick reviewed the Q-140 reasoning (a forwarded quote carries less of the recipient's information) and decided he wants the full block, the way a business quotation normally carries one. It is the ONLY field that moved from withheld to shown. Email and phone are tappable (`mailto:` / `tel:`), matching the rep block. Only the email is REQUIRED on a quote, so phone and address are blank on most quotes and each renders as nothing at all - never an empty labelled row. The address is split once, by the pure `quoteAddressLines` in [lib/quotes/quote-display.ts](lib/quotes/quote-display.ts) (blank lines dropped so a pasted address opens no gap, capped at `QUOTE_ADDRESS_MAX_LINES` = 8 so one pathological value cannot push the line-item table around), imported by BOTH the page and the PDF model so the two surfaces structurally cannot disagree.** Display rules live in the pure client-safe [lib/quotes/quote-display.ts](lib/quotes/quote-display.ts) (31 vitest cases): `formatQuoteDate` splits the `YYYY-MM-DD` string instead of `new Date(...)` (parsing gives UTC midnight, which formats as the PREVIOUS day in a negative-offset timezone - a quote that expires a day early is a real customer-facing error, and it is why `formatDate` from lib/utils.ts is NOT used here), `isQuoteExpired` compares date-only strings and treats the expiry day itself as still valid, `quoteLineTitle` falls back displayName → label → referenced product title → a neutral word so a line never renders nameless, and `shownAmount`/`shownQuantity` drop zero and blank so a charge line shows no setup/shipping rows at all (an empty column reads as missing information). **Half-filled quotes never throw:** every field is treated as possibly absent, a quote with zero line items renders an empty state instead of a totals block, and the verification run proves the word `undefined` reaches no rendered markup. **Expired quotes still render** with a polite notice and their prices intact (a link that vanishes looks broken); **`sentAt` is never gated on**, so Patrick can preview his own quote before sending. Long descriptions fold behind a native `<details>` "Read more" ([components/quote/QuoteLineDescription.tsx](components/quote/QuoteLineDescription.tsx), the FaqAccordion idiom - **no client JS, so the full text is in the static HTML**), with the full text also emitted in a print-only paragraph because a closed `<details>` does not print and `::details-content` is too new to rely on. **Print stylesheet** ([components/quote/QuotePrintStyles.tsx](components/quote/QuotePrintStyles.tsx), rendered ONLY by this page so its `body > header` / `body > footer` rules cannot touch another route) hides the site chrome and keeps a line item whole across a page break - the customer's way to save or forward the quote until the PDF ships. No new document type (so **no webhook Filter change** - the `quote` case and Filter entry were wired in Q-110), no new cache tag, no new env var, no new dependency, no route write to Sanity.

**Customer actions + notifications (Q-150, 2026-08-01)** - what closes the loop. Q-140's reply-by-email block is replaced by the client island [components/quote/QuoteActions.tsx](components/quote/QuoteActions.tsx): **Accept this quote** (optional comment + optional artwork), **Request a change** (comment REQUIRED - an unexplained revision request is useless to Patrick), and a third control that Q-150 shipped as **Print or save as PDF** (honestly labelled - it called `window.print()` over the Q-140 print stylesheet) and **Q-160 turned into the real `Download PDF` without moving it**; the print stylesheet stays as the failure fallback. **Staticness is the load-bearing constraint here:** every value the island renders (token, expired flag, rep contact, the last action) is a PROP computed in `QuoteDocument` and baked into the HTML, and the island **never calls `useSearchParams`** (Section 13). Expired quotes show the buttons DISABLED with the reason rather than hidden; the page is **never locked** after a response, because request-change → revise → accept is a real sequence, and the latest action is shown from the static HTML so a returning visitor is not left wondering.

- **Write route** [app/api/quote-response/route.ts](app/api/quote-response/route.ts) (`nodejs` + `force-dynamic`): three kinds (`viewed` / `accepted` / `revisionRequested`), and it **NEVER patches the quote** - every response is a new append-only `quoteResponse` (the Q-110 rule). The browser sends the TOKEN and nothing else that matters: quote id, recipient, customer, totals and expiry are all re-derived server-side. **Unknown / malformed / empty / tag-hostile tokens all get one identical 404 body**, so the route cannot be used to probe whether a token exists. **Expiry is decided at SUBMIT time** (409, nothing recorded), because a page opened last night and submitted this morning is the realistic case. **The record is written BEFORE the email and the email is non-fatal** - a mail failure logs loudly and the customer still succeeds. Comment cap `QUOTE_COMMENT_MAX_CHARS` = 2000, REJECTED over-length rather than silently truncated.
- **Shared rules** in the pure client-safe [lib/quotes/quote-response.ts](lib/quotes/quote-response.ts) (22 vitest cases): the required-comment rule, `latestCustomerAction` (views excluded), and the view debounce - `VIEW_RECORD_WINDOW_MS` = 30 min (a repeat open writes NOTHING) and `VIEW_NOTIFY_WINDOW_MS` = 6 h (Patrick is emailed at most that often per quote). **The debounce state is PERSISTED IN SANITY, never in per-instance memory** (instances recycle, which would defeat exactly the case it guards). **A quote with a blank `sentAt` records but never notifies** - the only person who can be opening an unsent quote is Patrick previewing his own work. `sentAt` is filled by the Q-155 Mark-as-sent control (below); before Q-155 nothing filled it, which left the whole open-notification feature permanently inert. **Views are deliberately NOT CAPTCHA'd** (no interaction to challenge) and have their own bucket.
- **Rate limiting (revised Q-155)** in [app/api/quote-response/route.ts](app/api/quote-response/route.ts). The first version had ONE action bucket of 5/hr/IP checked BEFORE validation, so every rejected attempt (oversized file, empty comment, expired quote) cost the customer the same as a completed one - and the Q-150 verification run was refused a legitimate acceptance because of it. **Attempts and successes are now counted separately:** `attemptLimiter` 30/hr **per IP**, checked early, purely a flood guard (being wrong is not abuse); `submitLimiter` 10/hr **per IP AND token**, checked immediately before the write, so it is only ever spent by a submission that already passed expiry, comment, file and CAPTCHA validation (and before attachment upload, so a refusal leaves no orphaned assets). `viewLimiter` 20/hr per IP and token. **The submit and view keys include the token on purpose:** keying on IP alone makes one office behind a NAT share a budget across unrelated quotes, so one busy customer silently refuses another; a token is unguessable so it cannot be used to widen the limit. The 429 body always names a way out and the page adds the rep's email under it. Store is still per serverless instance (spam damping, not a security boundary - the durable protections are the 128-bit token, the honeypot, Turnstile and server-side validation).
- **Freshness:** the route busts `QUOTES_TAG` + `quoteTag(token)` and `revalidatePath('/quote/<token>')` **itself**, in the same request. That is precisely why `quoteResponse` stays OUT of the webhook Filter. The customer's accept/change records ride the EXISTING `getQuoteByToken` projection as a sub-query, so the page still makes ONE tagged fetch and one bust refreshes both.
- **Three notification emails** (pure builders in [lib/leads/quote-lead.ts](lib/leads/quote-lead.ts), sent through the existing generic `sendBuiltEmail`): opened, accepted (artwork attached when it fits Gmail's ceiling, else "it is in Studio"), change requested (the comment leads). Subjects lead with the quote number and the verb (`Quote Q-1007 ACCEPTED - Acme Corp`) so they read from a locked phone. Destination is the quote's own `repEmail`, else the site default - **never a browser-supplied address**. **Honesty rule: the opened email says the LINK WAS OPENED**, never that the customer read it, because mail scanners and link previews open links too; Patrick opening his own link after sending is indistinguishable, and the guide says so.
- **Mark as sent (Q-155, 2026-08-01)** - `QuoteSentControl` inside `QuoteTokenInput` ([sanity/components/QuoteInputs.tsx](sanity/components/QuoteInputs.tsx)), rendered **directly under the Copy customer link buttons** because copying the link and marking it sent are the same moment in Patrick's workflow; beside the read-only `sentAt` field in a collapsed fieldset he would hunt for it after every quote. **It is NOT a Send button and must never become one:** it emails nobody, composes nothing, and sets one timestamp. The word "Send" is deliberately absent from the control ("I have emailed this to the customer" / "Mark as not sent") because a button saying Send that does not send would lie about what the site does. **It patches BOTH the draft and the published document in one transaction** - patching only the draft would leave alerts off until the next publish (the button would appear to do nothing, since the customer route reads the PUBLISHED quote), and patching only published would let a later publish from an already-open draft silently un-send the quote, which is the module's core clobber problem. Undo is a confirm dialog that clears `sentAt`; existing `quoteResponse` records are never touched. **Disabled while the quote is unpublished** (a draft's link 404s, so there is nothing to have sent), re-checked at click time rather than trusted from mount so a publish in another tab cannot leave it stale. The quote list preview already selected `sentAt`, so it reflects the change with no extra query (`Sent <date>` / `NOT SENT`). No new field, no new document type, **no webhook change**, no route change.
- **Studio:** the Responses box ([sanity/components/QuoteInputs.tsx](sanity/components/QuoteInputs.tsx)) is real - kind, time, comment, and artwork as download links. Two desk panes, **Accepted by customer** and **Change requested** ([sanity/desk-structure.ts](sanity/desk-structure.ts)), each ONE filtered query run only when opened. Deliberately NOT a badge on every row in All quotes: response state lives in other documents and a list preview cannot join across documents, so inline state would cost a query per row.
- **Customer-initiated DRAFT quote (part 6):** a Get a Quote submission on `/products/<slug>` also starts a draft quote. Rules in the pure [lib/quotes/quote-draft.ts](lib/quotes/quote-draft.ts) (22 vitest cases), orchestrated by [lib/leads/quote-draft-creator.ts](lib/leads/quote-draft-creator.ts). **DRAFT, never published** (`drafts.<uuid>`) - an unreviewed quote must never be reachable at a live link, and the customer page reads the published perspective so its token 404s until Patrick publishes. **Prices are recomputed from the PRODUCT** via `buildOwnProductPrefill` + `getProductQuoteSource` (the form posts a formatted display string and an annotated decoration label - `stripDecorationAnnotation` recovers the method name, no form change). **No quote number is allocated** (an abandoned enquiry must not hole the sequence). **The lead can never be lost to it:** the creator cannot throw, runs before the lead email only so that email can say a draft is waiting, and every failure returns `created: false` and logs.
**The PDF, the status banner and the lifecycle (Q-160, 2026-08-02)** - the last build task of the module itself.

- **The document.** Rendered by **`@react-pdf/renderer` 4.5.1, pinned exactly** (no caret) - the version the Q-120 spike validated and the Q-121 addendum measured on a real Vercel deployment. It is the ONLY new dependency: 45 packages, ~22.6 MB installed, no native modules and no wasm, and it enters no bundle other than the PDF route (only [lib/quotes/pdf/QuotePdfDocument.tsx](lib/quotes/pdf/QuotePdfDocument.tsx) and [lib/quotes/pdf/render-quote-pdf.tsx](lib/quotes/pdf/render-quote-pdf.tsx) import it, and only the route imports those). **Typography is the built-in Helvetica - no font file is committed** (the repo has no binary font asset, every existing HTML email is on the same stack, and this was the spike's explicit recommendation). **The LOGO is the real one, not a typeface** (fixed 2026-08-02 after Patrick noticed Ctrl+P showed the logo and the PDF showed a wordmark): the renderer decodes JPEG and PNG only and cannot load an SVG file, so `public/logo.svg` is rasterised ONCE at 600x225 by [scripts/quick-quote/generate-pdf-logo.mjs](scripts/quick-quote/generate-pdf-logo.mjs) (Playwright, the `generate-og-default.mjs` precedent) into the **generated, inlined** [lib/quotes/pdf/quote-pdf-logo.ts](lib/quotes/pdf/quote-pdf-logo.ts) and printed 150 pt wide, near 290 dpi. **Inlined rather than read from `public/` at request time**, because public assets are served by the static layer and are not guaranteed to be on a serverless function's filesystem - a customer's quote silently losing its logo is exactly the failure worth spending 48 KB of generated source to remove. The text wordmark survives only as a fallback for a corrupted regeneration. **Re-run that script if the logo changes**; the verification script asserts the module decodes to real PNG bytes and that the document uses it.
- **It is deliberately SIMPLER than the web page**, a fixed six-column business table rather than the responsive card list: two layouts that chase each other pixel for pixel drift apart the moment either is touched. What they may never disagree on is the money, so **every amount comes from the shared `computeQuoteTotals`** over the stored line fields, exactly like the page, the Studio totals box and the quote list preview. The pure [lib/quotes/quote-pdf-model.ts](lib/quotes/quote-pdf-model.ts) (27 vitest cases) decides everything else - line naming, which amounts are worth a column, expiry, the filename, and which image URL is safe - so the renderer only lays out.
- **Rendered vs withheld: the SAME list the page uses** (Section 7 quote "Customer page"). Rendered: quote number, quote date, expiry, the FULL customer block (company, contact name, email, phone, address - Q-200, one `<Text>` per address line inside the `flexBasis: 0` party box so a long address wraps in its own column instead of pushing the other box sideways), rep name/email/phone, and per line the name, picture, decoration, description, note, quantity, unit price, setup, shipping and line total, then subtotal / shipping / sales tax / grand total and the not-an-invoice footer. **Withheld: the internal `title`, `sentAt`, a Geiger line's supplier `sku`, and any `/products/<slug>` link.** Nothing internal is even modelled, so the renderer cannot print it by accident (asserted structurally by the verification script). A charge line's setup and shipping cells stay BLANK rather than printing `$0.00`, and a column NO line uses is dropped from the table entirely.
- **Images, and why they are fetched by us** ([lib/quotes/pdf/quote-pdf-images.ts](lib/quotes/pdf/quote-pdf-images.ts)). The renderer decodes **JPEG and PNG only**, and every Geiger catalog `imageUrl` carries `format=webp` - it has only ever worked because the CDN content-negotiates, which is the CDN's choice and not a guarantee. So: (1) `normalizeQuoteImageUrl` **strips any format parameter the renderer cannot decode** and rejects anything that is not http(s); (2) images are fetched HERE, before layout, **in parallel**, each with an explicit **3.5 s `AbortSignal.timeout`** and a 3 MB cap, at most 14 per document - the Q-121 addendum measured a dead URL costing ~1,700 ms while the renderer discovered it mid-layout, about five times a whole normal render, and the renderer exposes no per-image timeout; (3) the returned bytes are **verified as JPEG or PNG by magic number**, so a host that really does send webp is dropped rather than handed to the renderer. Sanity-hosted line images ask the image builder for `format('jpg')` explicitly. **A failed image resolves to null, renders a clean empty framed box, and never fails the download** - and is logged loudly, because it is silent to the customer and a systematically broken host would otherwise ship unnoticed (the spike's "failures are quiet" warning).
- **Layout rules that are load-bearing:** `flexBasis: 0` on every flexible cell (without it ONE long product name sets the item column's width and silently pushes the money columns off the right edge - the spike's exact bug, and it never shows up against short fixture names); the table header is `fixed` so it repeats on every page; rows are `wrap={false}` so a line item never splits across a break; `Page N of M` in a `fixed` footer. The model hard-caps a line description at 600 characters and a note at 400 so a non-wrapping row can never outgrow a page and be clipped. If anything else is ever marked `fixed`, re-check all three together.
- **The button** ([components/quote/QuoteActions.tsx](components/quote/QuoteActions.tsx)) stayed exactly where Q-150 put it, third of the three controls, relabelled **Download PDF**. It fetches the route and triggers a blob download rather than navigating to the URL, so a failure becomes ONE plain sentence plus a link to the browser's print option instead of a raw error page on the customer's own quote. It shows "Preparing your PDF..." while it works and is **never disabled by expiry**. **The Q-140 print stylesheet is deliberately KEPT** - it costs nothing, Ctrl+P uses it anyway, and it is the honest answer when the PDF route is briefly unavailable.
- **The status banner** now leads that block with real visual weight (thick left rule, filled panel, a heading a size larger than anything under it) plus a line saying they can still do something else. It was a quiet note above a large green Accept button, which in testing read as though the acceptance had not registered. **It shows state and changes nothing else: no button is disabled, hidden or reordered because of what the customer already did.** Accept → request a change → accept again after a revision is a real supported sequence, and any per-state button rule would create edge cases that get got wrong. The only gate is EXPIRY, which is about the quote and not about their history.
- **Lifecycle: no new control was built, because three existing mechanisms already cover it** (documented for Patrick in the guide). Set the expiry date to today to stop responses while the page stays readable and downloadable; unpublish to take the link offline entirely (it then 404s for the customer too); delete to remove it permanently - and `quoteResponse` records survive that, because they are separate documents carrying the quote number. A published link stays live after acceptance on purpose, and accepting is explicitly not placing an order.
- **Staticness + cost:** the route is `force-dynamic` with no render path, and the action island still takes every value as a PROP and never calls `useSearchParams`, so `/quote/[token]` remains statically generated (Section 13). **No new Sanity document type, so NO webhook Filter change** (the `quote` branch and its Filter entry were wired in Q-110); no new cache tag, no new env var, no route write to Sanity, no email added or changed. Verified by [scripts/quick-quote/verify-q160.ts](scripts/quick-quote/verify-q160.ts) (dry run = offline checks + a real local render of a deliberately awkward fixture, with a hand-written zlib PDF text extractor rather than a second dependency; `--apply` drives the deployed route and refuses to write a fixture if the route is not live there yet) → [docs/quick-quote/Q-160-verification-report.md](docs/quick-quote/Q-160-verification-report.md).

- **Reused, not forked:** the leads route's rate limiter, IP reader, Turnstile fail-open/fail-closed policy, write client, and read-bytes-once attachment handling were EXTRACTED to [lib/api/rate-limit.ts](lib/api/rate-limit.ts), [lib/api/turnstile.ts](lib/api/turnstile.ts), [lib/sanity/write-client.ts](lib/sanity/write-client.ts), [lib/leads/attachments.ts](lib/leads/attachments.ts) and imported by both routes. Pure code motion: same constants, same log strings (`verifyTurnstile` takes a prefix so `[leads] ...` is byte-identical). The leads route's own `validateFiles` was left untouched so no existing form's error wording changes. **No webhook Filter change, no new cache tag, no new env var, no new dependency.**

**quoteResponse.** Append-only record of a customer's action on a quote (Q-110): `kind` (`viewed` / `accepted` / `revisionRequested`), weak `quote` reference (weak so deleting a quote is never blocked) + redundant `quoteNumber`, `createdAt`, optional `comment` + `files[]` (acceptance artwork) + coarse `context` (no precise personal/device data). Modelled on `leadSubmission`: `readOnly: true` at the document level AND on every field - customer actions are NEW documents, never edits to the quote, because a Studio publish replaces the published doc wholesale and would silently erase state stored there ([sanity/schemas/documents/quote-response.ts](sanity/schemas/documents/quote-response.ts)). **Written by exactly ONE place: [app/api/quote-response/route.ts](app/api/quote-response/route.ts) (Q-150), which revalidates the affected quote's tags itself in the same request.** **Deliberately NOT in the Sanity webhook Filter** for that reason - webhook deliveries would be redundant plus one more forgettable manual Filter step (the faq/brand silent-failure class); the reasoning is also in the webhook route comment. A quote can hold MANY responses and is never locked after the first: request-change → Patrick revises → accept later is a supported sequence, and the customer page shows the latest action.

Bulk AI-generated category pages are NOT in Sanity. They live as JSON in `/data/categories/`. When a curatedCategory or customCategory exists in Sanity with the same slug, Sanity wins.

## 8. Geiger Integration

**Data source:** Geiger uses Searchspring as its product search and category engine. Site ID is `kfx28d`. API endpoint:

```
https://kfx28d.a.searchspring.io/api/search/category.json
```

Key parameters:

- `siteId=kfx28d` (constant)
- `bgfilter.category_path=Home > Drinkware > Water Bottles` (category path with spaces and `>` separator)
- `resultsFormat=native`
- `page=N` (1-indexed)
- `perPage=60` (Geiger's native page size; what we use to match Geiger pagination exactly)
- `filter.[field]=[value]` (optional, for facet filtering)

Response includes full product objects (sku, name, brand, low_price, high_price, msrp, min_qty, imageUrl, description, category_path[], badges, is_new_item, is_on_sale, product_type_unigram), aggregated facets array with counts, pagination metadata, and sort options.

**Actual catalog size:** ~7,957 unique SKUs (verified 2026-05-24 via no-filter Searchspring query returning 7,971 total; our Phase B captured 99.82% of that). Note that summing per-category counts on Geiger's `/b/` pages produces inflated totals (around 13,500) because the average Geiger product appears in approximately 3.3 category paths (cross-listed across Apparel, Shop By > Brand Names, Shop By > Collections, etc). The 7,957 figure is the true unique-SKU count, not the sum of category page totals.

**HTML entity handling.** Geiger's product names and descriptions contain HTML entities directly in the data (e.g. `4' Dynamo Trifecta Display &amp; Graphics`, `6&quot; Key Card Holder`). These must be decoded centrally at the data loader level (`lib/categories.ts` → `getProductsForCategorySlug`) so every consumer sees clean plain text. Do NOT decode in components individually; decode once at the source. Entities to handle at minimum: `&amp;`, `&quot;`, `&#039;`, `&apos;`, `&lt;`, `&gt;`, `&nbsp;`, `&reg;`, `&trade;`, `&copy;`. **Product `imageUrl`s are decoded too** — Geiger's image URLs in `products.json` carry literal `&amp;` (e.g. `?format=webp&amp;w=275`); a browser decodes that fine inside `<img src>`, but it leaks `&amp;` into `og:image` / `twitter:image` / ItemList `image` and breaks social previews. So `imageUrl` is decoded at the loader (`loadProductsIndex` in `lib/categories.ts`, plus the sibling product indexes in `lib/products/lookup.ts` and `lib/brands.ts`) so EVERY consumer — grid, OG/Twitter image, schema `image` — gets a clean `&` URL (M-SEO3 follow-up).

**Image fallback.** Hot-linked Geiger images may 404 if Geiger removes a product mid-month between auto-rebuilds. ProductCard must include an `onError` handler that swaps the broken image for a clean placeholder (a simple inline SVG or `/public/placeholder-product.svg` with the product name in plain text). The monthly auto-rebuild drops removed products entirely, so the placeholder is only a between-rebuild safety net.

**Per-product attributes (color, material, size) are NOT on the product object.** They only appear in the aggregated facets array. To know which products belong to a facet URL like `/cat/water-bottles/material/stainless-steel`, the pipeline makes a filtered API call per facet URL (Phase C of the scraper). This is the 21,715 calls described in Section 16.

**Affiliate URL rewrite rule:** Replace `https://www.geiger.com/` with `https://patrickblack.geiger.com/` in any scraped Geiger URL before emitting it on the new site. Works for both `/p/` product URLs and `/b/` category URLs.

Source URL: `https://www.geiger.com/p/vinyl-football-510336?pid=208667`
Output URL: `https://patrickblack.geiger.com/p/vinyl-football-510336?pid=208667`

Helper function lives at `lib/affiliate-url.ts` and is the only place this transformation should happen. Never hardcode the transformation in components.

**Product images:** Hot-linked from Geiger's CDN at `imgsirv.geiger.com`. Do NOT download to our origin. Patrick is an authorized Geiger distributor and hot-linking is permitted. Use explicit width and height on every image to prevent CLS. Use `loading="lazy"` on images below the fold.

**Brand logos.** Scraped from Geiger's brand pages (linked from `https://www.geiger.com/c/shop-by-brand`) via Phase E of the scraper. Stored at `data/geiger/brand-logos/{slug}.{webp|png|jpg}`. Used on the brands index page and per-brand pages. Auto-refreshed during monthly rebuild.

**Deals + new-products data.** Geiger's deals (sale + closeout) and the "New Products" feed both turn over fast, so they are NOT included in the monthly Phase B catalog dump. Instead, **weekly** scrapers (`scripts/scrapers/geiger/scrape_deals.py` for Phase F, `scrape_new_products.py` for Phase G — see Section 16) hit Searchspring with the relevant `bgfilter.category_path`, capture products + per-facet-value SKU memberships, and write `data/geiger/deals.json` / `data/geiger/new-products.json` respectively. Both `/deals` and `/new-products` read from those files at build time.

On top of the scraped baseline, Sanity adds three editorial levers per page (via `globalSettings.dealsPage` and `globalSettings.newProductsPage`):

1. **Hide:** `hiddenDealSkus[]` / `hiddenNewProductSkus[]` blocklists — drop a scraped SKU from the grid. Facet counts auto re-derive.
2. **Pin:** `pinnedDealSkus[]` / `pinnedNewProductSkus[]` allowlists — promote any existing Geiger SKU into the page even if the weekly scrape did not include it. Lookup runs against `data/geiger/products.json`.
3. **Add:** `customProduct` docs with `placements.onDeals == true` / `placements.onNewProducts == true` — fully manual non-Geiger products. See Section 7 customProduct.

Hero copy (heading, intro, meta) is also Sanity-controlled. Augmentation pipeline: [lib/products/augment.ts](lib/products/augment.ts) (pure merger) + [lib/products/lookup.ts](lib/products/lookup.ts) (SKU index over products.json) + [lib/deals.ts](lib/deals.ts) / [lib/new-products.ts](lib/new-products.ts) (orchestrators). See M5-510 (deals scrape) and M5-511 (custom + pinned additions) in TASKS.md.

**Category mapping:** Each PI root category maps to one Geiger category via `data/mappings/pi-to-geiger.json`. Categories with no good match link to the closest Geiger top-level category. If absolutely nothing matches, fall back to `https://patrickblack.geiger.com/`.

**Affiliate subdomain status:** Until Geiger activates `patrickblack.geiger.com`, the affiliate host is configurable via `NEXT_PUBLIC_GEIGER_HOST`. Default value is `https://patrickblack.geiger.com`. If Geiger has not activated the subdomain by launch, set this to `https://www.geiger.com` temporarily. Patrick confirmed (2026-05-25) that Geiger has not activated the subdomain yet; this is being chased on Geiger's side.

## 9. AI Content Pipeline

Source of truth for what each category page needs: the entry in `data/mappings/pi-to-geiger.json` plus the corresponding Geiger category data in `data/geiger/`.

**Three-tier generation** based on URL type from `data/pi-urls/category-urls.json`:

### Root category pages (465 total, `type=root`)

Format: **Buying guide** style (upgraded 2026-05-26 per Patrick feedback on the Week 2 demo).

Generated fields:

- SEO H1 (40-70 chars)
- Meta title (under 60 characters)
- Meta description (under 155 characters)
- **Hero intro (1-2 paragraphs, 150-250 words)** rendered at the top of the page above the product grid. Sets context and persona.
- **Bottom buying guide content (400-600 words)** rendered under an H2 titled `Custom [Category Name] Buying Guide`. Structured as a buyer-research piece covering:
  - What buyers should look for when ordering this category
  - Materials, build quality, durability considerations
  - Common use cases and which buyers each fits (corporate events, trade shows, employee gifts, safety programs, etc.)
  - Decoration and customization options (screen print, embroidery, laser engraving, full-color, debossing, etc.)
  - Quantity guidance and MOQ context
  - Tips to avoid common buying mistakes
- **Five FAQs** with answers (50-100 words each), specific to the category
- Hero image alt text (60-120 chars, includes keyword)

**Keyword derivative injection.** The bottom buying guide must naturally include multiple plural keyword variations across its 400-600 words:

- `custom [category]`
- `promotional [category]`
- `branded [category]`
- `personalized [category]`
- `logo [category]` or `logo-printed [category]`
- `bulk [category]`
- `wholesale [category]`

Goal is natural, non-stuffed integration. Patrick's buying guide reference at `https://www.perfectimprints.com/blog/buying-guide-for-stadium-seat-cushions` shows the target tone and structure.

### Modifier pages (576 total, `type=modifier`, `/cat/[root]/[modifier]`)

- SEO H1 incorporating both the root category and the modifier (e.g. "Closeout Water Bottles", "Eco-Friendly Tote Bags", "Custom Pens With No Minimum")
- Meta title and meta description tuned to the modifier intent (sale/closeout = price-sensitive, no-minimum = small-order buyers, eco-friendly = sustainability-conscious, etc.)
- One short intro paragraph (60-80 words) explaining what makes this subset different (lower MOQ, sale pricing, eco materials, faster production)
- No FAQs, no buying guide section (those live on root pages)

### Facet pages (21,137 standard + 2 compound, `type=facet|compound-facet`)

- SEO H1 optimized for the long-tail keyword
- Meta title (under 60 characters)
- Meta description (under 155 characters)
- One short intro paragraph (60-80 words)
- No FAQs, no buying guide section

For compound facet pages, the H1 incorporates both filter dimensions.

### Prompt templates

Located at `scripts/ai-pipeline/prompts/`:

- `root_category.txt` — buying-guide format root template (promptVersion `root-v2`)
- `modifier_category.txt` — modifier lite template
- `facet_category.txt` — facet lite template (also used for compound facets)

All templates inject:

- Top product names from Geiger (slug-token relevance filtered to stay on-topic)
- Target keywords (always plural form)
- Buyer personas (marketing directors, HR directors, safety managers, business owners)

Root prompt varies hero intro opening structure to avoid sameness across 465 pages:

- 30 percent open with use cases
- 30 percent open with target industry or buyer
- 30 percent open with material or quality angle
- 10 percent open with seasonal or trending angle

### Phased generation

- **Phase 2.1 (Week 2 end, COMPLETE):** Top 35 root categories generated as a demo sample. Patrick approved content tone on 2026-05-25 with the buying-guide upgrade as a follow-up.
- **Phase 2.1b (2026-06-01, COMPLETE):** v1 demo quality pass — `scripts/ai-pipeline/generate_sample_roots.py` rebuilt with: dedup-by-Geiger-path selection so 35 entries are 35 distinct categories; `EXCLUDED_SLUGS` list (11 entries) for incoherent fuzzy mappings and PI admin artifacts; depth-aware SKU filter (see below); compound-noun H1 rule in `prompts/root_category.txt`; `post_process_lengths()` safety net that truncates metaTitle/metaDescription at a word boundary when the model overshoots SEO caps. Cumulative cost across all rounds: $0.065 for 60 calls. Zero length violations across all 35 outputs.
- **Phase 2.2 (COMPLETE):** Upgraded `root_category.txt` to buying-guide format (`promptVersion: "root-v2"`) and regenerated the 35 demo pages. Patrick spot-checks pending Pause Point 3 sign-off.
- **Phase 2.3 (COMPLETE):** All 465 PI root pages generated with v2 buying-guide format (`buyingGuideHtml` + `buyingGuideH2` populated). Committed in `91a4b3de`.
- **Phase 2.4 (COMPLETE):** All 21,715 non-root pages (576 modifiers + 21,137 facets + 2 compound facets) generated using their lite templates. Committed in `91a4b3de`.

### SKU filter rules (applied in `apply_sku_filter`)

PI-to-Geiger mappings sometimes point a root slug at a broad parent department (`business-card-holders` → `Home > Office & Technology`, 1738 SKUs), which would drown the product grid in unrelated items. The pipeline applies a three-tier rule at generation time and persists the result in the output JSON:

1. **`full`** — applied when `matchType ∈ {exact, fuzzy}` OR Geiger path depth ≥ 3. The full subtree SKU list is trusted as-is.
2. **`slug-filtered`** — applied when `matchType == override` AND depth < 3. Each candidate SKU is scored by token overlap between the category slug and the product name. Keep all SKUs scoring above the median, capped at 200.
3. **`full-capped-60`** — fallback when `slug-filtered` would leave fewer than 30 SKUs (single-digit grids look broken). Use the raw set capped at 60.

Three fields in the output JSON record the decision: `skuFilterMode`, `rawSkuCount`, `filteredSkuCount`.

### Output schema

Written to `data/categories/[encoded-slug].json`:

```json
{
  "url": "/cat/water-bottles",
  "type": "root|modifier|facet|compound-facet",
  "h1": "...",
  "metaTitle": "...",
  "metaDescription": "...",
  "introHtml": "...",
  "buyingGuideHtml": "...",
  "buyingGuideH2": "Custom Water Bottles Buying Guide",
  "faqs": [{ "q": "...", "a": "..." }],
  "heroAltText": "...",
  "productSkus": ["SKU1", "SKU2", ...],
  "skuFilterMode": "full|slug-filtered|full-capped-60",
  "rawSkuCount": 1738,
  "filteredSkuCount": 56,
  "generatedAt": "ISO timestamp",
  "model": "deepseek-chat",
  "promptVersion": "root-v2",
  "openingStyle": "use_case|buyer|material_quality|seasonal"
}
```

Note: `introHtml` holds the hero intro (above product grid). `buyingGuideHtml` holds the longer bottom content (under the H2 `buyingGuideH2`). Modifier and facet types only populate `introHtml`; the buyingGuide\* fields are null for those.

Never overwrite a Sanity document, only the JSON files. DeepSeek is called build-time only, never at runtime.

**Sanity AI button:** customCategory documents have a "Generate with AI" Studio action that POSTs to `/app/api/sanity/generate-content/route.ts`. The route calls DeepSeek with the root_category prompt and the customCategory's title and target keyword, then returns the generated intro, buying guide, and FAQs. The Studio action patches the document fields with the result. Patrick reviews and publishes.

## 10. Brand Tokens

Defined as Tailwind theme extensions and CSS variables.

| Token                  | Value     | Use                                                    |
| ---------------------- | --------- | ------------------------------------------------------ |
| `--color-brand-red`    | `#E11F1E` | Primary brand color, logo, accents, headings, dividers |
| `--color-brand-ink`    | `#231F20` | Body text, footer background                           |
| `--color-brand-green`  | `#16A34A` | CTA buttons, success states                            |
| `--color-brand-white`  | `#FFFFFF` | Page background                                        |
| `--color-text-primary` | `#1A1A1A` | Body copy                                              |
| `--color-text-muted`   | `#666666` | Captions, metadata                                     |
| `--color-border`       | `#E5E5E5` | Dividers, card borders                                 |
| `--color-bg-soft`      | `#F5F5F5` | Section backgrounds                                    |

Brand red `#E11F1E` and ink `#231F20` extracted from `pi-logo.svg`. Confirm exact green shade with Patrick during style guide review on staging.

Typography: system font stack with Inter as the preferred web font. Headings semibold or bold. Body 16px on mobile, 17px on desktop. Line height 1.6 for body, 1.2 for headings.

Buttons: green primary CTA (`--color-brand-green`), outlined dark secondary, no rounded-full pills, 6px to 8px radius.

## 11. SEO Requirements

Every page must have:

- Unique title and meta description
- Canonical URL
- Open Graph and Twitter card metadata
- Schema markup appropriate to the page type

**Category meta title = the page H1 (M-SEO3).** Category pages set their meta `<title>` to the on-page H1 (the full descriptive phrase) rather than the shorter templated `metaTitle` — Patrick found category titles too short. `categoryMetaTitle(h1)` in [app/cat/[...slug]/page.tsx](app/cat/[...slug]/page.tsx) appends `| Perfect Imprints` only when the result still fits comfortably (≤ ~60 chars); a long H1 stands alone. The canonical and the on-page H1 are untouched. Applies to root, modifier, and facet pages (baked JSON); customCategory pages keep their own Sanity `seo.metaTitle`.

**Open Graph + Twitter, complete on every page (M-SEO3, Twitter completed in Task D).** Next merges metadata shallowly per top-level key, so a page that sets `openGraph` (or `twitter`) REPLACES the layout default entirely. The shared builder `socialMeta()` in [lib/seo/open-graph.ts](lib/seo/open-graph.ts) is spread into each page's metadata so every page emits a full set — `og:title`, `og:description`, `og:image`, `og:url` (= canonical), `og:type` (`website` for home/listings, `article` for blog posts), `og:site_name` (`Perfect Imprints`), `og:image:alt` — **plus a complete Twitter Card: `twitter:card=summary_large_image`, `twitter:title`, `twitter:description`, `twitter:image` (same image as `og:image`), and `twitter:site` + `twitter:creator` = `@perfectimprints`** (`TWITTER_HANDLE`, exported from the same file; also set on the root-layout `twitter` default so it applies to any page not spreading `socialMeta`). The video detail page sets its own inline `twitter` block (own `video.other` OG type) and re-declares `site`/`creator` there because the shallow merge would otherwise drop them. **Category `og:image`/`twitter:image` = the first product image** of the category (resolved off `products.json` in `generateMetadata`, no network/Sanity call), **upsized to a ~1200px Geiger variant via `largeSocialImage()`** (rewrites the `thumbnail`/`w`/`h` size params on `imgsirv.geiger.com` URLs only — the 275px grid thumbnail is too small/square for X's large-image card; the on-page grid still requests 275px, so this only affects the social tags). CTA-only categories (no products) fall back to the branded raster social card `LOGO_OG_IMAGE` = **`/og-default.png`** (1200×630). **It is NOT `logo.svg` — X/Twitter does NOT render SVG card images** (only JPG/PNG/WebP/GIF), so the SVG logo would show no image on X; the PNG is generated by `scripts/seo/generate-og-default.mjs` (Playwright renders the logo on a white card with a brand-red accent bar) and committed to `public/`. Re-run that script only if the logo or card design changes. Blog posts use the hero image (Sanity, 1200px), brand pages the brand logo, video pages the poster (1200px); everything else falls back to `LOGO_OG_IMAGE`. `og:image:alt` always falls back to the title. **X caches cards aggressively:** after deploy a previously-shared URL may still show the old (no-image) card for a while; sharing a fresh URL or appending a one-off dummy query param confirms the new card — expected, not a code issue.

Schema types in use:

- Organization (**home + contact pages ONLY** as of M-SEO3 — this is the local-business block carrying address/phone, so it was removed from the root layout and is no longer emitted on categories/blogs/listings; name/url/logo/telephone + `contactPoint`; **`sameAs` = the URLs of the ENABLED `globalSettings.socialLinks` only, `telephone`/`email`/`PostalAddress` from `globalSettings.contact`**; `sameAs`/`address` are omitted when empty rather than fabricated) — `organizationSchema(settings)` in [lib/seo/schema-generators.ts](lib/seo/schema-generators.ts), fed by `getSiteSettings()` via the async [components/seo/OrganizationJsonLd.tsx](components/seo/OrganizationJsonLd.tsx), rendered by [app/page.tsx](app/page.tsx) + [app/contact/page.tsx](app/contact/page.tsx)
- WebSite + `SearchAction` (sitewide, in root layout — enables the Google sitelinks search box; target `/search?q={search_term_string}`) — `websiteSchema()` in [lib/seo/schema-generators.ts](lib/seo/schema-generators.ts) (M5-508)
- CollectionPage (every category page — root/modifier/facet + customCategory — the page represents a product collection) — `collectionPageSchema()` (M-SEO3)
- ItemList of FULL Product entities (brand pages as of SNIP-100 2026-08-18, ALL category pages as of SNIP-110 2026-08-19): `/brands/<slug>` (+ its `/page/N` variants), every baked `/cat` page (root/modifier/facet + `/page/N`), and every Sanity `customCategory` page emit an ItemList whose ListItems carry a nested `Product` built by the pure shared serializer [lib/seo/product-list-schema.ts](lib/seo/product-list-schema.ts) (`productItemListSchema()`, vitest-covered) over the SAME in-memory products the grid renders: zero new reads, every route stays fully static. Per item: name; url = the affiliate destination (or the internal `detailUrl` when one exists, and NO url at all when neither is real, so the bare affiliate homepage is never claimed - this also fixed the SNIP-000 bug where override-added custom products on live /cat/pens claimed the bare homepage); the ~1200px `largeSocialImage` image; `Brand` only when the record has one (15.6% of the catalog - a fully brandless grid like tote-bags page 1 emits zero brand keys and still validates 0/0); `sku` only when real (synthetic `custom-<id>` ids suppressed, the ProductCard rule); `offers` = `AggregateOffer` (USD lowPrice/highPrice) + `eligibleQuantity` = min qty, omitted entirely when there is no usable price. Deliberately omitted per the SNIP-000 measurements, do not "complete" these: per-product `description` (it alone tripled the block), `availability` (no stock field exists in any scraped data), any single price or `msrp`-as-list-price (`msrp` is a byte-for-byte alias of `high_price` on every catalog record), gtin/mpn/ratings. `offers.url` is not emitted (it would duplicate the Product url byte for byte). Each `/page/N` document describes ONLY the products it renders, positions restarting at 1 (Google's "all the items that are listed on the page" rule; verified on the real /cat/tote-bags/page/2). **CTA-only categories emit NO ItemList at all** (the `!showCTA && products.length > 0` guard in [app/cat/[...slug]/page.tsx](app/cat/[...slug]/page.tsx) and the `products.length > 0` guard in [components/category/CustomCategoryView.tsx](components/category/CustomCategoryView.tsx)) rather than an empty list. Measured cost on /cat (SNIP-110, both copies: script tag + RSC flight): ~+27.5 KB raw (+5.9%) / ~+2.6 KB gzip / ~+1.4 KB brotli on a 60-product page, +1.4 KB raw on a 3-product facet page. The old 3-field `itemListSchema()` (M-SEO3) now has ZERO call sites - it stays in [lib/seo/schema-generators.ts](lib/seo/schema-generators.ts) for the SNIP-000 §E blog-listing piece; do not delete it, and do not point a product surface back at it. The dynamic [/api/category-products](app/api/category-products/route.ts) filter route returns bare JSON and emits no structured data (correct - filtered views are client state, not indexable documents); remaining zero-markup surfaces (Deals, New, Rush, /promotional-products, blog/video strips, home rails, shop-by-theme) are each their own later piece and must reuse this serializer.
- BreadcrumbList (every category, blog, video page) — **emitted ONCE** by the shared [components/layout/Breadcrumbs.tsx](components/layout/Breadcrumbs.tsx) component, with **absolute `item` URLs** (relative hrefs are prefixed with the canonical origin; the current-page leaf has no `item`). Schema.org requires `item`/`@id` to be absolute — relative values tripped Google Rich Results' "Invalid URL in field 'id'". `CustomCategoryView` no longer emits its own `breadcrumbSchema` (that was a duplicate); it relies on the same `<Breadcrumbs>` component. The `breadcrumbSchema()` generator stays for any explicit absolute-URL use.
- BlogPosting (every blog article)
- FAQPage (every root category page with FAQs, every customCategory with FAQs, and the `/faq` library)
- Product (within category page grids, summarized)
- VideoObject (every video page)
- Custom JSON-LD (any page, editor-driven) — Patrick attaches arbitrary JSON-LD blocks (WebApplication, HowTo, Product, Event, …) to any page via a `customSchema` Sanity doc keyed by `pageUrl`, injected by the shared `<CustomSchemaJsonLd>` server component (Section 7 customSchema, Task C). Not a built-in generator — raw blocks validated at publish time.

**Every JSON-LD block goes through ONE escape (FIX-830 task 4, 2026-08-14).** `jsonLdHtml()` in [lib/seo/json-ld.ts](lib/seo/json-ld.ts) is the only thing allowed to turn a schema object into the string inside a `<script type="application/ld+json">`; all **12** emitters import it ([app/blog/[slug]/page.tsx](app/blog/[slug]/page.tsx), [app/faq/page.tsx](app/faq/page.tsx), [app/layout.tsx](app/layout.tsx), [app/products/[slug]/page.tsx](app/products/[slug]/page.tsx), [app/shop-by-theme/[slug]/page.tsx](app/shop-by-theme/[slug]/page.tsx), [app/videos/[slug]/page.tsx](app/videos/[slug]/page.tsx), [components/landing/LandingPageTemplate.tsx](components/landing/LandingPageTemplate.tsx), [components/layout/Breadcrumbs.tsx](components/layout/Breadcrumbs.tsx), [components/page-sections/FaqAccordion.tsx](components/page-sections/FaqAccordion.tsx), [components/seo/CustomSchemaJsonLd.tsx](components/seo/CustomSchemaJsonLd.tsx), [components/seo/OrganizationJsonLd.tsx](components/seo/OrganizationJsonLd.tsx), and [components/seo/Schema.tsx](components/seo/Schema.tsx) - the last of which is what all 22,180 `/cat` pages and every customCategory render their CollectionPage/ItemList/FAQPage through). **Before the fix, four escaped `<` and eight did not**, and the eight interpolate Sanity fields Patrick edits (blog titles, breadcrumb labels, video and FAQ text, footer/contact settings), so a value containing `</script` would have ended the script element early - broken structured data on that page and a markup-injection path, not a style nit. The escape expression is character-for-character the one the four correct call sites already used (replacing every `<` with its `<` JSON escape), so **output is byte-identical for any content with no `<`**, and a parser reads exactly the same string back; verified by rendering `/cat/water-bottles` locally and diffing all three of its JSON-LD blocks against the live page (3 identical, 0 differing). Do not hand-roll a `JSON.stringify` into a `ld+json` script again - import the helper.

**Pagination indexing rule (added 2026-05-26 per Patrick feedback).** For paginated category URLs (`/cat/[slug]/page/N`):

- Page 1 is fully indexable, with canonical pointing to itself (clean root URL like `/cat/water-bottles`, NOT `/cat/water-bottles/page/1`)
- Pages 2 and beyond carry a `noindex,follow` meta robots tag AND a canonical pointing back to page 1
- This prevents duplicate-content penalties while still letting Google discover product variations through follow links

Sitemap is auto-generated at build time ([app/sitemap.ts](app/sitemap.ts)) and covers every indexable route — all 22,180 category page-1 URLs, blog posts + blog categories, per-brand + brands index, per-video + videos index, deals/new-products/rush-products, services, the static/legal pages, `/promotional-products`, `/faq`, and home. ~23k URLs sits under Google's 50k-per-file cap, so Next emits a single spec-valid `sitemap.xml` (switch to `generateSitemaps()` to split into an index only if it ever exceeds 50,000). The `noindex` `/search` route, paginated `/page/N` variants (page 2+ is `noindex,follow` + canonical to page 1), and any other `noindex` route are excluded. **Category entries carry a Google image-sitemap entry** (M-SEO5): one representative image per grid-bearing category — first resolvable SKU with an image, upsized to the ~1200px `largeSocialImage()` variant, mirroring `og:image`; CTA-only categories (per the baked `shouldShowEmptyStateCTA` rule) claim no image. The map is built from `getAllGeneratedCategorySlugs().imageSkus` + the memoized products index — one extra pass over the baked JSONs at build (~2s warm; `generateStaticParams` reads the same files earlier in the build, so the OS cache is warm), no network. The build logs per-section counts (`[sitemap] N URLs — static:… category:… (with images:…) …`). Site-wide, the root layout also emits `<meta name="googlebot" content="max-image-preview:large">` (metadata `robots.googleBot` — pages that set their own `robots`, like `/page/N` and `/search`, override it but are noindex anyway). **robots.txt** is generated by [app/robots.ts](app/robots.ts) (NOT a static `public/robots.txt`): allows all, disallows `/admin3773752` (obfuscated Studio) + `/api`, references the sitemap. (M5-508)

Internal linking: every root category page links to at least three related category pages and three related blog posts. Every blog post links to at least one category page in its body where contextually relevant.

## 12. Performance Targets

- LCP under 2.5 seconds on 4G
- CLS under 0.1
- INP under 200 milliseconds
- Initial JS bundle under 100 KB compressed for static category and blog pages

Geiger CDN images include explicit width and height attributes to prevent CLS. Non-Geiger images use Next.js Image component with responsive sizes, AVIF and WebP output, and lazy loading below the fold. Above-the-fold images use priority loading. Hero category image (first product card or category banner) is preloaded via `<link rel="preload" as="image">` for fastest LCP.

Fonts loaded with `font-display: swap` and preloaded for the main font weight. No more than two font weights total across the site.

## 13. Deployment

Vercel, two environments:

- Staging: `dev.perfectimprints.com`, deploys on every push to `develop` branch
- Production: `perfectimprints.com`, deploys on every push to `main` branch

Build command: `pnpm build`
Output: handled by Vercel's native Next.js support

**Revalidation webhook (manual setup, per environment).** Instant freshness for Sanity-managed content (mega menu, global settings, home, services pages, blogs, videos, custom products/categories, and the live search delta) depends on a single GROQ-powered Sanity webhook POSTing to `/api/sanity/revalidate`. This is a **one-time manual setup in the Sanity project** (API → Webhooks), not provisioned by code. **Status (2026-07-01): BOTH webhooks are now created — staging (`dev.perfectimprints.com`) and production (`www.perfectimprints.com`), each with `SANITY_WEBHOOK_SECRET` set in the matching Vercel env (Preview / Production), same filter + projection.** Full steps: [docs/sanity-webhook-setup.md](docs/sanity-webhook-setup.md) / TASKS.md M5-512. Without it, content still updates via ISR/on-demand fallback (up to the route's `revalidate` interval), just not instantly.

**Static-path budget.** Vercel's build runner has a hard ceiling on output filesystem inodes — Next.js 16 emits ~5 segment artifacts per static page, and pre-building all 22,180 categories plus pagination (~34,857 paths × 5 ≈ 175k symlinks) fails with `ENOSPC: no space left on device` during Vercel's output-assembly step (incident: first full deploy on 2026-05-31). Mitigation in place on the `/cat/[...slug]` route: `dynamicParams = true` + `revalidate = false`, and `generateStaticParams` returns root + modifier + compound-facet types (+ pagination) **plus the currently-owned customCategory slugs**, giving ~**1,840 static paths**. The 21,137 facet pages serve as on-demand SSG — first hit generates, then cached at the edge permanently until the next deploy (functionally identical to SSG for crawlers). Do NOT widen `PREBUILD_TYPES` in `app/cat/[...slug]/page.tsx` without re-measuring the path-vs-budget ratio.

**Keeping `/cat` static (three things that silently break it).** Next cannot statically prerender the route if EITHER (a) an uncached Sanity read (`@sanity/client` defaults to `no-store` in Next 16) runs in render, or (b) the page reads `searchParams` (a Dynamic API) — and (c) even with the route still `●` static, a **render-time `useSearchParams()` in ANY client component under the route** silently destroys the static HTML. All three are avoided: (a) all render-path Sanity reads go through the tag-cached `cachedClient` (Section 7 owned-slug precedence); (b) the page **does not read `searchParams`** — it renders the unfiltered, path-paginated view (static + indexable); (c) **no client component under `/cat` calls `useSearchParams()` during render** — during prerender that hook forces a CSR bailout (`BAILOUT_TO_CLIENT_SIDE_RENDERING`) up to the nearest Suspense boundary, which is the route-level `loading.tsx`, so the ENTIRE page body (H1, intro, product `<img>` grid, CollectionPage/ItemList/BreadcrumbList/FAQPage JSON-LD) was replaced by the loading skeleton in the served static HTML **while the build still reported `●`** (shipped that way until M-SEO5, 2026-07-07 — Google had to render JS to see any content). Instead, [components/category/CategoryShell.tsx](components/category/CategoryShell.tsx) owns the query state: it reads `window.location.search` in a post-mount effect (pathname-keyed + popstate, mid-transition reads guarded), and hands `FilterSidebar`/`SortDropdown` a `searchKey` string + `navigate()` callback (`filterStateFromSearchKey` in [lib/filter-types.ts](lib/filter-types.ts) parses it). Server prerender and first client render are the unfiltered view (hydration-safe, full static HTML); deep-linked filter params apply right after mount. Faceted filtering still runs in the dynamic route [app/api/category-products/route.ts](app/api/category-products/route.ts): CategoryShell fetches it when filters are active and paginates in-browser (path-based `/page/N` pagination stays server-side for the unfiltered SEO view). If you reintroduce `searchParams`/`cookies`/`headers` or an uncached `client.fetch` in the `/cat` render path, the whole route flips back to `ƒ (Dynamic)`; if you reintroduce a render-time `useSearchParams()` anywhere under the route, the HTML silently reverts to skeletons. **Verification after any change here: the `●` marker alone is NOT sufficient** — also curl the deployed page (e.g. `/cat/water-bottles`) and confirm the RAW HTML contains the `<h1>`, product `<img>` tags, and the CollectionPage + ItemList JSON-LD, with no `BAILOUT_TO_CLIENT_SIDE_RENDERING` template.

**Post-deploy warmup (monthly + manual only — NOT per-deploy; SMART capped list as of 2026-07-06).** Cold facets serve at 400-800ms on first hit while the on-demand SSG renders; warm ones serve under 200ms from edge cache. The warmup **no longer crawls all ~21K facet pages** — that full crawl was the main driver of both the Vercel bill (one ISR write per generated page) and the Sanity bill (each render's live API read), and Patrick approved reducing it (Option B "Smart"). `scripts/warmup/warmup-facets.ts` now warms the list from [scripts/warmup/build-warmup-list.ts](scripts/warmup/build-warmup-list.ts): **(1) guaranteed — every `/cat/` page linked from the mega-menu nav** (`lib/nav-data.ts`, the megaMenu seed source; all 465 are root-type and already prebuilt, so this is a verification that adds URLs only if that ever stops being true), **plus (2) the top `WARMUP_FACET_CAP = 3500` single-facet pages ranked by SKU count** from `facet-memberships.json` (single-facet `/cat/<root>/<type>/<value>` only — the 2 compound-facets are prebuilt via `PREBUILD_TYPES` and multi-segment combos stay on-demand; already-prebuilt/owned slugs excluded; ranking function isolated so SKU count can later be swapped for real GSC/Cloudflare traffic data). Total "never-cold" surface ≈ 1,840 prebuilt + 3,500 warmed; the remaining ~16.8K facets generate on-demand on first visit then cache until the next deploy — intended and fine (nothing about `/cat` staticness, freshness, the sitemap, or indexability changes; the sitemap still lists all 22,180 category URLs). Tune `WARMUP_FACET_CAP` after observing a billing cycle; dry-run the list + old-vs-new report with `pnpm warmup:list`. **It deliberately does NOT run on every deploy.** It used to fire on `deployment_status: success` for every Vercel deploy, but with several deploys a week (the weekly deals/new/rush scrape PRs + ordinary commits — none of which change facet pages) that generated millions of edge requests + ISR writes and **exhausted Vercel's account-wide free-tier limits**. A warmup is only actually needed after the **monthly full-catalog rebuild**, the one event that turns every facet page cold. So warmup runs only: (1) **automatically after the monthly rebuild's production deploy** — [.github/workflows/monthly-warmup.yml](.github/workflows/monthly-warmup.yml) listens to `deployment_status`, runs a cheap `gate` job that exits in seconds for every deploy EXCEPT the `Production` deploy whose merge commit is `chore(data): monthly catalog rebuild`, and only then calls the reusable [.github/workflows/post-deploy-warmup.yml](.github/workflows/post-deploy-warmup.yml) (`workflow_call`) targeting production `www`; and (2) **manually** via `post-deploy-warmup.yml`'s `workflow_dispatch` (defaults to staging; pass production www to warm prod) — locally `pnpm warmup` runs the same script and reads `NEXT_PUBLIC_SITE_URL`. Weekly scrape deploys and normal commits **never** trigger the warmup. **Two warm scopes (2026-07-06):** the workflow's `warm_scope` input (default `smart`) → env `WARMUP_SCOPE` in `warmup-facets.ts`. `smart` = the capped list above (what the automatic monthly `workflow_call` path uses — `monthly-warmup.yml` passes no `warm_scope`, so the default keeps it on the capped list). `all` = EVERY category URL from `category-urls.json` (~22,180 incl. prebuilt — a warm hit on a static page is cheap; `buildFullWarmupList()` in [scripts/warmup/build-warmup-list.ts](scripts/warmup/build-warmup-list.ts)). `all` is used ONLY by the Studio **Warm All Pages** button (Site Refresh panel, below) and explicit manual dispatch — it costs real Vercel/Sanity resources, so the panel gates it behind a cost-warning confirm. This is safe because on-demand SSG works without warmup — a cold facet just serves its first hit slower then caches permanently until the next deploy — so the capped list costs only a little long-tail speed while removing the recurring resource drain. The monthly rebuild PR (M6-606) is still covered automatically once merged + redeployed; no explicit warmup step lives in `monthly-rebuild.yml`.

DNS is managed by Cloudflare (DNS-only mode, no proxy). DNS cutover plan: lower TTL on existing perfectimprints.com records 48 hours before launch. On launch day, repoint apex to Vercel production via Cloudflare DNS.

**Monthly auto-rebuild (M6-606, IMPLEMENTED; manual-only as of M6-608):** [.github/workflows/monthly-rebuild.yml](.github/workflows/monthly-rebuild.yml). **The monthly `cron` was REMOVED** — the full-catalog rebuild now runs ONLY on `workflow_dispatch`: either Patrick presses **Full Catalog Rebuild** in the Sanity **Site Refresh** panel (Section 7 / Section 13 "Site Refresh") or a manual dispatch. The dispatch form takes a `phases` input (default `A,B,C,E`) so a subset can be tested without the long Phase C — e.g. run `A,B,E` to validate the pipeline in minutes.

**Job structure (the 6-hour-per-job split).** GitHub-hosted runners cap a single job at 6h. Phase C's main pass is ~2.6-2.8h at 6 workers (SCRAPE-901 counted ~60k paginated Searchspring requests, not the ~21.7k one-per-URL floor earlier estimates assumed), and the recovery passes add a search crawl of their own, so the run is split into separate jobs joined by artifacts (no single job runs A+B+C):

- **`config`** - parses the `phases` input into `run_ab` / `run_c` / `run_e` flags (scheduled runs = all).
- **`scrape-ab`** - Phase A → B (taxonomy + product catalog) → **the Phase B global top-up** (`--phase b --global-only`, SCRAPE-910: the no-filter Searchspring pass that catches products reachable only through "Shop By" virtual collections; 822 of the committed 7,957 products, 10.3%, exist ONLY through it, and it was run by hand in May 2026 and never wired in, so every fresh rebuild would have silently dropped them and the prune would then have stripped them from 4,319 category pages. ~137 extra requests, ~2-3 min, merges by SKU, idempotent. If it fails the job fails and no PR opens). ~30-50 min total. Checkpoint-cached (`phase-b-<run_id>` with `phase-b-` restore-key) + `--resume`. **Phase A does not kill the job when www.geiger.com blocks the runner (SCRAPE-910):** on a fetch/parse failure it keeps the committed `categories.json`, warns loudly, and records the fallback in the gitignored `data/geiger/taxonomy-fetch-status.json`, which rides the artifact so the PR body + Patrick's email lead with it. A fallback run misses brand-new Geiger categories, and a category Geiger renamed keeps its old path (0 results, its products drop unless cross-listed; the data-loss guard catches a big drop). Uploads `categories.json` + `products.json` + the status file as the `geiger-ab` artifact.
- **`scrape-e`** - Phase E (brand logos), ~5 min. **No longer runs Phase A first** (SCRAPE-910: the old step's comment claimed Phase E reads `categories.json`; `brand_logos.py` never does, it reads `products.json` and the shop-by-brand page). The single index fetch is **non-fatal**: on failure (or a parse yielding zero brands) it keeps the committed `brands.json` + logo files (the scrape_catalogs.py keep-previous pattern) and records the fallback in the gitignored `data/geiger/brand-index-fetch-status.json`. Per-logo downloads stay on plain httpx deliberately: they only run after the index fetch succeeded, i.e. from a machine Geiger is not challenging. Uploads `brands.json` + `brand-logos/` + `public/brand-logos/` + the status file as `geiger-e`.
- **`scrape-c`** - Phase C (facet/modifier memberships), the long pole. Runs with `--workers 6` (six independent 1-req/sec clients per §16) **plus** `--resume` and a cached `phase-c-<run_id>` checkpoint, so an overrun resumes from the last 100-URL checkpoint on a re-run instead of restarting. `timeout-minutes: 350`. Downloads `geiger-ab` when present (else uses the committed `products.json`/`categories.json`); uses the committed `pi-to-geiger.json`. Uploads `facet-memberships.json` as `geiger-c`.
- **`scrape-c-recover`** - SCRAPE-910: the Phase C **Tier 1 brand fallback** (`--retry-brands`) then **Tier 2 search fallback** (`--retry-search`), the two recovery passes that were only ever run by hand in May 2026 (they supplied ~3,434 of the 13,968 product-bearing facet URLs: 809 brand + 2,625 search). Without them a fresh Phase C regenerates `facet-memberships.json` with those URLs back at zero. Both are Searchspring-only, idempotent, and self-narrowing (they touch only currently-zero URLs and only write back URLs that GAIN products). Own job so the combined crawl cannot hit the 6h cap (Tier 1 is minutes; Tier 2 re-queries every remaining zero facet URL, roughly 1-3h at 6 workers). Downloads `geiger-ab` (or committed) + `geiger-c`; uploads the merged file as `geiger-c-recovered`. **A failure here blocks assemble** (shipping the unrecovered file would regress those URLs).
- **`assemble`** - downloads all scrape artifacts over the committed copies (the recovered memberships file over the main-pass one), then: (0) **the data-loss guard** ([scripts/monthly/verify-rebuild-data.ts](scripts/monthly/verify-rebuild-data.ts), SCRAPE-910) - compares fresh vs HEAD and **fails the run before any PR** if total products drop >5%, product-bearing category pages drop >2%, or facet URLs with products drop >5% (rules + margin reasoning in [scripts/monthly/rebuild-data-guard.ts](scripts/monthly/rebuild-data-guard.ts), vitest-covered; small drops are normal Geiger churn, large ones are a bug - the two failure modes it exists for are 10.3% and 24.6%). Runs before the prune so `data/categories` is still the HEAD baseline; (1) **AI content for NEW categories only** via `generate_content.py --skip-existing` (`DEEPSEEK_API_KEY`) - existing pages are untouched; since the PI URL set is frozen this is usually a cheap no-op, present as a safety net; (2) **prune removed products** ([scripts/monthly/prune-removed-skus.ts](scripts/monthly/prune-removed-skus.ts)) - drops SKUs no longer in `products.json` from each baked `data/categories/*.json` `productSkus[]` (render-time `resolveProducts` already skips missing SKUs, so pages are correct either way; the prune keeps the committed data accurate + gives an explicit diff); (3) **compute the change summary** ([scripts/monthly/compute-summary.ts](scripts/monthly/compute-summary.ts)) - diffs the fresh data vs `HEAD` for products added/removed/price-changed, new brands + logo changes, and new/updated category pages, writing `pr-body.md` + `summary.json`; SCRAPE-910: it also reads the two fetch-status files + the guard report, leads the PR body with a fallback warning when one happened, and emits a `fallback` output; (4) **open a PR** to `main` (branch `monthly-rebuild`, `peter-evans/create-pull-request`); (5) **email Patrick** the summary via Gmail SMTP ([scripts/monthly/send-summary-email.ts](scripts/monthly/send-summary-email.ts)) - a fallback is the FIRST thing the email says (red panel + "[Needs attention]" subject prefix), and the email also fires on a no-change fallback run; (6) **auto-merge the PR** as the FINAL step (`gh pr merge --squash --delete-branch`). Steps (4) and (6) only fire when something actually changed (`steps.summary.outputs.changed`).

**SCRAPE-910 (2026-08-16): why the fallbacks, the wired-in passes, and the guard exist.** Patrick's first Full Catalog Rebuild press failed in one minute: of the run's ~61,000 requests, exactly three hit `www.geiger.com`, which challenges GitHub's datacenter IPs (Cloudflare bot management; diagnosis in [docs/scrape-900-rebuild-403-diagnosis.md](docs/scrape-900-rebuild-403-diagnosis.md), full request count in [docs/scrape-901-request-count.md](docs/scrape-901-request-count.md)). Phase A had no error handling, so those three requests killed the ~60,000 Searchspring requests that would all have succeeded (Searchspring is proven from Actions by the weekly deals/new/rush scrapes). Worse, the diagnosis found the rebuild would have DESTROYED data had it succeeded: the Phase B global top-up and the Phase C recovery passes were only ever run by hand and lived in no workflow. **That was our own gap - nobody blocked us and nothing changed underneath us; the passes were simply never wired in.** The fix: Phase A and Phase E fall back to committed data (loudly, surfaced first in the PR body and the email), the two hand-run passes are now workflow steps (`scrape-ab`'s top-up step, the `scrape-c-recover` job), the client fails fast on 4xx instead of retrying a deterministic 403 five times and preserves the real response headers/body in the error, and the assemble job's data-loss guard refuses to open a PR when the catalog shrinks past margin. **A fully-blocked run still refreshes:** products (against the kept taxonomy), facet memberships incl. recoveries, prune + re-expand. **It does not refresh:** the category tree and the brand list/logos (kept at committed state; only brand-new Geiger categories/brands are missed). **If you remove the top-up step, the recover job, or the guard again, a "successful" rebuild deletes ~10% of the catalog and a quarter of the facet coverage - do not.**

**Auto-merged (M6-608), with the warmup-trigger commit title.** Every refresh workflow now **merges its own PR as its final step after a successful run** — the monthly rebuild included (changed in M6-608; it was previously left open for manual review). The squash subject is held to exactly **`chore(data): monthly catalog rebuild`** so [.github/workflows/monthly-warmup.yml](.github/workflows/monthly-warmup.yml)'s gate fires and the 21K facet warmup runs after the production redeploy. Because the merge is the LAST step, a **Cancel** (from the Site Refresh panel) before it leaves `main` untouched. **Phase D is not re-run** (mapping is stable; the committed `pi-to-geiger.json` is used as-is). **Warmup stays automatic** — on merge Vercel rebuilds production and emits a successful `deployment_status`, which `monthly-warmup.yml` matches against that commit title and then calls [.github/workflows/post-deploy-warmup.yml](.github/workflows/post-deploy-warmup.yml); no explicit warmup trigger is needed in the monthly job.

**Required repo secrets** (Settings → Secrets and variables → Actions — on BOTH the staging repo `raoalihamza/perfectimprints` and Patrick's production repo `pbnj53/perfectimprints`): `DEEPSEEK_API_KEY` (AI content for new categories), `GMAIL_USER` + `GMAIL_APP_PASSWORD` (summary email). Optional `LEAD_EMAIL_TO` / `LEAD_EMAIL_FROM` (default `patrick@perfectimprints.com`). **No Sanity secrets** — content writes only to `data/categories/*.json`. The auto-merge step uses the built-in `GITHUB_TOKEN` (no extra secret). Actions settings must already allow Read/write permissions + "Allow GitHub Actions to create and approve pull requests" (same as the weekly scrape jobs); the auto-merge additionally needs PR merges allowed (no required status checks blocking `main`, or the merge step retries then fails). The AI-content + email steps no-op with a logged warning if their secret is absent, so a misconfigured run still produces the data PR. **Note:** the Site Refresh panel's `GITHUB_WORKFLOW_TOKEN` (Section 14) is a **Vercel** env var (the API route reads it), NOT a GitHub Actions secret.

**Site Refresh Studio panel (M6-608; +Warm All Pages 2026-07-06).** Patrick triggers / watches / cancels all five refresh workflows from a dedicated top-level Sanity Studio tool — **"Site Refresh"** ([sanity/tools/site-refresh-tool.tsx](sanity/tools/site-refresh-tool.tsx), registered in the `tools` array of [sanity/sanity.config.ts](sanity/sanity.config.ts), a separate tab like "Push to Sanity", NOT under globalSettings). The five buttons (Refresh New Products / Deals / Rush Products / Full Catalog Rebuild / **Warm All Pages**) call the server route [app/api/sanity/workflows/route.ts](app/api/sanity/workflows/route.ts), which dispatches / polls status of / cancels the GitHub Actions workflows via a **server-only GitHub fine-grained PAT** (`GITHUB_WORKFLOW_TOKEN`, Section 14). The route is **auth-protected with a cookie-session-safe handshake** (this Studio authenticates by COOKIE — there is NO JS-accessible Sanity session token, and the session cookie lives on `*.api.sanity.io` so the route can't read it either; a bearer-token/`users/me` scheme is therefore impossible here). Instead it mirrors the proven **Push to Sanity** pattern (privileged action via a cookie-authed Sanity write): the panel writes a random nonce to a **draft** doc `drafts.siteRefreshAuth` via the cookie-authed Studio client (only a logged-in user with dataset-write grants can write it; a **draft** is not returned by anonymous/public dataset reads, so an unauthenticated caller can neither create the doc nor read the nonce), sends that nonce on every call in `x-refresh-nonce`, and the route reads the draft nonce with its **own server `SANITY_API_TOKEN`** and compares timing-safe (+ a 24h freshness window; the panel re-handshakes on the resulting 401). Anonymous/curl callers (no valid nonce) get 401; cross-origin browser callers get 403 (Origin guard); the **GitHub PAT stays server-only**. Tradeoff: this proves "an authenticated project member with dataset-write access initiated the call from the first-party Studio," which is the strongest gate available without a JS token — not a per-user identity assertion. The target repo is configurable (`GITHUB_REPO_OWNER` / `GITHUB_REPO_NAME`, default the production repo `pbnj53/perfectimprints`; staging points at `raoalihamza/perfectimprints`). The shared workflow registry is [lib/site-refresh/workflows.ts](lib/site-refresh/workflows.ts). **Warm All Pages (2026-07-06):** registry key `warm-all` dispatches the EXISTING warmup engine `post-deploy-warmup.yml` with inputs `{site_url: 'https://www.perfectimprints.com', warm_scope: 'all'}` — always production, always the full ~22,180-URL set (see the warmup section above for the two scopes). No new crawler, no new secret (same PAT + nonce auth). The entry has no `branch` (the warmup opens no PR; `RefreshWorkflow.branch` is now optional and the route's Cancel skips PR/branch cleanup when it's absent — Cancel just stops the crawl, already-warmed pages stay warm). The entry's `confirmMessage` drives a plain-language **cost-warning confirm dialog** in the panel (`window.confirm` before trigger); the run only starts on confirm. Status/poll/Cancel work like the other buttons — note the panel polls `post-deploy-warmup.yml`'s latest **dispatch** run (the monthly path invokes it via `workflow_call`, whose runs list under `monthly-warmup.yml`, so they don't collide). **Cancel = full revoke** for the PR-opening workflows: it cancels the in-progress run, then closes any PR that run opened and deletes its working branch — and because the auto-merge is each workflow's LAST step, cancelling before it means `main` was never touched. If a run already merged before Cancel, the panel says so honestly (can't undo a completed merge). **No Sanity webhook/projection change** — this panel triggers workflows, it does not add a rendered content surface; the route is server-only and not in any render path (so `/cat` stays static). Manual PAT setup is required (see Section 14).

**Aggregator scrapes (manual-only as of 2026-07-06):** Three separate GitHub Actions: `scrape-deals.yml` (Phase F → `data/geiger/deals.json`), `scrape-new-products.yml` (Phase G → `data/geiger/new-products.json`), and `scrape-rush-products.yml` (Phase H → `data/geiger/rush-products.json`). Each runs only its Phase script, diffs its single output file, opens a PR if anything changed, and (M6-608) **auto-merges that PR as its final step** via `gh pr merge --squash --delete-branch`. Why separate from the monthly job: these feeds turn over within days, not months, and each script finishes in ~1 minute (1 base call + 1 per-facet-value call). **The Sunday `cron`s were DISABLED 2026-07-06 (cost control, client decision)** — the `schedule:` blocks are commented out in all three YAMLs (re-enable by uncommenting); each keeps `workflow_dispatch` (used by the Site Refresh panel buttons + manual dispatch). A fourth aggregator scrape, `scrape-catalogs.yml` (Phase I → `data/geiger/catalogs.json`, P2-CAT-000), follows the same diff → auto-merge-PR pattern (branch `chore/catalog-refresh`) but was born dispatch-only — it never had a cron (yearly cadence; the Site Refresh **"Refresh Catalogs"** button drives it). So ALL refreshes are now on-demand only: the four scrapes, the monthly rebuild, and the warmup.

## 14. Environment Variables

Required at build and runtime:

```
SANITY_PROJECT_ID
SANITY_DATASET
SANITY_API_TOKEN
SANITY_WEBHOOK_SECRET
DEEPSEEK_API_KEY
GMAIL_USER=patrick@perfectimprints.com
GMAIL_APP_PASSWORD
LEAD_EMAIL_TO=patrick@perfectimprints.com
LEAD_EMAIL_FROM=patrick@perfectimprints.com
NEXT_PUBLIC_SITE_URL=https://www.perfectimprints.com
NEXT_PUBLIC_GEIGER_HOST=https://patrickblack.geiger.com
GA4_MEASUREMENT_ID
NEXT_PUBLIC_GTM_ID=GTM-MCQP434P
NEXT_PUBLIC_TURNSTILE_SITE_KEY
TURNSTILE_SECRET_KEY
GITHUB_WORKFLOW_TOKEN
GITHUB_REPO_OWNER=pbnj53
GITHUB_REPO_NAME=perfectimprints
```

**Site Refresh GitHub PAT (`GITHUB_WORKFLOW_TOKEN` + `GITHUB_REPO_OWNER` / `GITHUB_REPO_NAME`).** Powers the Studio **Site Refresh** panel via [app/api/sanity/workflows/route.ts](app/api/sanity/workflows/route.ts) (Section 13). `GITHUB_WORKFLOW_TOKEN` is a GitHub **fine-grained PAT** scoped to the target repo, with these repository permissions: **Actions — Read and write** (dispatch + cancel runs), **Pull requests — Read and write** (close PRs on cancel), **Contents — Read and write** (delete the run's branch on cancel). It is read **server-side only** by the route and is **never** exposed to the browser. **Manual step:** create the PAT in GitHub, then add it (plus the repo owner/name) in **Vercel env** for BOTH deployments — staging pointed at `raoalihamza/perfectimprints`, production at `pbnj53/perfectimprints` (the defaults when the owner/name vars are unset). The route returns a clear 500 if the token is unset, and 401/403 if the caller isn't an authenticated first-party Studio user. The route also uses the **already-required `SANITY_API_TOKEN`** (server-side) to verify the Studio handshake nonce (Section 13 auth model) — no new Sanity secret is introduced. This is NOT a GitHub Actions secret — the workflows' own auto-merge step uses the built-in `GITHUB_TOKEN`.

**Cloudflare Turnstile (`NEXT_PUBLIC_TURNSTILE_SITE_KEY` + `TURNSTILE_SECRET_KEY`).** The lead form ([components/forms/LeadForm.tsx](components/forms/LeadForm.tsx), used on `/contact` and in the category-page modal) renders a managed/auto-detect Turnstile widget ([components/forms/Turnstile.tsx](components/forms/Turnstile.tsx)) when the public key is set; Cloudflare injects a `cf-turnstile-response` token into the form, and [app/api/leads/route.ts](app/api/leads/route.ts) verifies it against `siteverify` (with the secret key) before sending. **Both keys absent → the widget doesn't render and server verification is skipped (one-line warning logged), so the form still works** (e.g. on staging); the CAPTCHA activates automatically once both are present. Honeypot + 5/IP/hr rate limit stay active regardless. Create a free Turnstile site in Patrick's Cloudflare dashboard to get the keys, then add both in Vercel. Never hardcode keys.

Never commit a `.env` file. Use `.env.example` with empty values as the template.

**Google Tag Manager (`NEXT_PUBLIC_GTM_ID`).** GTM is loaded in the root layout ([app/layout.tsx](app/layout.tsx)) via `@next/third-parties`'s `GoogleTagManager` component (head script, `next/script` default `afterInteractive` strategy → deferred, off the render-blocking path so it does not regress the M5-508 LCP/Speed Index work) plus a manual `<noscript>` iframe as the first child of `<body>` (the component does not emit the noscript fallback). It is **env-driven**: if `NEXT_PUBLIC_GTM_ID` is unset (e.g. staging without analytics) the layout renders NOTHING, so the build never hard-depends on it. Only the GTM container loads — **no individual tags are hardcoded**; Patrick manages GA4, live chat, and every other tag from the GTM dashboard. The raw `<head>` snippet is intentionally NOT pasted — the component is the correct equivalent. Reading `process.env.NEXT_PUBLIC_GTM_ID` is a build-time inlined constant, so `/cat` stays static. M5-513.

## 15. External Services

- **Sanity** for CMS. Project lives under Patrick's Sanity account.
- **Vercel** for hosting. Account is Patrick's.
- **Cloudflare** for DNS only (DNS-only mode, not proxied).
- **DeepSeek** for AI content. API key on Patrick's account, billed to him directly.
- **Gmail SMTP** for transactional email. Uses Patrick's Google Workspace account with an app password. Requires SPF and DKIM records in DNS.
- **Google Search Console and Google Analytics 4** for analytics. Existing GA4 property continues, new GSC property added for new site verification.
- **Google Tag Manager** is the tag container (id `GTM-MCQP434P`, env var `NEXT_PUBLIC_GTM_ID`). Patrick manages GA4, live chat, and other tags from the GTM dashboard — the site only loads the container (deferred via `@next/third-parties`), it does not hardcode individual tags. See Section 14.

## 16. Data Pipeline Rules

Geiger data integration is permitted. Patrick has confirmed (he is a Geiger distributor).

Pipeline runs locally or in scheduled GitHub Actions, never in production runtime.

Throttle: one request per second per worker against the Searchspring API. Use `httpx` HTTP/2 client (and `curl_cffi` for Cloudflare bypass on the main geiger.com hostname) with `tenacity` retry on transient failures.

Checkpointing: save state every 100 requests so partial runs resume. State file at `scripts/scrapers/geiger/.checkpoint/`.

**Six-phase pipeline (A–E monthly, F weekly):**

**Phase A: Taxonomy discovery.** One HTTP GET to a Geiger category page (e.g., `https://www.geiger.com/b/accessories`), parse the mega menu HTML with BeautifulSoup, extract the full category tree with parent-child relationships. Output: `data/geiger/categories.json` (544 categories, 482 leaves). Runtime: minutes.

**Phase B: Product catalog.** For each Geiger leaf category, paginate the Searchspring API with `perPage=60`. Deduplicate by SKU. Output: `data/geiger/products.json` (7,957 unique SKUs, 99.82% of Geiger's total catalog of 7,971). Runtime: 20-40 minutes.

**Phase C: Facet and modifier memberships.** For each of the 21,715 PI URLs that need product membership data (576 modifiers + 21,137 facets + 2 compound facets), one filtered Searchspring API call to capture the SKU list. Output: `data/geiger/facet-memberships.json`. Runtime: 6 hours unattended. **The file is written in DETERMINISTIC order (SCRAPE-940): `memberships` keys and `urlsWithErrors` sorted at the single write point (`_save_output` in [scripts/scrapers/geiger/memberships.py](scripts/scrapers/geiger/memberships.py)); the scrape itself stays fully parallel.** Without the sort, 6 workers write the file in completion order, different every run from the first key: run #5's rebuild PR carried ~1.42M diff lines from this one file with 12,818 of 21,486 entries byte-identical and merely moved, which is what made GitHub's PR-creation endpoint 500 (SCRAPE-930). Do not remove the sort as a "pointless operation". The nested per-URL SKU lists are deliberately NOT sorted: each is one sequential paginated fetch in Searchspring's relevance order, largely stable between runs, and `generate_content.py` feeds it into new pages' baked `productSkus` (grid order), so that order carries meaning. Every reader is order-insensitive to the key sort (the guard counts entries, `lib/filters.ts` converts each list to a `Set`, lookups are by key). **The first rebuild after 2026-08-18 would have paid a one-time transitional diff (3.6-4.6M changed lines in this file depending on diff algorithm, 2.5-3.2x run #5) as every entry moves to its sorted position once - but the re-sorted file is already staged alongside this change (content-identical, verified), so once committed the transition never reaches a rebuild PR; after that, diffs show only genuine change.**

**Phase D: PI-to-Geiger mapping.** Match each of the 465 PI root categories to a Geiger leaf via exact slug match (preferring non-aggregator leaves over `All <X>` aggregators), then fuzzy match with rapidfuzz (WRatio + token_set_ratio, threshold 80), then manual overrides in `scripts/scrapers/geiger/mapping_overrides.json`. Output: `data/mappings/pi-to-geiger.json` (465/465 mapped, 0 unmapped) plus a CSV report. Runtime: seconds.

**Phase E: Brand logo scrape (added 2026-05-26 per Patrick feedback).** Visit `https://www.geiger.com/c/shop-by-brand` to enumerate brand pages, then download the logo image from each brand's page. Store at `data/geiger/brand-logos/{brand-slug}.{webp|png|jpg}`. Output also includes `data/geiger/brands.json` with brand metadata (name, slug, description, logo path, product count cross-referenced from products.json). Runtime: 30-60 minutes. Runs as part of monthly auto-rebuild.

**Phase F: Weekly deals scrape (added 2026-06-13 per Patrick feedback).** Standalone weekly job (`scripts/scrapers/geiger/scrape_deals.py`, `pnpm scrape-deals`) that captures Geiger's current sale + closeout list. Three steps:

1. **Meta** — one call to `https://kfx28d.a.searchspring.io/api/meta/meta.json?siteId=kfx28d` for human-readable facet labels (Color, Brand, Material, etc.).
2. **Base deals** — paginate `category.json?siteId=kfx28d&bgfilter.category_path=Home > Shop By > Deals&resultsFormat=native` to capture product objects + the embedded `facets` array (values + counts but no SKU lists).
3. **Per-facet-value SKU memberships** — one filtered call per facet value (`filter.<field>=<value>` for list facets, `filter.<field>.low/.high` for range facets) to capture which deal SKUs belong to that value. Currently ~40-60 calls per run (under 1 minute at the 1 req/sec throttle).

Output: `data/geiger/deals.json` shaped as `{scrapedAt, totalDeals, products[], facets[{field, label, type, values:[{id, label, count, low, high, type, skus[]}]}]}`. The `skus[]` arrays power accurate client-side filter intersections in the `/deals` filter sidebar (`components/deals/DealsClient.tsx`), so OR-within-section + AND-across-section semantics work without any runtime API call.

`ss_category_hierarchy` is dropped at scrape time; the loader (`lib/deals.ts::getDealsData`) synthesizes a flat top-level "Category" section from `product.category_paths` instead (Geiger's "Shop By" pseudo-department excluded). Patrick can blocklist specific SKUs via `globalSettings.dealsPage.hiddenDealSkus` in Sanity — `applyHiddenSkus` re-derives every facet section's value counts so the sidebar stays consistent with the visible grid.

Runs from `.github/workflows/scrape-deals.yml` via `workflow_dispatch` only (the Sunday `cron: '0 23 * * 0'` was DISABLED 2026-07-06 — commented out, re-enable by uncommenting; trigger from the Site Refresh panel or manual dispatch). Opens an auto-merge PR if and only if `deals.json` actually changed.

**Phase G: Weekly new-products scrape (added 2026-06-16 per Patrick feedback).** Identical pattern to Phase F but for `bgfilter.category_path=Home > Shop By > New Products`. Script: `scripts/scrapers/geiger/scrape_new_products.py` (`pnpm scrape-new-products`). Output: `data/geiger/new-products.json`. Runs from `.github/workflows/scrape-new-products.yml` via `workflow_dispatch` only (the Sunday `cron: '30 23 * * 0'` was DISABLED 2026-07-06 — commented out; trigger from the Site Refresh panel or manual dispatch). Note: this feed sends `is_new_item` as the string `"Yes"` rather than a boolean, so the normalizer coerces it explicitly. Drives `/new-products`.

**Phase H: Weekly rush-products scrape (added 2026-06-18 per Patrick feedback, M5-506a).** Identical pattern to Phase G but for `bgfilter.category_path=Home > Shop By > 24 Hour Rush Products`. Script: `scripts/scrapers/geiger/scrape_rush_products.py` (`pnpm scrape-rush-products`). Output: `data/geiger/rush-products.json` shaped `{scrapedAt, totalRushProducts, products[], facets[...]}` (~53 products / 1 page today). Runs from `.github/workflows/scrape-rush-products.yml` via `workflow_dispatch` only (the Sunday `cron: '45 23 * * 0'` was DISABLED 2026-07-06 — commented out; trigger from the Site Refresh panel or manual dispatch), branch `chore/weekly-rush-refresh`. Rush-specific handling: (a) the feed sends `is_new_item` as `"Yes"` (coerced like Phase G); (b) results carry no `badges` array, so no NEW/SALE/CLOSEOUT ribbons; (c) at least one SKU contains a space (e.g. `"501622 1BC"`), preserved as-is; (d) **facet hygiene** — degenerate single-value facets are dropped at scrape time (`_is_degenerate_section`: fewer than 2 values, or one value covers 100% of products). For rush this drops `production_time` (single value "1"), plus any other single-bucket facet (`refine_by`, `pen_style` today). Drives `/rush-products`.

**Phase I: Catalog products scrape (added 2026-07-15, P2-CAT-000 — YEARLY cadence, dispatch-only).** One scraper for the 7 Geiger themed catalogs behind the Milestone 3 catalog lead pages. Script: `scripts/scrapers/geiger/scrape_catalogs.py` (`pnpm scrape-catalogs`), config in the in-file `CATALOGS` list with STABLE slugs (`ideas`, `green-guide`, `womens-collection`, `holiday-guide`, `usa-made`, `retail-collective`, `trend-talk` — the Sanity catalog docs key off them; do not rename). Output: `data/geiger/catalogs.json` (~1.9 MB) shaped `{scrapedAt, catalogs:[{slug, title, source, browseUrl, pdf, totalProducts, products[], facets[]}]}` — full product objects (not bare SKUs, so a catalog renders products newer than `products.json`) + deals-shaped facets with per-value `skus[]` (same per-facet-value membership calls + degenerate-facet hygiene as Phases F–H). Three source modes: `category` (category.json `bgfilter.category_path` — Ideas `Home > Shop By > Ideas` 203, Green Guide 54), `search` (search.json `q=` — Women's Collection 88, Holiday Guide 87), and `filter` (search.json **`bgfilter.refine_by=Made in the USA`** for USA Made → the CLEAN 611 set Patrick approved; identical result set to `filter.refine_by`, but `bgfilter.` keeps the degenerate refine_by facet out of the sidebar; deliberately NOT the literal SHOP NOW `q=made in the usa` search, ~641 noisy). Retail Collective + Trend Talk have **`source: null`** (no Searchspring source; Patrick curates their products manually in Studio) but still ship in the file with their external flipbook `browseUrl`s. **Catalog asset metadata (5 internal viewers):** each run re-parses `https://patrickblack.geiger.com/c/<viewer-slug>` for the embedded yupub share link (`my.yupub.com/?tid=<uuid>` — NEVER hardcode tids, new editions get new tids) then calls `https://api.yupub.com/?task=get_me&tid=<tid>` (XML) → `pdf: {tid, url, filename, filesize, pages, baseUrl}`; `baseUrl` is the CloudFront prefix for `Leaf_N.jpg` page images. Asset failures are **NON-FATAL** (keep previous catalogs.json values, warn, continue; blanket-retried because curl_cffi DNS errors bypass the client's httpx-only tenacity retry). Internal `browseUrl`s use the affiliate host (Section 18). Workflow: `.github/workflows/scrape-catalogs.yml`, `workflow_dispatch` only (no cron — yearly), branch `chore/catalog-refresh`, auto-merge PR as the final step; Site Refresh panel button **"Refresh Catalogs"** (registry key `catalogs` in [lib/site-refresh/workflows.ts](lib/site-refresh/workflows.ts)).

**Sanity augmentation on top of Phase F + G + H (added 2026-06-16, see M5-511 + M5-506a).** `/deals`, `/new-products`, and `/rush-products` all go through an augmentation layer before render — see Section 8 "Deals + new-products data" for the editorial levers. Pipeline modules: [lib/products/augment.ts](lib/products/augment.ts) (pure merger — rebuilds synthetic Category section, injects custom-product filter tags into facet values), [lib/products/lookup.ts](lib/products/lookup.ts) (SKU index over products.json), [lib/sanity/queries/custom-products.ts](lib/sanity/queries/custom-products.ts) (`customProductToGeigerProduct` normalizer). Orchestrators: `getAugmentedDealsData()` / `getAugmentedNewProductsData()` / `getAugmentedRushProductsData()` in their respective lib files. Custom products use synthesized SKUs of the form `custom-<sanity-_id>` so they never collide with Geiger SKUs.

For **modifier URLs** (search, no-minimum, closeout, production-time, eco-friendly, material), the Searchspring filter mapping is (verified during the first end-to-end Phase C run, 2026-05-22):

| PI modifier                | Searchspring filter             | Notes                                                                                                    |
| -------------------------- | ------------------------------- | -------------------------------------------------------------------------------------------------------- |
| `search`                   | no extra filter                 | Treated as a search-landing variant of the root                                                          |
| `no-minimum`               | `filter.min_qty.high=24`        | Items needing fewer than 25 units. Range filters use `.low`/`.high` suffixes, NOT bracketed `[lt]`       |
| `closeout`                 | `filter.refine_by=Deals`        | `filter.is_on_sale=true` returned 0 across all categories; `Deals` is the only "on sale" refine_by value |
| `production-time`          | `filter.production_time.high=5` | Rush items (1–5 day production)                                                                          |
| `eco-friendly`             | `filter.refine_by=Eco Friendly` | Best when the root mapping is also `Home > Shop By > Eco-Friendly > <child>` where one exists            |
| `material` (1 PI URL only) | no extra filter                 | Treat as a landing variant of the root                                                                   |

For **compound facet URLs** (2 of them), send multiple `filter.[type]=[value]` params in one call. The request must use a list of tuples, not a dict, to preserve duplicate keys.

**Searchspring vocabulary gotchas:**

- `filter.refine_by` has only 3 valid values: `Made in the USA`, `Eco Friendly`, `Deals`.
- Numeric fields require integer values (`filter.production_time=5`, not `=5 Days`).
- Range syntax uses dotted suffix: `filter.<field>.low=N`, `filter.<field>.high=N`.
- Brand filter values use human-readable form: `filter.brand=Vineyard Vines` (not the slug).

**Slug-based resolver.** Many PI facet URLs map to a dedicated Geiger category SLUG instead of a filter combination. The resolver in `scripts/scrapers/geiger/memberships.py::resolve_slug_match` tries children-of-root name match, then `<value>-<root>` / `<root>-<value>` / bare `<value>` slug candidates restricted to the same top-level Geiger branch.

**Empty-page handling (zero-result PI URLs).** Patrick confirmed (2026-05-23) that PI URLs with no Geiger match should link to the Geiger homepage. To minimize how many pages need that fallback, Phase C includes a 4-tier recovery chain:

1. **Tier 1 — brand fallback** (`--retry-brands`). Recovered 809 URLs.
2. **Tier 2 — search-keyword fallback** (`--retry-search`). Recovered 2,625 URLs.
3. **Tier 3 — parent-root fallback** (Module 3 template logic). Renders parent root's product grid with explanatory header.
4. **Tier 4 — Geiger homepage CTA** (Module 3 template logic, last resort).

**Final Phase C breakdown:** 13,968 with products (64.3%), 7,518 zero (34.6%), 229 errors (1.1%). Plus 465 roots (all mapped). Overall: 14,433 URLs (65%) render with real product grids, 7,747 (35%) use Tier 3/4 fallback at render time.

**Blog migration (completed 2026-06-10).** PI is geo-blocked from Pakistan at the Cloudflare WAF and CF Turnstile escalates to interactive challenge on any rapid sequential automation. The working stack is **SeleniumBase UC mode + system-wide US/EU VPN** (Cloudflare WARP, ProtonVPN — _not_ a browser-extension VPN like Browsec). Implementation at [scripts/scrapers/blogs/scrape_sbase.py](../scripts/scrapers/blogs/scrape_sbase.py); see [scripts/scrapers/blogs/README.md](../scripts/scrapers/blogs/README.md) for prereqs. Final coverage: 649 of 731 blogs scraped + published to Sanity; 82 verified-deleted URLs preserved at `data/blogs/.failed-slugs.txt` for delivery handoff. MPower export was investigated and ruled out — no usable bulk-export option in their admin. Tried-and-failed shortcuts: cloudscraper, curl_cffi chrome131, Playwright stealth (all fail under Turnstile escalation).

## 17. Conventions

- Server Components are the default. Use `'use client'` only where state, effects, or browser APIs are needed.
- Data fetching happens in Server Components, never in client components except via the Next.js Route Handler API.
- Forms post to App Router Route Handlers under `/app/api/`.
- All TypeScript is strict mode. No `any` without an inline justification comment.
- Sanity queries use GROQ in dedicated query files under `/lib/sanity/queries/`.
- All user-facing copy lives in Sanity or in JSON data files. No hardcoded marketing strings in components.
- Image alt text is required, not optional. Lint rule enforces it.
- Affiliate URL transformation lives only in `lib/affiliate-url.ts`. Never inline the replace.
- HTML entity decoding lives only in `lib/categories.ts`. Never decode in components individually.
- The site-search index is **hybrid** (M5-507 follow-up). The **static bulk** (slow-changing Geiger data: 22,180 categories + ~7,955 products + brands) is generated at build time by the **`prebuild`** script (`pnpm build:search-index` → `public/search-index.json`); it runs automatically before `next build` (the Vercel build command) and makes NO Sanity calls. Running `next build` directly skips it and ships a stale bulk file. The **live delta** (editor-managed Sanity content that turns over between deploys: blogs, videos, custom categories, custom products, and answered FAQs — `faq` type, M5-506) is served by the ISR route [app/api/search-index/route.ts](app/api/search-index/route.ts) — 1-week `revalidate` floor, refreshed within seconds of publish by the Sanity webhook (`revalidatePath('/api/search-index')`). The client ([lib/search/load-index.ts](lib/search/load-index.ts)) fetches both and merges + de-dupes (Sanity-first). Matching stays **client-side Fuse** — the only runtime fetch is to our own ISR route serving a few hundred Sanity docs; still never Searchspring (or any external search service) at runtime. See M5-502 and `scripts/search-index/README.md`.
- Mega menu now renders from the Sanity `megaMenu` singleton via `getMegaMenu()` ([lib/sanity/queries/mega-menu.ts](lib/sanity/queries/mega-menu.ts)); the header no longer reads `lib/nav-data.ts` at render time (M5-503). `lib/nav-data.ts` stays as the **seed reference** — it still builds departments from PI's own slug universe (`data/pi-urls/category-urls.json` + `data/mappings/pi-to-geiger.json`, not Geiger's tree) and exports `SIMPLE_NAV`; `pnpm seed-mega-menu` serializes both into the singleton so the seeded menu is byte-for-byte the previous hard-coded one. After seeding, the menu is Sanity-managed (reorder/rename/hide/add in Studio), not auto-derived from data files. Column labels still mirror Geiger's top-level departments; items/links use PI slugs only; Tradeshow & Events stays a non-clickable header (no PI department-level slug).
- Commit messages follow Conventional Commits.

## 18. Never Do

- Do not introduce 301 redirects for any URL that exists in the GA4 export.
- Do not put bulk AI-generated content into Sanity. Hybrid model only.
- Do not use the geiger.com host in any link emitted by this site. Always rewrite to the affiliate host via `lib/affiliate-url.ts`.
- Do not download Geiger images to our origin EXCEPT brand logos which are intentionally cached during Phase E.
- Do not call DeepSeek at runtime for bulk pages. Generation is build-time only. The Sanity AI button is the one runtime exception.
- Do not call the Searchspring API at runtime in production. Data is baked at build time.
- Do not use Server Actions for the lead form. Use a Route Handler.
- Do not add e-commerce features (cart, checkout, inventory).
- Do not add user accounts or authentication.
- Do not add tracking scripts beyond GA4 unless Patrick approves in writing.
- Do not block the main thread with the search index.
- Do not index paginated category pages (page 2+). Add `noindex,follow` + canonical to page 1.

## 19. Definition of Done

A page or feature is done when:

- TypeScript compiles with zero errors
- Lighthouse mobile score is 90 or above for Performance, Accessibility, Best Practices, SEO (root templates) or 85 plus (facet pages)
- All images have alt text
- All meta tags are present and unique
- Internal links resolve, no 404s in the build report
- Screenshots from staging match the reference layouts in `/docs/references/`
- Patrick has signed off if the page is in his review list

## 20. References

- Reference category layout: `/docs/references/category-layout.jpg`
- Reference blog layout: `/docs/references/blog-layout.jpg`
- Reference buying guide format: `https://www.perfectimprints.com/blog/buying-guide-for-stadium-seat-cushions`
- Logo: `/public/logo.svg` (brand red `#E11F1E`, ink `#231F20`)
- PI category URL list: `/data/pi-urls/category-urls.json` (22,180 valid URLs)
- PI blog URL list: `/data/pi-urls/blog-urls.json` (731 valid blog post URLs)
- Geiger source: `https://www.geiger.com/`
- Geiger Searchspring API: `https://kfx28d.a.searchspring.io/api/search/category.json`
- Geiger brand index: `https://www.geiger.com/c/shop-by-brand`
- Affiliate target host: `https://patrickblack.geiger.com/`

## 21. Open Questions Resolved

All five major architectural questions resolved as of May 15, 2026:

1. **Pagination URL pattern:** Static `/cat/[slug]/page/N`. Page 1 indexable, page 2+ noindex + canonical to page 1 (per Patrick feedback 2026-05-26).
2. **AI content depth:** Lite-on-facets. Buying-guide format on 465 root pages (400-600 words + 5 FAQs + keyword derivatives, upgraded 2026-05-26), meta plus 1 short intro paragraph on 21,715 non-root pages.
3. **Sanity AI button:** Build it. Custom Studio action on customCategory documents calling DeepSeek to auto-fill intro, buying guide, and FAQs.
4. **Per-facet membership scrape:** One Searchspring API call per facet URL (~6 hours unattended).
5. **Product detail scraping:** No. PI does not have product detail pages.

Remaining pending items (track in TASKS.md):

- Exact green hex shade confirmation (OQ-4)
- Lead form "from" address (OQ-1)
- Old site cutover timing (OQ-3)

## 22. Current Project State (Week 5)

Updated: 2026-06-17.

**Module 1 (Data Pipeline): Complete (Phase A-E + weekly F/G).**

- Phase A: 544 Geiger categories, 482 leaves
- Phase B: 7,957 unique SKUs (99.82% of Geiger's 7,971 catalog)
- Phase C: 21,715 non-root URLs processed with 4-tier recovery. 13,968 with products, 7,518 zero, 229 errors
- Phase D: 465 PI roots mapped, 0 unmapped (72 exact + 224 fuzzy + 169 manual)
- Phase E (brand logos): DONE — `data/geiger/brands.json` + `data/geiger/brand-logos/` populated; `/brands` index + `/brands/[slug]` pages live (M4-405)
- Phase F (weekly deals scrape): DONE — `scrape_deals.py` → `data/geiger/deals.json`, wired to `/deals` (M5-510). Weekly GitHub Action `scrape-deals.yml`
- Phase G (weekly new-products scrape): DONE — `scrape_new_products.py` → `data/geiger/new-products.json`, wired to `/new-products`. Weekly GitHub Action `scrape-new-products.yml`
- Phase H (weekly rush-products scrape): DONE — `scrape_rush_products.py` → `data/geiger/rush-products.json` (53 products), wired to `/rush-products` (M5-506a). Weekly GitHub Action `scrape-rush-products.yml` (Sun 23:45 UTC). Degenerate single-value facets dropped at scrape time.

**Module 2 (AI Content): Complete — all 22,180 pages generated, 465 roots on v2 buying-guide format.**

- All 22,180 category JSONs exist in `data/categories/`: 465 roots with v2 buying-guide format (`promptVersion: "root-v2"`, populated `buyingGuideHtml` + `buyingGuideH2`) + 21,715 lite non-roots (modifiers/facets/compound-facets). Full set committed in `91a4b3de`.
- Week 2 demo: 35 root pages generated. Patrick reviewed 2026-05-25 and approved content tone.
- v1 quality pass (dedup-by-Geiger-path selection, 11-entry `EXCLUDED_SLUGS`, depth-aware SKU filter with `full`/`slug-filtered`/`full-capped-60` modes, compound-noun H1 rule, `post_process_lengths()` safety net) applied to the v2 prompt as well. Zero meta-length violations across all 465 roots.
- Buying-guide format delivered: 400-600 word `buyingGuideHtml`, H2 "Custom [Category] Buying Guide", keyword derivatives (custom, promotional, branded, personalized, logo, bulk, wholesale), structured buyer-research content matching the Stadium Seat Cushions blog example. Word-count adherence is stochastic — ~one-third of pages undershoot the 400-word floor by 30-100 words. Tracked for retry-on-validation-fail loop before any future re-runs.

**Module 3 (Category Templates): All 22,180 paths live; lead form delivering in production.**

DONE:

- Routing for all 22,180 paths. Roots + modifiers + compound-facets pre-built (~1,840 paths including pagination); facets on-demand SSG via `dynamicParams=true` (Vercel build budget ENOSPC at ~34k paths, so the 21,137 facets must not be pre-built — do not widen `PREBUILD_TYPES` without re-checking).
- Production ProductCard (image, name, **`Item # <sku>` line under the name — Geiger-style, small/muted; hidden for custom non-Geiger products whose synthetic `custom-<id>` SKU is an internal id, not a catalog number**, price, MOQ, brand badge, NEW/SALE/CLOSEOUT ribbons, affiliate link via `lib/affiliate-url.ts`), with `onError` placeholder fallback for hot-linked Geiger images that 404 between monthly rebuilds. The card is shared everywhere a product grid renders (category pages, custom categories, `/deals`, `/new-products`, `/rush-products`, `/search`, blog product blocks), so the SKU shows on all of them. SKUs that contain a space (e.g. `501014 90A`, `501622 1BC`) render as-is.
- Production ProductGrid, AI content rendering (H1, intro, FAQs, hero alt), breadcrumb, CTA banner ([components/category/CTABanner.tsx](components/category/CTABanner.tsx) — red banner, hours from `globalSettings.hoursOfOperation` via the deduped `getSiteSettings()` (fallback "Monday through Friday, 9am to 5pm EST" when blank; hardcoded until 2026-08-05), a "Call 800-773-9472" button + an **"Email Us" button linking to `/contact`**; the plain email address was removed 2026-06-27, M5-514), 404 page. The bottom **footer SEO text block** ([app/page.tsx](app/page.tsx), `home.textContent`) renders **full content width** (`prose max-w-none`, not the old narrow `max-w-3xl`) with **brand-red, hover-underline hyperlinks** (M5-514).
- Mega menu (`lib/nav-data.ts`) rebuilt 2026-06-01 to read from PI's slug universe. Items grouped under Geiger top-level departments (Apparel, Bags & Totes, etc.) for visual familiarity; all 465 root links resolve. Column headers link to their PI department-equivalent root (`apparel`, `bags`, `drinkware`, `health`, `household`, `office`, `outdoor`, `writing`, `products` for Shop By); Tradeshow & Events column header is non-clickable because PI has no department-level slug for it. **Replaced by Sanity-driven menu in M5-503 (2026-06-17):** the header renders from the `megaMenu` singleton; `lib/nav-data.ts` is retained only as the `pnpm seed-mega-menu` reference.

**PATRICK FEEDBACK FROM WEEK 2 DEMO** (scheduled across Weeks 3-5):

Week 3:

- ✅ HTML entity decoding bug in product titles (`&amp;`, `&quot;`) — fixed at loader level in `lib/categories.ts` via `lib/text-utils.ts::decodeHtmlEntities`
- ✅ Image fallback when Geiger image 404s — `onError` handler in `components/category/ProductImage.tsx` swapping to `/public/placeholder-product.svg`
- ✅ H2 "Custom [Category] Buying Guide" above bottom text — `buyingGuideH2` field populated in all 465 root JSONs; H2 rendered in `app/cat/[...slug]/page.tsx` on root pages only
- ✅ Buying-guide format content (longer, keyword derivatives) — `root_category.txt` v2 prompt landed; all 465 roots have `promptVersion: "root-v2"` and populated `buyingGuideHtml`. Word-count adherence is stochastic (~one-third of pages under target by 30-100w); add retry-on-validation-fail loop before any future re-runs.
- ✅ Full 22,180-page content generation — 465 roots (v2 buying-guide) + 21,715 lite non-roots all in `data/categories/`
- ✅ Pagination with noindex on page 2+ — M3-306 done 2026-05-31. `/cat/[slug]/page/N` URLs with 60 products per page, Prev/Next/numbered nav with adjacent-page prefetch, page 1 canonical to clean URL, pages 2+ emit `noindex,follow` + canonical back to page 1, `/page/1` 308-redirects to clean URL, out-of-range → 404. Sitemap at `/sitemap.xml` (22,921 URLs: 10 static + 22,180 category page-1 + 731 blog) excludes paginated variants entirely. First Vercel deploy hit `ENOSPC` at 34,857 static paths (Next.js 16 segment-artifact explosion); mitigated by switching facet pages to on-demand SSG — see Section 3 and Section 13.

Week 4:

- Filter sidebar with new "Minimum Quantity" filter (Patrick's addition)
- "Search within this category" input above filter sidebar
- Context-specific filters (apparel/drinkware/tech show extra filters when relevant)
- Brand logos scrape (Phase E)
- Brands tab in main menu + `/brands` index + `/brands/[slug]` per-brand pages
- ✅ Related Blogs section ("Related Blogs About [Category Name]" H2 with up to 8 related blog cards) — wired into root category pages in commit `b33f8333` (2026-06-10). Server-rendered from `relatedCategorySlugs`. 327 of 645 published blogs have at least one mapping; rest hide the section gracefully when zero matches.
- ✅ Lead capture form (M3-308) — `components/forms/LeadForm.tsx` + `LeadFormModal.tsx` post to `/api/leads`, which sends via Gmail SMTP (`lib/email/gmail-smtp.ts`) and writes a `leadSubmission` doc to Sanity (non-fatal if Sanity is down). Honeypot + 5/IP/hr rate limit. Delivering in production as of 2026-06-17 (`GMAIL_APP_PASSWORD` set in Vercel).
- ✅ Lead form file uploads + auto-detect CAPTCHA (Part 7, 2026-06-28). The lead form (contact page + category modal) now takes **optional logo/artwork attachments** (up to 3 files, ≤10MB each / ≤20MB total; `.pdf/.png/.jpg/.jpeg/.gif/.svg/.ai/.eps`; validated client + server). The route switched from JSON to **`multipart/form-data`** (`request.formData()`); files are emailed to Patrick as Nodemailer attachments AND stored on the `leadSubmission` doc's new `attachments` field as Sanity assets (non-fatal — email still sends if upload/write fails). Added a **Cloudflare Turnstile** managed/auto-detect CAPTCHA ([components/forms/Turnstile.tsx](components/forms/Turnstile.tsx)) verified server-side before sending; **no-ops without `NEXT_PUBLIC_TURNSTILE_SITE_KEY` + `TURNSTILE_SECRET_KEY`** (form still works, warning logged), activates automatically once both are set in Vercel. Honeypot + rate limit retained. `/cat` untouched. **⚠️ Patrick: create a free Turnstile site in Cloudflare to get the two keys and add them in Vercel to enable the CAPTCHA.**
- ✅ Native date pickers on ALL forms (2026-07-11, client request). Every date-entry field site-wide renders a native `<input type="date">` (calendar picker), never a free-text field: `LeadForm.dateNeeded` (contact page, category CTA modal, landing pages, blog sidebar, search-empty), `ProductQuoteForm.dateNeeded` (`/products/<slug>` Get a Quote), and the form-builder `date` fieldType ([components/forms/FormRenderer.tsx](components/forms/FormRenderer.tsx)) — keep this rule for any future form. The native input posts ISO `YYYY-MM-DD`, so all three forms convert the picked value to **MM/DD/YYYY at submit** via the shared pure `usDateFromIso()` in [lib/forms/form-def.ts](lib/forms/form-def.ts) (non-ISO/legacy values pass through unchanged) — lead emails + `leadSubmission` records stay in US format. **No Sanity schema change needed** (`dateNeeded` / `answers[].value` are plain strings) and no `/api/leads` change (it validates non-empty only). Free-text flexibility ("next month") was deliberately dropped in favor of the picker.

**Module 4 (Blogs): Complete (2026-06-15, second re-scrape pass).**

- **645 blogs published** to Sanity with current PI content (current MPower template), 86 hidden stub drafts for confirmed-deleted-on-PI URLs
- Scraped directly from PI via SeleniumBase UC mode (Cloudflare Turnstile bypass) + US-exit VPN — Wayback-based first attempt and the no-scroll second attempt were both discarded as inferior
- **Re-scrape required (2026-06-15)** because the prior scrape didn't scroll, so multi-section listicles got truncated at the first lazy-loaded product grid. The new scraper scrolls to bottom in 800px steps until DOM height stabilises (max 90s per page), filters to `data-block-type="contents"` blocks only (skipping the 12 megamenu navigation fdb-blocks at the top + the footer fdb-block at the bottom), and strips product-grid blocks (sections with 2+ anchors to `/products/` each wrapping an img — see M4-rescrape in TASKS.md). Product grids are added back as the `blogProducts` Studio editing block (M4-406, 2026-06-16) — Patrick inserts these manually post-migration.
- 100% have inline author (~33 unique authors), most have hero image (real MPower CDN), some blogs have YouTube/Vimeo video embeds preserved, ~50% have inline published+updated dates
- 818 inline body images across 645 published blogs after the throttle+retry+`/undefined/`-URL fix in commit `xxxxxxx` (2026-06-15). Earlier import was silently losing ~62% of images to parallel-upload ECONNRESETs and to a bogus skip on URLs containing `/undefined/` (MPower CDN legitimately uses that segment).
- Hero-image dedupe ran twice: asset-ref match (104 docs) for cases where the OG/social image and the body's first image were the same Sanity asset, then a position-based pass (~157 docs) for the common MPower pattern where the OG image is a system-generated `_1200_1200_*.jpg` thumbnail and the body image is the full-resolution descriptive filename — same visual image, different bytes → different Sanity assets, but the dedupe script (`pnpm dedupe-header-images`) drops the first body image when a `headerImage` exists and a body image is found in the first 6 blocks.
- BlogBody renderer drops empty paragraph blocks (PI's Froala editor inserts `<p><br></p>` between every heading + image + paragraph as spacers — rendering them as `<p class="mt-5">` was creating big vertical gaps not present in PI's original look). Paragraph margin tightened `mt-5` → `mt-3`.
- Blog templates live: `/blog`, `/blog/[slug]`, `/blog/cat/[slug]` + pagination variants, vertical sticky social share bar, sidebar with categories + popular links + LeadForm CTA
- Related Blogs section live on root category pages (M3-311)
- M4-406 (2026-06-16): editor controls added on `blogPost`. (a) `blogProducts` body block — insertable anywhere in a blog body, renders a row of `ProductCard`s with live SKU-backed data + manual-card fallback (mirrors the legacy MPower in-body product grids that were stripped during the 2026-06-15 re-scrape). (b) `relatedBlogs` reference array — when populated overrides the "See Related Blogs About …" section under the post in editor-chosen order; when empty, auto-derives by shared category slugs. Schema field `relatedPosts` (fetched but never rendered) was renamed to `relatedBlogs` in the process.
- Raw scrape JSONs archived outside repo at `~/Documents/perfectimprints-archive/blogs-snapshot-2026-06-15/` (16 MB, 645 JSONs). The 2026-06-10 archive is kept for the older Wayback+no-scroll baseline but the 2026-06-15 snapshot supersedes it for any re-import. List of 86 deleted/unrecoverable URLs retained at `data/blogs/.failed-slugs.txt` for delivery handoff. See M4-401 and M4-402 in TASKS.md for the full story.

Week 5:

- ✅ Home page (M5-501, 2026-06-17). Built from the `homePage` Sanity singleton: hero, value pillars, new-products rail, testimonials, brands strip, blog preview, free-text section, CTA banner — all server-rendered; Sanity wins when the singleton is populated. The six featured-category image blocks component (`components/home/FeaturedBlocks.tsx`, fed by `getHomePage().featuredBlocks` with hard-coded fallbacks) is built and ready but currently commented out in `app/page.tsx` — Patrick will enable it when wanted.
- ✅ Home content editable + Patrick's copy (M5-501 Part 1, 2026-06-27). The text hero is now **editable in Studio** via the new `homePage.heroText` group (`eyebrow`/`headline`/`subheadline`) — Patrick previously couldn't find it because the hero text wasn't a discoverable field. Added editable `bannerRowHeading` (H2) + `bannerRowSubheading` rendered above the banner row. **Value Pillars `body` changed plain-string → portable text** so hyperlinks work (was rendering raw code when Patrick tried to link a pillar); links render brand-red, external in a new tab. Home meta title/description set to Patrick's copy (`Custom Promo Products & Branded Apparel by Perfect Imprints` / `Custom promotional products & branded apparel for bulk B2B orders. Shop 22,000+ trending promo items with free art proofs and rush options.`), self-canonical intact. Seed/migrate with `pnpm seed-home-content` (idempotent; fills hero/banner-row blanks only, migrates legacy pillar strings to portable text preserving text). No `/cat` changes; LCP stays text-bound.
- ✅ Home interactive sections (M5-501 Part 6, 2026-06-28, "not critical for launch"). (1) **Fixed the Rush value pillar that rendered literal `<a href="/rush-products">…</a>` code** — `pnpm fix-pillar-links` ([scripts/migrations/fix-pillar-inline-links.ts](scripts/migrations/fix-pillar-inline-links.ts)) rewrites pasted literal-HTML anchors in any `valueProps[].body` into proper portable-text `link` marks; ran it live (fixed the Rush pillar → "24 hour rush promos" is now a real red link) and re-ran to confirm idempotent. (2) **Value Pillars rotate when >3** — shared [components/home/PillarCard.tsx](components/home/PillarCard.tsx); ≤3 → static row (unchanged), >3 → client carousel [ValuePillarsCarousel.tsx](components/home/ValuePillarsCarousel.tsx) (3 per view desktop / 1 mobile, snap-scroll + prev/next + reduced-motion-aware auto-advance). `valueProps` validation relaxed to min-1. (3) **Testimonials → 3-up carousel + editable heading** — new `homePage.testimonialsHeading` (default "What Our Customers are Saying"); [components/home/Testimonials.tsx](components/home/Testimonials.tsx) is now a client carousel (3 per view desktop / 1 mobile, prev/next + swipe + auto-advance, dark styling kept). (4) **Rush Products rail on home** — new `homePage.rushProductsHeading` (default "Rush Production Promotional Products"); reuses `NewProductsRail` (now props-parameterized for subtitle/view-all/background), fed by new `getRushProducts(12)` in [lib/rush-products.ts](lib/rush-products.ts) from the existing `data/geiger/rush-products.json` (no new scrape), placed directly after the New rail, affiliate links via `ProductCard`. Home stays `force-static`; `/cat` untouched.
- ✅ Deals main menu button + `/deals` aggregator page (M5-510, 2026-06-13). "Promotional Products" removed from header nav, "Deals" added after Rush Products. `/deals` is fully static (`force-static`) with a Geiger-style filter sidebar (Category, Color, Price, Production Time, Brand, Min Qty, Material, Refine By, Ounces, Full Color, New Items) — all filtering + pagination is client-side (no URL params, no server roundtrips). Data sourced from `data/geiger/deals.json` produced by the weekly Phase F scrape (see Section 16). Hero copy + SKU blocklist editable via `globalSettings.dealsPage` in Sanity.
- ✅ New Products page + Phase G weekly scrape — `/new-products` aggregator mirrors `/deals`, sourced from `data/geiger/new-products.json` (`scrape_new_products.py`, weekly `scrape-new-products.yml`). Hero copy + SKU levers via `globalSettings.newProductsPage`.
- ✅ Rush Products page + Phase H weekly scrape (M5-506a, 2026-06-18) — `/rush-products` aggregator clones `/new-products`, sourced from `data/geiger/rush-products.json` (`scrape_rush_products.py`, weekly `scrape-rush-products.yml` Sun 23:45 UTC) for Geiger's `Home > Shop By > 24 Hour Rush Products` (~53 products). `force-static`, client-side filters + pagination, breadcrumbs + BreadcrumbList schema. No NEW/SALE/CLOSEOUT ribbons (rush feed has no badges). Degenerate single-value facets dropped at scrape time. Hero copy + SKU levers via `globalSettings.rushProductsPage`; custom products via `customProduct.placements.onRush`. The "Rush Products" header nav item now points at `/rush-products` (was `/rush-promotional-products`); update `lib/nav-data.ts` then re-run `pnpm seed-mega-menu` (or patch the `megaMenu` singleton item) to push it live.
- ✅ Custom + pinned editorial levers on `/deals`, `/new-products`, and `/rush-products` (M5-511 + M5-506a). Three levers per page without touching the scraper: hide a scraped SKU (`hidden*Skus[]`), pin any existing Geiger SKU (`pinned*Skus[]`), or add a fully custom non-Geiger product (`customProduct` with `placements.onDeals`/`placements.onNewProducts`/`placements.onRush`). Augmentation pipeline: `lib/products/augment.ts` + `lib/products/lookup.ts`, orchestrated by `getAugmentedDealsData()` / `getAugmentedNewProductsData()` / `getAugmentedRushProductsData()`.
- ✅ Services pages as an editable page-builder (M5-506b, 2026-06-18). New generic `page` Sanity document type — ordered `sections[]` of polymorphic, individually-hideable section objects (heroBanner, richText, imageText, infographic, iconFeatures, statBanner, cardGrid, ctaBlock, eventList, faqAccordion) — gives Patrick website-builder behavior (reorder / insert / delete / hide) in Studio. Rendered by slug via `SectionRenderer` ([components/page-sections/](components/page-sections/)) at the existing Services dropdown routes (`/services/kitting`, `/services/company-stores`, `/services/popup-stores`, `/services/custom-products`) — no new routes, no nav changes. `dynamicParams=true`, `revalidate=false`, webhook revalidates `/services/<slug>` on publish. Four pages seeded as **drafts** (`pnpm seed-service-pages`) with the section structure/order of the reference layouts but **original placeholder copy + empty image slots** — the source-site marketing copy and infographics were intentionally NOT reproduced; Patrick fills copy/images and publishes. The `page` type is generic and will also power About/Privacy/Terms/Contact (later prompt). Routes added to `app/sitemap.ts`; each page emits title/meta/canonical + BreadcrumbList schema.
- ✅ Videos section (M5-507, 2026-06-21). `/videos` index (card grid + blog-style **client-side** category filter over the shared `blogCategory` taxonomy, newest first) and `/videos/[slug]` detail (responsive player + related videos in the same category + VideoObject JSON-LD). The `video` schema was generalized from YouTube-only to a single `embedUrl` (provider auto-detected) + optional custom `thumbnail`. Embed parsing in [lib/video/embed.ts](lib/video/embed.ts) handles YouTube watch/`youtu.be`/Shorts, Vimeo, Instagram reel/post, and Facebook video/reel, returning `{ provider, embedSrc, aspect }` (Shorts/reels 9:16, rest 16:9); rendered by client [components/videos/VideoEmbed.tsx](components/videos/VideoEmbed.tsx). YouTube/Vimeo embed cleanly; Instagram/Facebook are best-effort (recommend a custom thumbnail + per-link testing). Thumbnail priority: custom → YouTube auto → placeholder. On-demand SSG (`dynamicParams=true`, `revalidate=false`); webhook revalidates `/videos` + `/videos/<slug>` on publish. Detail URLs added to `app/sitemap.ts`. `video` titles added to the search index (`collectVideos()` in the prebuild) + the overlay/`also-matching` groups. Query helpers in [lib/sanity/queries/videos.ts](lib/sanity/queries/videos.ts); `video` is already in the nav (`/videos`). No seed data — Patrick adds videos in Studio.
- ✅ Hybrid search — instant freshness for Sanity content (M5-507 follow-up, 2026-06-21). Search index split into two layers so newly-published Sanity content is searchable without a full rebuild. **Static bulk** (`public/search-index.json`, prebuild): Geiger categories + products + brands only — no Sanity calls, cacheable for a deploy lifetime. **Live delta** ([app/api/search-index/route.ts](app/api/search-index/route.ts)): blogs + videos + custom categories + custom products, assembled by [lib/search/sanity-index.ts](lib/search/sanity-index.ts) (`buildSanitySearchItems()`) from [lib/sanity/queries/](lib/sanity/queries/) (`getAllBlogSearchEntries`, `getAllVideoSearchEntries`, new `getCustomCategorySearchEntries` + `getCustomProductSearchEntries`). The route is ISR with a **1-week `revalidate` floor** (the requested auto-refresh) and is **refreshed within seconds of publish** by the Sanity webhook ([app/api/sanity/revalidate/route.ts](app/api/sanity/revalidate/route.ts) → `revalidatePath('/api/search-index')` for `blogPost`/`video`/`customProduct`/`customCategory`/`curatedCategory`, shared path constant in [lib/search/constants.ts](lib/search/constants.ts)). Client ([lib/search/load-index.ts](lib/search/load-index.ts)) fetches both, merges + de-dupes by `type+url` (Sanity-first so a custom category overrides a bulk slug); the live delta is best-effort (search still works on the static bulk if it's briefly down). Matching stays client-side Fuse — no runtime Searchspring. **Rendering gap closed too:** `/deals`, `/new-products`, `/rush-products` switched from `force-static` to ISR (`revalidate` 1 week + the same webhook), so custom products / pins / hides surface without a rebuild. Custom categories/blogs/videos already rendered from Sanity. Net: add anything in Studio → page live immediately + searchable within seconds, weekly auto-refresh as the safety net.
- ✅ Mega menu fully Sanity-driven (M5-503, 2026-06-17). Header reads the `megaMenu` singleton via `getMegaMenu()` ([lib/sanity/queries/mega-menu.ts](lib/sanity/queries/mega-menu.ts)) and renders the existing components (`ShopByMegaMenu` cascade, `AllCategoriesPopover` grid, `SimpleNavDropdown`, `MobileDrawer`) unchanged, so the live menu is visually/behaviorally identical to the prior hard-coded one. Singleton seeded byte-for-byte from `lib/nav-data.ts` (departments + `SIMPLE_NAV`) via `pnpm seed-mega-menu`. Schema extended with `kind`/`variant`/`columns`. Reorder/rename/hide/add/delete in Studio reflects live — the Sanity webhook handler (`app/api/sanity/revalidate/route.ts`) verifies the HMAC signature and `revalidatePath('/', 'layout')` for `megaMenu`/`globalSettings` changes. No hard-coded fallback. **✅ Webhook status (2026-07-01): the GROQ webhook is now created on BOTH environments — STAGING (`dev.perfectimprints.com`) and PRODUCTION (`www.perfectimprints.com`), each with its `SANITY_WEBHOOK_SECRET` in the matching Vercel env. Revalidations (mega menu, global settings, home, services pages, search/blog/video/custom-product/category) now fire in seconds on publish on both. Setup steps: [docs/sanity-webhook-setup.md](docs/sanity-webhook-setup.md) / TASKS.md M5-512.**
- ✅ Site-wide search (M5-502 + M3-309, 2026-06-19). Build-time Fuse.js index → `public/search-index.json` (`{ generatedAt, items }`), built by `scripts/search-index/build-index.ts` as the **`prebuild`** step before `next build`. **As of the M5-507 hybrid split this static file holds ONLY the Geiger bulk — ~30,340 entries: 22,180 categories + ~7,955 products + 205 brands (541 KB gzipped).** Blogs, videos, custom categories, and custom products moved to the LIVE delta route (see the hybrid-search bullet below / Section 17). Entry shape is minimal `{ type, title, url, brand?, category?, image?, sku? }` — products carry name + brand + raw `geiger_url` (+ a thumbnail `image` for the overlay, added in M5-502b; + the **item number as a `sku` search key**, P2 batch 2 2026-07-09 per Patrick's request — no description) and their results link STRAIGHT to the affiliate host (via [lib/affiliate-url.ts](lib/affiliate-url.ts), new tab), never a category page. **SKU search (P2 batch 2):** every Geiger product's real item number (spaces preserved, e.g. "501014 90A") is indexed in the static bulk; `productPage` docs get an optional **`sku` "Item / SKU number" field** (Basics fieldset — a REAL number Patrick enters; the synthetic `custom-<id>` ids of customProduct/productPage are deliberately NEVER indexed) flowing through the live delta. Fuse key `sku` weight 0.5 in BOTH the overlay index ([lib/search/load-index.ts](lib/search/load-index.ts)) and the server `/search` Fuse ([lib/search/server-search.ts](lib/search/server-search.ts) over `GeigerProduct.sku`) — numeric queries barely match titles so SKU hits dominate naturally; word queries don't hit the key at all. Index grew ~122 KB raw → 4.23 MB / 571 KB gzipped, far under the 2 MB warn budget. Result routing unchanged (affiliate vs internal `detailUrl`). Answered FAQs are searchable too — they live in the live delta and link to the `/faq` library (M5-506). The header input ([components/forms/SearchBox.tsx](components/forms/SearchBox.tsx)) opens a lazy autocomplete overlay: Fuse.js (dynamic `import`) + the index (fetch) load only on first search, so initial route JS is unaffected ([lib/search/load-index.ts](lib/search/load-index.ts): `loadSearchIndex()` + `search()`, keys weighted title 0.8 / brand 0.2, `threshold 0.32`, `ignoreLocation`). Results are grouped by type (Categories/Products/Brands/Blogs/Videos), arrow-key + Enter nav, Escape/outside-click close. `/search?q=` ([app/search/page.tsx](app/search/page.tsx), `noindex`) is a product-first faceted results page (see M5-502b below). No-results never dead-ends — it shows a lead-form CTA ([components/search/SearchEmptyCTA.tsx](components/search/SearchEmptyCTA.tsx), reuses `LeadFormModal`). No external search service; `app/api/search/route.ts` stays a 501 stub (reserved for a possible future live Searchspring proxy, out of scope). New shared loaders: `getAllProducts()` ([lib/categories.ts](lib/categories.ts)), `getAllBlogSearchEntries()` ([lib/sanity/queries/blogs.ts](lib/sanity/queries/blogs.ts)).
- ✅ Geiger-style faceted search results + grouped overlay (M5-502b, 2026-06-19). `/search` is now a **product-first faceted page** (mirrors Geiger's `/search`): matched products are resolved server-side from the full catalog (`searchProducts()` in [lib/search/server-search.ts](lib/search/server-search.ts), a cached Fuse over `getAllProducts()` — the 9 MB catalog never reaches the client), facets built by [lib/search/build-facets.ts](lib/search/build-facets.ts) drive the existing `/deals` filter sidebar + `ProductGrid` + pagination + a Sort control ([SearchFacetedResults.tsx](components/search/SearchFacetedResults.tsx)). **Facets are limited to Category / Price / Brand / Min Qty** — the only filterable attributes ON the product object; Color/Material/Production Time live only in per-category Searchspring facet arrays (not per-SKU), so they can't be derived for an ad-hoc query without a runtime API. A client-side "Also matching" strip ([SearchAlsoMatching.tsx](components/search/SearchAlsoMatching.tsx)) surfaces matching categories/brands/blogs above the grid. The header overlay now **groups** results (Categories/Products/Brands/Blogs/Videos) with **product thumbnails** + a "See all N results" footer — this added `image` (decoded `imageUrl`) to product index entries (index 563.7 KB gzipped, still well under budget). **Root-category promotion:** `search()` runs a second Fuse over only the ~465 root pages and promotes the best root to the front (gated by score ≤ 0.25 + a word-boundary title match) so a "main" category (e.g. `/cat/water-bottles`) outranks its own modifier/facet children — without hijacking modifier-specific queries ("closeout beer accessories"); root-ness is derived from the `/cat/<slug>` URL shape, no index change. Still zero runtime Searchspring.
- ✅ Instant-navigation loading skeletons (2026-06-20). Added route-level `loading.tsx` for `/cat/[...slug]`, `/search`, and `/brands/[...slug]` (shimmer skeletons in [components/ui/Skeleton.tsx](components/ui/Skeleton.tsx) mirroring each layout). Without a `loading.tsx`, the App Router holds the **old** page frozen until the server render finishes (worse on the on-demand-SSG `/cat` facets and the dynamic `/search`), and automatic `<Link>` prefetch of a dynamic route prefetches nothing — so navigation felt "stuck on click" even in production. The skeletons make navigation transition instantly + show a placeholder while data streams, and they also make `<Link>`/`router.prefetch` effective for the dynamic routes. Search-overlay rows additionally `router.prefetch()` their internal target on hover/focus ([useResultNavigation.ts](components/search/useResultNavigation.ts) → `prefetch`). Perceived-perf only — it does NOT change the on-demand-SSG strategy (that's a forced Vercel build-budget constraint, see Section 13).
- ✅ Mobile Pagespeed improvement (LCP + Speed Index) — M5-508 Part 1 baseline + **Part 8 PageSpeed pass (2026-06-28)**. Part 8 addressed Patrick's red PageSpeed items conservatively, without regressing the M5-508 LCP work or the static `/cat` render: (1) **preconnect + dns-prefetch** for the Geiger image CDN `imgsirv.geiger.com` (and GTM when configured) in [app/layout.tsx](app/layout.tsx) so product images (the category LCP candidate) connect earlier; (2) **Turnstile script → `lazyOnload`** ([components/forms/Turnstile.tsx](components/forms/Turnstile.tsx)) and the **lead-form modal lazy-loaded via `next/dynamic` (`ssr:false`, mount-on-open)** in [EmptyStateCTAButton.tsx](components/category/EmptyStateCTAButton.tsx) + [SearchEmptyCTA.tsx](components/search/SearchEmptyCTA.tsx) — keeps LeadForm + Turnstile + file-validation out of the **static `/cat`** initial bundle and the search overlay (Turnstile no longer ships on form-less pages); (3) **home carousels lazy** (below-the-fold, ssr:false) via [TestimonialsLazy.tsx](components/home/TestimonialsLazy.tsx) + [ValuePillarsCarouselLazy.tsx](components/home/ValuePillarsCarouselLazy.tsx); (4) **forced-reflow fixes** — rAF-batched reads + `ResizeObserver` in [ValuePillarsCarousel.tsx](components/home/ValuePillarsCarousel.tsx) + [Testimonials.tsx](components/home/Testimonials.tsx); (5) **modern `browserslist`** in [package.json](package.json) (`chrome 64 / edge 79 / firefox 67 / opera 51 / safari 12`) to drop legacy polyfills; (6) **`<select>` `aria-label`s** on the category + `/search` sort dropdowns. Product images already request the 275px webp grid variant (no upscale) with width/height + lazy/eager — verified, no change. The mega menu stays SSR'd for nav-link crawlability. `/cat` stays `●`/SSG (baseline build confirmed 1,840 prebuilt paths; the modal split is client-side, adds no `searchParams`/uncached fetch). `pnpm typecheck` clean; full `pnpm build` deferred to Vercel (local builds don't run cleanly on Patrick's machine). See TASKS.md M5-508 Part 8.
- ✅ SEO follow-ups + Sanity footer columns (M-SEO3 follow-up, 2026-06-28). (1) **Breadcrumb JSON-LD uses absolute `item` URLs** and is emitted **once** by the shared `<Breadcrumbs>` component (fixed Google Rich Results' "Invalid URL in field 'id'"); removed the duplicate `breadcrumbSchema` emission from `CustomCategoryView`. (2) **Geiger product `imageUrl`s decoded** (`&amp;` → `&`) at the product loaders (`lib/categories.ts` `loadProductsIndex`, `lib/products/lookup.ts`, `lib/brands.ts`) so `og:image` / `twitter:image` / ItemList `image` are clean and social previews resolve; rendered grids unchanged. (3) **Footer nav columns now Sanity-driven** via `globalSettings.footerColumns` (wired into `getSiteSettings()` + `Footer.tsx`), with the hardcoded `NAV_COLUMNS` as fallback; seeded idempotently via `pnpm seed-footer-columns` (ran 2026-06-28). No `/cat` data-fetch/render changes.
- ✅ SEO schema + meta + Open Graph pass (M-SEO3, 2026-06-27). (1) **Category meta title = the page H1** (full phrase; brand suffix only when it fits) via `categoryMetaTitle()` — canonicals/H1 untouched. (2) **CollectionPage + ItemList** JSON-LD added to category pages (baked + customCategory); ItemList lists the products shown (omitted for CTA-only categories); FAQPage (root + custom w/ FAQs) and BreadcrumbList retained. (3) **Complete Open Graph + Twitter on every page** via the shared `socialMeta()` helper ([lib/seo/open-graph.ts](lib/seo/open-graph.ts)) — category `og:image` = first product image, logo fallback when empty; blog hero / brand logo / video poster elsewhere; always sets site_name + image:alt. (4) **Organization (local) schema restricted to home + contact** — removed from the root layout (was on every page), now rendered only by [app/page.tsx](app/page.tsx) + [app/contact/page.tsx](app/contact/page.tsx); WebSite + SearchAction stays site-wide. No `/cat` data-fetching / Suspense / `loading.tsx` changes — route stays static.

**Already built (just confirming to Patrick):**

- Sale ribbon on product image (top-right corner) — will be visible on closeout pages once full generation completes

**Infrastructure state:**

- GitHub repo: `raoalihamza/perfectimprints` (linked to Vercel)
- Vercel deployment live at `dev.perfectimprints.com` (staging)
- Sanity Studio at `localhost:3333` and `/admin3773752` (the embedded Studio path was obfuscated from `/admin` in M5-506 to cut bot/scanner noise — light obfuscation only, Sanity auth still gates it; `basePath` in [sanity/sanity.config.ts](sanity/sanity.config.ts) + the route folder + the `ChromeGate` check all use the new path). Project ID `ii96lcy9`.
- DeepSeek API key on Patrick's account, working
- Affiliate URLs link to `patrickblack.geiger.com` but that subdomain isn't active yet on Geiger's side — Patrick is chasing them

**Schedule note:** Eid-ul-Adha holiday on Wednesday May 27 and Thursday May 28, 2026. Ali offline those days. Active work resumes Friday May 29.

**Large data files note:** `products.json` (9.6 MB) and `facet-memberships.json` (44.5 MB) currently live in the main repo. Relocation tracked as M5-509.

---

## 23. Phase 2 Scope (Post-Launch Paid Engagement, from 2026-07-04)

Phase 1 (the 22,180-page SEO site) is live in production and stable. Phase 2 is a separate paid engagement ($5,500, weekly payments). Full scope, built in this order per Patrick:

1. **AI Content Engine (build first): Blogs, then Videos, then Pages, then Landing pages.** A shared foundation (DeepSeek generation service, related-products matcher, internal-linking engine, schema emitter) is built once and reused by all four. See `docs/phase-2-ai-engine.md`.
   - **AI Blog:** long-form 1,500 to 2,000 words, two templates (list-style with a product strip per idea; single-category focus), related products by category + manual, internal links (4 to 5 per post), draft-for-review, generate-from-title. Target ~1 blog/day.
   - **AI Video tool (added post-deal, free):** paste script + link, generate video title/meta title/meta description/500 to 750-word description, suggest internal links + related products, emit VideoObject schema.
   - **AI Pages:** generate-with-AI in the page builder from a title.
   - **AI Landing pages:** fixed template (hero/trust/problem/options/why/form/FAQ), AI-researched local landmarks by city, keyword box to steer product matching, per-page lead form, top 10 + self-serve generator.
2. **Custom Product Pages:** `/products/<slug>` detail pages with up to 5-column tiered pricing, up to 10 images (zoom + thumbnails), optional video, related-products carousel (auto by category + manual), a "Get a Quote" form (emails Patrick + customer confirmation + lead record), Product schema + sitemap, and spreadsheet bulk upload (Google Sheet/CSV, ~50 at a time, update-by-SKU). **Parts 1 + 2 SHIPPED 2026-07-08 (P2-CP-001 + P2-CP-002)** — the `productPage` type, the `/products/<slug>` detail page (color-variant gallery, tiered pricing, sale badge, sizes-as-list, description, related carousel), New-Products/search/carousel surfacing, Product JSON-LD, sitemap, webhook case, AND the dedicated Get a Quote form with per-product `leadRecipient`, customer confirmation, and quote-field lead records (see Section 7 productPage + leadSubmission; ⚠️ manual webhook Filter addition required). **Bulk upload SHIPPED 2026-07-10 (P2-CP-003)** — a top-level Studio **"Bulk Upload" tool** ([sanity/tools/bulk-import-tool.tsx](sanity/tools/bulk-import-tool.tsx)) imports/updates up to **50 productPages per CSV/Excel upload**: download the committed template ([public/templates/product-pages-template.csv](public/templates/product-pages-template.csv)) → upload → **dry-run Preview** (per-row CREATE/UPDATE/ERROR + unknown-column/unknown-category-slug warnings, zero writes) → **Apply** (batches of 5 rows, live progress). Title is the only required column; blanks never overwrite; multi-value fields use numbered columns (`Size N`, `Tier N Qty`+`Tier N Price`, `Decoration N`+` Upcharge`, `Image N`, `Color N Name`/`Images`/`Swatch`); image cells are URLs **fetched server-side and uploaded as Sanity assets** (SSRF-guarded, timeout/size-capped, content-hash deduped — never hot-linked). **Upsert by slug** (explicit `Slug` column else slugified Title; draft OR published match → the draft is patched, published-only → draft copied first) so re-uploads update, never duplicate. **DRAFTS only** — Patrick publishes himself, so no webhook/tag/env change (publishing fires the existing productPage case). Pure parser [lib/bulk-import/parse.ts](lib/bulk-import/parse.ts) (SheetJS `xlsx`, now a prod dependency; 12 vitest tests) + pure doc builder [lib/bulk-import/build-doc.ts](lib/bulk-import/build-doc.ts) (schema-exact `_key`ed items); route [app/api/sanity/bulk-import/route.ts](app/api/sanity/bulk-import/route.ts) (`nodejs`+`force-dynamic`, `maxDuration 300`, 4 MB file cap) authed by the Site Refresh cookie-session nonce scheme factored into the shared [lib/sanity/studio-nonce-auth.ts](lib/sanity/studio-nonce-auth.ts) (own doc `drafts.bulkImportAuth` + `x-import-nonce` header). Unknown `Add To Categories` slugs warn — the import never auto-creates categories. Guide section 23; full write-up in TASKS.md P2-CP-003.
3. **Reusable form builder + 4 service forms** (Kitting, Company Stores, 100% Custom Products, Pop-Up Stores) opened from "Request a Quote" buttons; lead records; editable recipients. **SHIPPED 2026-07-11 (P2-FB-001 + P2-FB-002)** — the `form` doc type + generic `FormRenderer`/`FormModal`, the generalized `/api/leads` `formSlug` branch (server-resolved recipient + server-side validation, `leadSubmission.answers[]`), CTA `formSlug` wiring on ctaBlock/heroBanner, and the four seeded service forms (`pnpm seed-service-forms`, drafts — Patrick publishes). See Section 7 **form** + TASKS.md P2-FB-001/002; ⚠️ manual webhook Filter addition (`form`) required on both environments.
4. **CTA bar** on all product-bearing category/facet pages (below products, above FAQs), auto category name, editable wording, opens the "Find Products for Me" form. **SHIPPED 2026-07-12 (P2-CTA-001)** — `globalSettings.categoryCtaBar` + [components/category/CategoryCtaBar.tsx](components/category/CategoryCtaBar.tsx); see Section 7 globalSettings + TASKS.md P2-CTA-001. Zero extra Sanity reads (rides the layout's deduped `getSiteSettings()`), no webhook change, `/cat` stays static.
5. **Geiger catalog lead pages:** long-form lead pages from the Shop By Theme catalogs, multiple CTAs, email-gated catalog link (auto-email to a valid address, cc Patrick), Shop By Theme mega-menu dropdown, AI generation for future catalogs. **Prompt 1 (P2-CAT-000, data layer) SHIPPED 2026-07-15** — the Phase I scraper + `data/geiger/catalogs.json` + the Site Refresh "Refresh Catalogs" button (Section 16 Phase I). **Prompt 2 (P2-CAT-001, the pages) BUILT 2026-07-15** — the `catalogPage` doc type + the public `/shop-by-theme/<slug>` landing page + the gated (noindex) `/shop-by-theme/<slug>/catalog` aggregator page (see Section 7 catalogPage; ⚠️ manual webhook Filter addition required). **Prompt 3 (P2-CAT-002/003, lead capture + menu) BUILT 2026-07-15** — the seeded `catalog-request` builder form opens from the three landing CTAs, submissions email the customer the gated link (cc Patrick) + record `catalogTitle`/`catalogSlug`, and `pnpm seed-shop-by-theme-menu` adds the "Shop by Theme" header dropdown (⚠️ both seeds NOT yet run against live — run + publish once catalog pages exist; no NEW webhook change, `catalogPage` was added to the Filter in prompt 2). **Prompt 4 (P2-CAT-004, AI + photos) BUILT 2026-07-15** — "Generate Catalog Page with AI" on catalogPage (editorial-only, grounded in the catalog's real product names, hard boundary like generate-product) + the `pnpm fetch-catalog-images` page-raster helper (gitignored scratch dir; external flipbooks skip). Milestone 3 code-complete; remaining manual steps: webhook Filter addition (prompt 2), run the two seeds + publish (prompt 3), and Patrick's content/photos.
6. **feeds.perfectimprints.com:** 101-page rebuild from scratch with AI-readable schema and Gushwork removed. **This is a SEPARATE project/repo/Vercel on the feeds subdomain, NOT this repo.**

**On hold (not in scope/price):** image license metadata (GSC). Quote separately if Patrick confirms.

All Phase 2 features follow the existing rules in this file: `/cat` and all routes stay static, the freshness pattern (non-CDN `cachedClient` + cache tag + webhook `revalidateTag`) applies to every new Sanity read surface, every tag value goes through `sanitizeTagValue()`, and every Studio-facing change updates `perfect-imprints-sanity-guide.html`. Full task list in TASKS.md under "PHASE 2".

### Phase 2 AI content engine (P2-AI-001, shipped 2026-07-05 with the blog system)

The shared foundation every Phase 2 AI feature (blogs → videos → pages → landing pages → catalog pages) reuses with thin wrappers — do not rebuild per feature:

- **[lib/ai/brand-voice.ts](lib/ai/brand-voice.ts)** (pure) — `BUYER_PERSONA`, the plural keyword-derivative rule, the banned-phrases list, and `brandVoiceSystemBlock()` (the shared system-prompt fragment implementing Section 24). Every feature composes this into its own system prompt. (The category route `generate-content` still carries its own inline copy; it can adopt this later.)
- **[lib/ai/deepseek.ts](lib/ai/deepseek.ts)** (server-side) — `generateJson<T>()`, the single DeepSeek wrapper (same model/endpoint/`json_object` settings as the category route, reuses `DEEPSEEK_API_KEY`). Throws typed `DeepSeekError`; callers return a clean 500/502 so generation failure degrades to manual entry, never crashes a page or a publish.
- **[lib/ai/related-products.ts](lib/ai/related-products.ts)** (server-only, disk) — `matchRelatedProducts()`: category first (the baked category's curated SKUs, keyword-token-ranked), then Sanity custom products (guarded — degrades to Geiger-only), then keyword-scored catalog top-up. Deterministic, deduped. **Relevance rules (P2-AI-002b):** generic promo words (`GENERIC_PROMO_WORDS` in brand-voice.ts: custom/branded/logo/… ) + stopwords are stripped from BOTH sides before scoring (they match everything, so they prove nothing); `minScore` (default 1 shared significant token) gates eligibility for ALL sources — a short strip is never padded with sub-threshold catalog bestsellers; `exclude: Set<sku>` supports cross-strip dedup. `resolveCategoryForKeywords(phrase)` maps a concrete product phrase ("power banks") to its best generated root category so each list-post idea pulls from its OWN product type.
- **[lib/ai/word-budget.ts](lib/ai/word-budget.ts)** (pure, P2-AI-002b, retuned P2-AI-002c) — turns an editor word-count target (clamped 1300-1900, default 1500) into concrete prompt numbers: fixed section count (`listIdeaCount` 8-12 / `singleSectionCount` 4-6), intro budget (120-180), per-section budget (`buildWordBudget`), all stated as **"at least" minimums** in the prompt ("about N" framing trended well under). "Write ~1700 words" alone is unreliable (first real run: 963); stated per-section budgets are what the model actually follows. The route enforces a dynamic thin floor of 70% of target (`THIN_FLOOR_RATIO`) — accept/reject only, it does not drive length.
- **[lib/ai/internal-links.ts](lib/ai/internal-links.ts)** (server-only) — `suggestInternalLinks()`: real targets only (published blogs via `RELATED_BLOGS_TAG` read, published pages via `PAGES_TAG` read, generated root categories from disk), scored by keyword overlap, spread across kinds. Runs ONLY inside force-dynamic generate routes → no new cache tag, no webhook change, no render surface.
- **[lib/ai/place-internal-links.ts](lib/ai/place-internal-links.ts)** (pure, P2-AI-002b) — `placeInternalLinks(bodyInput, targets, max=5)`: deterministically AUTO-INSERTS the suggested links into normal body paragraphs (never headings/lists/product strips) as schema-exact `link` annotations — anchor = longest label n-gram (must contain a significant token) found as a case-insensitive word-boundary run; first occurrence wins; one link per target/href/phrase; no overlaps; spread across paragraphs; a target with no clean anchor is SKIPPED. Patrick confirmed auto-insert (2026-07-06), replacing the previous suggest-only default.
- **[lib/portable-text/build-blog-body.ts](lib/portable-text/build-blog-body.ts)** (pure) — structured input → valid `blogPost.body` Portable Text (normal/h2/h3, bullet/number lists with `level:1`, `blogProducts` strips, unique `_key`s; `strong`/`link` annotations supported but not emitted by default). Emits no image/embed blocks — those are manual inserts.
- **[lib/seo/content-schema.ts](lib/seo/content-schema.ts)** (pure) — `buildBlogPostingSchema()` (now used by `app/blog/[slug]/page.tsx`). VideoObject deliberately does NOT live here — the canonical emitter is the pre-existing `videoObjectSchema()` in [lib/seo/schema-generators.ts](lib/seo/schema-generators.ts), which P2-AI-003 reuses as-is.

**AI Blog (P2-AI-002 + P2-AI-002b):** "Generate Blog with AI" document action on `blogPost` ([sanity/actions/generate-blog-with-ai.tsx](sanity/actions/generate-blog-with-ai.tsx), registered in [sanity/sanity.config.ts](sanity/sanity.config.ts)) → POSTs title + Studio-only AI fields (`aiTemplate` list/single, `aiTopicKeywords`, `aiPrimaryCategorySlug` via the searchable `CategorySlugInput`, `aiWordCount` — approximate target, default 1500, range 1300-1900) to [app/api/sanity/generate-blog/route.ts](app/api/sanity/generate-blog/route.ts) (`nodejs` + `force-dynamic` — reads products.json + calls DeepSeek; must never be statically evaluated) → patches a reviewable DRAFT: word-budgeted body (per-section "at least" budgets from `lib/ai/word-budget`, thin floor = 70% of target) with per-idea (list) or single product strips, meta/excerpt (word-boundary clamped), `slug`/`publishDate` only-if-empty, and `aiSuggestedLinks` (`{label, href, reason}[]`, annotated placed / not-placed). Never auto-publishes. **Product strips (P2-AI-002b):** each list idea matches its OWN `productType` (resolved to a root category via `resolveCategoryForKeywords`; `aiPrimaryCategorySlug` is a soft fallback for list posts, the primary source for single posts), a strip needs ≥2 products clearing the relevance floor or it is skipped, and one `usedSkus` set dedupes products across the whole post. **Internal links are AUTO-INSERTED** into the body via `placeInternalLinks` at the marked call site (Patrick confirmed 2026-07-06); unanchorable targets are skipped, all suggestions still recorded. **The Studio action has no fs import graph** — the assembled body comes back from the route (a Studio-bundle import of `lib/categories`/`lib/ai/*` would break the Vercel build). Offline harness: `pnpm verify:blog-engine` (46 structural/data checks — blog + video engine, no network). All four "Generate … with AI" actions (blogPost / video / customCategory / customSchema) show a shared spinner **progress dialog** while generating ([sanity/components/AiProgressDialog.tsx](sanity/components/AiProgressDialog.tsx), P2-AI-002d — the button label alone is invisible since the actions menu closes on click); closing it early only hides it, generation continues. **Workflow assumption (still standing):** one-at-a-time draft-for-review, no batch. **Conscious no-ops:** no webhook Filter/Projection change (`blogPost` already covered; the `ai*` fields incl. `aiWordCount` are Studio-only), no new cache tag, no new env var.

**AI Video tool (P2-AI-003, shipped 2026-07-06):** "Generate Video Details with AI" document action on `video` ([sanity/actions/generate-video-with-ai.tsx](sanity/actions/generate-video-with-ai.tsx), registered in [sanity/sanity.config.ts](sanity/sanity.config.ts); disabled until the Studio-only `aiScript` — pasted script/transcript — is filled) → POSTs `aiScript` + title + `aiTopicKeywords` + `embedUrl` to [app/api/sanity/generate-video/route.ts](app/api/sanity/generate-video/route.ts) (`nodejs` + `force-dynamic`, same as generate-blog) → patches a reviewable DRAFT: refined `title`, `seo.metaTitle`/`seo.metaDescription` (deep-set after `setIfMissing` so an existing `ogImage` survives), a 500-750-word `description`, a pre-filled `relatedProducts` strip (only set when ≥1 product matched — never wipes a curated list on a zero-match), `slug`/`publishDate` only-if-empty, `aiSuggestedLinks`. Never auto-publishes. **The `description` is `richAnswer`, so its Portable Text is built by [lib/portable-text/build-rich-answer-body.ts](lib/portable-text/build-rich-answer-body.ts) (normal paragraphs + strong/em + link ONLY — no headings/lists/images/product blocks, Studio silently drops illegal blocks) and the auto-placed internal links use the richAnswer link shape `{_type:'link', _key, href}` with NO `openInNewTab`** (that field doesn't exist on the richAnswer link annotation, unlike the blog body link) — `placeInternalLinks` takes `{linkShape: 'richAnswer'}` for this (default `'blog'` unchanged). **`video.relatedProducts`** is an array of the shared `blogProduct` entry (promoted from an inline object in [sanity/schemas/objects/blog-products.ts](sanity/schemas/objects/blog-products.ts) to a registered named type; stored `_type` unchanged so existing blog data is untouched), rendered on `/videos/<slug>` between the description and Related Videos by [components/videos/VideoRelatedProducts.tsx](components/videos/VideoRelatedProducts.tsx) — SKUs resolved server-side from `products.json` via `resolveProductsBySku` exactly like the blog page's strips, so **`/videos/[slug]` stays on-demand SSG** (no `searchParams`, no uncached read; `relatedProducts` rides the existing `VIDEOS_TAG` cached fetch, no new tag). Product matching is keyword/`productType`-driven via `resolveCategoryForKeywords` (the video's `category` is a `blogCategory` editorial ref, not a product category). VideoObject JSON-LD **reused as-is** (`videoObjectSchema()`; its description plain-texts the AI description automatically). **Conscious no-ops:** no webhook change (`video` already revalidates `/videos` + `/videos/<slug>`), no new cache tag, no new VideoObject emitter, no new env var.

**AI Page generation (P2-AI-004, shipped 2026-07-06):** "Generate Page with AI" document action on `page` ([sanity/actions/generate-page-with-ai.tsx](sanity/actions/generate-page-with-ai.tsx), registered in [sanity/sanity.config.ts](sanity/sanity.config.ts); disabled until `title` is filled) → POSTs title + Studio-only `aiBrief` + `aiTopicKeywords` to [app/api/sanity/generate-page/route.ts](app/api/sanity/generate-page/route.ts) (`nodejs` + `force-dynamic`, same as generate-blog/-video) → **APPENDS** (never overwrites — `setIfMissing:[]` + `insert after sections[-1]`; re-clicking appends again) a reviewable set of **TEXT-ONLY** sections to the draft: `heroBanner` (no image, `overlayText:false`, CTA → the verified `/contact`) → one `richText` per generated body section → a `productStrip` (matched via `resolveCategoryForKeywords`/`matchRelatedProducts`, skipped below a 2-product relevance floor) → `statBanner` (honest GENERIC framing, never a fabricated precise number) → `faqAccordion` (plain-text answers — the page FAQ item is a `text` field) → `ctaBlock` (button → `/contact`); plus `seo.metaTitle`/`seo.metaDescription` (deep-set, `ogImage` preserved), `slug` only-if-empty, `aiSuggestedLinks`. `title` is never overwritten; image-dependent sections (imageText/infographic/iconFeatures/cardGrid) are deliberately not generated (Patrick adds images himself). **Internal links are auto-placed** into the body paragraphs via `placeInternalLinks` in the new **`'page'` link-shape mode** (href-only — the page `portableBody` uses the DEFAULT block-editor link annotation `{_type:'link', _key, href}` with NO `openInNewTab`, structurally the richAnswer shape), and the body Portable Text is built by the pure [lib/portable-text/build-page-body.ts](lib/portable-text/build-page-body.ts) (normal blocks + bullet/number `level:1` lists + strong/em + href-only link markDefs; no images/blogProducts; section headings live in the richText section's `heading` field, not the body). Thin floor: <450 body words → clean 502 "click Generate again". **Conscious no-ops:** no webhook Filter/Projection change (`page` already covered; the `ai*` fields are Studio-only), no new cache tag (the strip rides `PAGES_TAG`/`pageTag`), no new env var. Offline harness extended to 56 checks (`pnpm verify:blog-engine`).

**AI Landing pages (P2-AI-005 part 1, shipped 2026-07-07):** the `landingPage` document type + fixed template + top-level route + "Generate Landing Page with AI" — full description in Section 7 (landingPage) and the Section 4 URL list. Engine reuse: `generateJson`/brand voice, `matchRelatedProducts`/`resolveCategoryForKeywords`, `placeInternalLinks` ('page' shape), `buildPageBody` + the new pure `buildPageSectionsBody` (multi-section body — H2s IN the body), and `suggestInternalLinks` **extended with `video` + `landing` kinds** (all four Sanity sources unified in `suggestSanityDocLinks`; the mixing loop is the pure exported `interleaveScoredSuggestions`, now href-deduped — this benefits blog/video/page generation too). New pure `findMissingLandmarks` ([lib/ai/landing-landmarks.ts](lib/ai/landing-landmarks.ts)) mechanically enforces Patrick's must-include-landmarks requirement (missing landmark → 502 retry, never silently shipped). Offline harness extended to 71 checks. Post-review hardening: the generate-video route now self-filters `/videos/<currentSlug>` (the new video link source made self-links possible); the landing schema additionally blocks the four Services slugs (a landingPage has no /services/ render path); landing `og:image` honors `seo.ogImage`; the route 502-retries on < 3 usable FAQs and the action never patches an empty `faqs`/`relatedProducts`; the landing strip passes `includeCustom: false` (SKU-only entries can't render custom products); landmark matching is word-bounded + per-segment. **⚠️ Manual step: add `landingPage` to the Sanity webhook Filter on BOTH environments** (Section 7 landingPage / [docs/sanity-webhook-setup.md](docs/sanity-webhook-setup.md)).

**Landing lead form (P2-AI-005 part 2, shipped 2026-07-07):** the three deltas that complete the landing pages — the Phase 1 form already had the right fields, so no new form was built. (1) **Per-page recipient**: `LeadForm` takes `landingSlug` → hidden field → [app/api/leads/route.ts](app/api/leads/route.ts) resolves the stored `leadRecipient` server-side (`getLandingLeadInfo` + the pure `resolveLandingLeadRouting` in [lib/leads/landing-lead.ts](lib/leads/landing-lead.ts)); the client can never choose a recipient (no open relay); invalid/blank/unknown → site default; no `landingSlug` → pre-part-2 behavior byte-for-byte. (2) **Customer confirmation, landing-only, non-fatal**: `sendCustomerConfirmationEmail` (gmail-smtp) after lead email + record, `replyTo` the landing recipient; content from the pure offline-verified builder. (3) **Editable CTA**: `heroCtaLabel` + `leadFormHeading` with automatic defaults; the generate route pre-fills `leadFormHeading` deterministically, patched only-if-empty. Offline harness extended to 77 checks. No webhook/tag/env changes.

## 24. AI Content Keyword & SEO Guidance (applies to all generated content)

Perfect Imprints sells **bulk, branded promotional products to businesses and organizations** (B2B, not consumer retail). All AI-generated content (blogs, videos, landing pages, catalog pages, category and page copy) must follow this:

- **Use plural keyword forms** (e.g. "custom water bottles", "branded tote bags", "personalized trade show giveaways"), not singular.
- **Target personas:** marketing directors, human resource directors, safety program managers, and business owners searching for bulk quantity promotional items.
- **Weave in modifier terms** that appear in real promotional-product searches: custom, customized, personalized, logo, printed, branded (and where natural: bulk, wholesale, corporate, promotional).
- **B2B framing:** minimum order quantities, decoration methods (screen printing, embroidery, pad printing, laser engraving, full-color heat transfer), use cases (trade shows, employee appreciation, safety programs, company stores, client gifts, giveaways), and lead times. Not single-unit consumer purchases.
- Product cards link to the Geiger affiliate site; the site funnels qualified leads, it does not sell directly.

## 25. Current Project State (updated 2026-07-04)

Phase 1 is LIVE in production (www.perfectimprints.com) and stable. All nine of Patrick's post-launch feedback items shipped, plus: custom pages at top-level `/<slug>` (`app/[slug]` with reserved-slug guard + schema validation + sitemap), page-builder inline images + video embeds, slash-tolerant footer/mega-menu links (`normalizeHref`), footer/mega-menu freshness fix (non-CDN client + `SETTINGS_TAG`/`MEGA_MENU_TAG`), the Site Refresh control panel (cookie-session nonce auth, GitHub PAT server-side, auto-merge, cancel), monthly rebuild workflow, warmup-monthly-only, custom-product filter fields (madeInUsa/ecoFriendly/closeout/features/types), the categoryOverride replace-products filter overlay, and the `sanitizeTagValue()` fix for the invalid-cache-tag 5xx incident (M5-525). Phase 2 (above) is the current active work.
