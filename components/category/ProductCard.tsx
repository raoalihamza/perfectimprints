// TODO: M3-302 - Product card with Geiger CDN image, affiliate URL, badges, MOQ.

export interface ProductCardProps {
  sku: string;
  name: string;
  imageUrl?: string | null;
  geigerUrl?: string | null;
}

export function ProductCard(_props: ProductCardProps) {
  return null;
}
