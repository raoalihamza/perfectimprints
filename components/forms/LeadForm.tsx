// TODO: M3-308 - Lead capture form (Name, Email, Company, Phone, Quantity, Message).
// POSTs to /api/leads, shows success/error inline, honeypot field, basic rate limiting.

export function LeadForm() {
  return null;
}
