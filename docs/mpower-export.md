# MPower export investigation

STUB for M4-401. Document findings here after investigating `app.mpowerpromo.com` for a clean blog export option.

Outline:

- Login flow and account access
- Available export formats (CSV, JSON, XML)
- Field coverage (does it include header images, inline images, authors, dates, categories?)
- Decision: use export or fall back to Playwright scraper
- Date investigated and who decided
