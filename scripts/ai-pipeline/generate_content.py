"""AI content generation pipeline. STUB for M2-204.

Reads PI URLs + Geiger mapping + Geiger products and invokes DeepSeek with the
appropriate prompt template per URL type. Output: data/categories/[encoded-slug].json.

See TASKS.md M2-204.
"""


def run() -> None:
    raise NotImplementedError(
        "Generation pipeline is scheduled for Module 2. See TASKS.md ticket M2-204."
    )
