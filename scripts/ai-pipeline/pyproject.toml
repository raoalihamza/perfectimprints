[project]
name = "perfectimprints-ai-pipeline"
version = "0.1.0"
description = "DeepSeek-V3 content generation pipeline for Perfect Imprints."
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",
    "tenacity>=8.2",
    "orjson>=3.9",
    "tqdm>=4.66",
]
