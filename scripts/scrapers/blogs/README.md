# Blog Scraper

STUB for M4-401. Will use Playwright to scrape the 731 perfectimprints.com blog posts when a clean MPower export is unavailable.

Investigate the MPower dashboard at `app.mpowerpromo.com` for an export option first. Document findings in `docs/mpower-export.md`.

Output target: `data/blogs/raw/[slug].json` plus images downloaded with hashed filenames.
