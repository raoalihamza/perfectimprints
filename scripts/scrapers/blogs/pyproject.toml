[project]
name = "perfectimprints-blog-scraper"
version = "0.2.0"
description = "Scrapes perfectimprints.com blog posts directly via SeleniumBase UC mode (Cloudflare Turnstile bypass)."
requires-python = ">=3.11"
dependencies = [
    "seleniumbase>=4.49",
]
