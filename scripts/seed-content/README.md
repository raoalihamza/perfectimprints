# Seed Content

STUB. Seeds the Sanity dataset with default `globalSettings` and `megaMenu` documents once a project exists. Schemas include `initialValue` so the Studio defaults are usable without seeding.
